---
phase: 02-ingredient-library
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-03", "02-04"]
files_modified:
  - app/Http/Controllers/Ingredients/IngredientController.php
  - app/Http/Controllers/Admin/IngredientVerificationController.php
  - app/Http/Requests/Admin/VerifyIngredientRequest.php
  - app/Http/Resources/IngredientDetailResource.php
  - routes/web.php
  - resources/js/pages/ingredients/show.tsx
  - resources/js/components/ingredients/nutrition-panel.tsx
  - resources/js/components/ingredients/allergen-panel.tsx
  - resources/js/components/ingredients/verify-action.tsx
  - resources/js/components/ui/tabs.tsx
  - resources/js/types/ingredient.ts
  - tests/Feature/Ingredients/IngredientDetailTest.php
  - tests/Feature/Ingredients/IngredientVerificationTest.php
autonomous: false
requirements: [INGR-03, INGR-04, INGR-05, INGR-06]

must_haves:
  truths:
    - "An ingredient has its own dedicated shareable detail page"
    - "The detail page shows full nutrition values grouped into sections"
    - "The detail page shows allergen data distinguishing contains from may contain"
    - "The detail page shows unit-conversion data and the category"
    - "A verified ingredient shows a verified badge with who verified it and when"
    - "A Moderator or Admin can mark an ingredient as verified; a plain User cannot"
  artifacts:
    - path: "resources/js/pages/ingredients/show.tsx"
      provides: "Ingredient detail page with tabbed sections"
      min_lines: 60
    - path: "app/Http/Controllers/Admin/IngredientVerificationController.php"
      provides: "Verify action gated by verify-ingredients permission"
      contains: "verify-ingredients"
    - path: "resources/js/components/ingredients/nutrition-panel.tsx"
      provides: "29-nutrient grouped panel"
      min_lines: 30
  key_links:
    - from: "resources/js/pages/ingredients/show.tsx"
      to: "/ingredients/{id} controller"
      via: "Inertia page render"
      pattern: "ingredients/show"
    - from: "app/Http/Controllers/Admin/IngredientVerificationController.php"
      to: "ingredients table"
      via: "set verified, verified_by, verified_at"
      pattern: "verified_by|verified_at"
    - from: "routes/web.php"
      to: "verify-ingredients permission"
      via: "permission middleware on the verify route"
      pattern: "permission:verify-ingredients"
---

<objective>
Build the ingredient detail page — the `IngredientController::show` action, an `IngredientDetailResource`, the tabbed detail page (Nutrition / Allergens / Conversions), and the moderator/admin verification flow with a permission-gated controller, FormRequest, and the verify-action UI with confirmation dialog.

Purpose: ROADMAP success criterion 2 (ingredients show nutrition, allergen flags, and unit-conversion data) and the CONTEXT verification decision — a moderator/admin confirms an ingredient's stored data is correct via a global verified flag.
Output: show action, detail resource, verification controller + request, the detail page with three panel components, the verify-action component, the tabs primitive.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ingredient-library/02-CONTEXT.md
@.planning/phases/02-ingredient-library/02-RESEARCH.md
@.planning/phases/02-ingredient-library/02-UI-SPEC.md
@.planning/phases/02-ingredient-library/02-01-SUMMARY.md
@.planning/phases/02-ingredient-library/02-03-SUMMARY.md
@.planning/phases/02-ingredient-library/02-04-SUMMARY.md

<interfaces>
<!-- Schema/models from 02-01; controller/routes from 02-03 and 02-04. -->

App\Models\Ingredient — 29 nutrition columns (decimal:4 cast), translations() hasMany, allergens() belongsToMany pivot 'state', conversions() hasMany, category() belongsTo (with parent), verifiedBy() belongsTo User, verified boolean, verified_at datetime, data_hash. Helper nameFor(locale), isPrivate().
App\Models\IngredientCategory — parent() / children(); breadcrumb is parent.name > category.name.
App\Models\IngredientConversion — from_amount, from_unit_id (unit() relation), gram_weight, modifier, source.

Phase 1 permission gate — routes use ->middleware('permission:NAME'). The verify-ingredients permission was added to Moderator + Admin in Plan 02-01's RolesAndPermissionsSeeder change. HandleInertiaRequests shares auth.permissions to the frontend (used by app-sidebar for canManageUsers).

02-03 created IngredientController with index(); routes/web.php has ingredients.index, ingredients.create, ingredients.store, ingredients.edit, ingredients.update, ingredients.destroy (02-04). IngredientListResource exists. resources/js/types/ingredient.ts exists. The index row Link and the create-form redirect both point at /ingredients/{id} — the show route added here makes those links resolve.

Inertia mutations RETURN A REDIRECT (back()->with('success', ...)). Existing admin verify-style routes live under the admin prefix with name 'admin.' — see routes/web.php admin.ingredients.index (review queue, Phase 7 territory — leave it untouched).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the show action, detail resource, verification controller + request, and routes</name>
  <files>app/Http/Controllers/Ingredients/IngredientController.php, app/Http/Resources/IngredientDetailResource.php, app/Http/Controllers/Admin/IngredientVerificationController.php, app/Http/Requests/Admin/VerifyIngredientRequest.php, routes/web.php, tests/Feature/Ingredients/IngredientDetailTest.php, tests/Feature/Ingredients/IngredientVerificationTest.php</files>
  <read_first>
    - app/Http/Controllers/Ingredients/IngredientController.php (02-03 — add show() alongside index())
    - app/Http/Resources/IngredientListResource.php (02-03 — resource pattern to mirror)
    - routes/web.php (the ingredients route group from 02-03/02-04; the admin permission-gated group for review-ingredients)
    - app/Http/Controllers/Admin/UserController.php (DB::transaction + redirect pattern)
    - tests/Feature/Ingredients/IngredientDetailTest.php, tests/Feature/Ingredients/IngredientVerificationTest.php (02-01 scaffolds)
    - .planning/phases/02-ingredient-library/02-CONTEXT.md (Ingredient verification decisions)
  </read_first>
  <behavior>
    - Test: `GET /ingredients/{id}` renders `ingredients/show` with nutrition, allergens, conversions, category props.
    - Test: a user CAN view an official ingredient's detail; a user CANNOT view another user's private ingredient (404 or 403).
    - Test: a Moderator POSTs `/admin/ingredients/{id}/verify` and the ingredient becomes verified with verified_by + verified_at set.
    - Test: an Admin can also verify; a plain User gets 403 on the verify route (permission gate).
    - Test: re-running an import that changes data_hash resets verified to false (covered by the import resetVerifiedForChangedRows logic — assert the end state).
  </behavior>
  <action>
**IngredientController::show** — add `show(Request $request, Ingredient $ingredient): Response` to the existing controller.
- Visibility guard: if `$ingredient->user_id !== null && $ingredient->user_id !== auth()->id()`, `abort(404)` (a private ingredient of another user must not be viewable).
- Eager-load `['translations', 'allergens', 'conversions.unit', 'category.parent', 'verifiedBy', 'prices' => fn ($q) => $q->where('user_id', auth()->id())->orderByDesc('recorded_at')]` (prices are scoped to the current user — pricing UI lands in 02-06; loading them now is harmless and the resource will expose them).
- Return `Inertia::render('ingredients/show', ['ingredient' => (new IngredientDetailResource($ingredient))->resolve(), 'can' => ['verify' => $request->user()->can('verify-ingredients'), 'manage' => $request->user()->can('update', $ingredient)], 'units' => Unit::orderBy('type')->orderBy('name')->get(['id','name','symbol','type'])])`. The `can.verify` flag uses the permission; `can.manage` uses the IngredientPolicy from 02-04.

**IngredientDetailResource** — `php artisan make:resource IngredientDetailResource`. `toArray()` returns the full shape: id, `name` (nameFor current locale), `name_en`, `name_el`, `is_private`, `category` => ['name' => ..., 'parent' => parent name], all 29 nutrition columns as `float|null`, `allergens` => map of {slug, name, state}, `conversions` => map of {from_amount, unit: {name, symbol}, gram_weight, modifier, source}, `verified` => bool, `verified_at` => ISO string|null, `verified_by` => verifier name|null, and `prices` => map of {id, amount, currency, quantity, unit, per_gram_cost, recorded_at, notes} (consumed by 02-06).

**IngredientVerificationController** (`php artisan make:controller Admin/IngredientVerificationController`) — single `store(VerifyIngredientRequest $request, Ingredient $ingredient): RedirectResponse` action. Inside `DB::transaction()`: set `$ingredient->verified = true; $ingredient->verified_by = auth()->id(); $ingredient->verified_at = now(); $ingredient->save();`. Redirect `back()->with('success', __('app.ingredients.verify_success'))`. The verify action is one-way (no revoke) per UI-SPEC.

**VerifyIngredientRequest** (`php artisan make:request Admin/VerifyIngredientRequest`) — `authorize()` returns `$this->user()->can('verify-ingredients')`; `rules()` returns `[]` (no body fields — the confirm dialog is purely a UI step).

**Routes** — in `routes/web.php`:
- Add the show route to the authenticated ingredients group, AFTER `ingredients/create` and `ingredients/{ingredient}/edit` so the wildcard does not shadow them:
```php
Route::get('ingredients/{ingredient}', [IngredientController::class, 'show'])->name('ingredients.show');
```
- Add the verify route. Put it inside the existing `admin` prefix group OR add a new permission-gated group. To match the UI-SPEC route `POST /admin/ingredients/{id}/verify`, add inside a group with `->prefix('admin')->name('admin.')` and `->middleware('permission:verify-ingredients')`:
```php
Route::post('ingredients/{ingredient}/verify', [IngredientVerificationController::class, 'store'])->name('ingredients.verify');
```
Resulting route name `admin.ingredients.verify`, path `/admin/ingredients/{ingredient}/verify`. Add the `use` import for `IngredientVerificationController`.

**Tests** — finalise `IngredientDetailTest.php` (detail renders with all sections; private-of-others returns 404) and `IngredientVerificationTest.php` (Moderator + Admin can verify, User gets 403, verified_by/verified_at set; assert the import-driven reset end state by importing-then-changing data via the IngredientImporter or a direct data_hash mismatch). Run `vendor/bin/pint --dirty --format agent` and `npm run build`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="IngredientDetailTest|IngredientVerificationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list --path=ingredients` lists `ingredients.show` (GET) and `admin.ingredients.verify` (POST)
    - Grep `routes/web.php` for `permission:verify-ingredients` returns a match
    - Grep `app/Http/Controllers/Admin/IngredientVerificationController.php` for `verified_by` and `verified_at` both return matches
    - Grep `app/Http/Controllers/Ingredients/IngredientController.php` for `function show` returns a match
    - Grep `app/Http/Controllers/Ingredients/IngredientController.php` for `abort(404)` returns a match (private visibility guard)
    - `php artisan test --compact --filter="IngredientDetailTest|IngredientVerificationTest"` exits 0
  </acceptance_criteria>
  <done>The show action renders a detail page with all data; private ingredients of others 404; the verify action is gated by the verify-ingredients permission and records who/when; both tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build the detail page, nutrition/allergen panels, and the verify-action component</name>
  <files>resources/js/pages/ingredients/show.tsx, resources/js/components/ingredients/nutrition-panel.tsx, resources/js/components/ingredients/allergen-panel.tsx, resources/js/components/ingredients/verify-action.tsx, resources/js/components/ui/tabs.tsx, resources/js/types/ingredient.ts</files>
  <read_first>
    - resources/js/pages/ingredients/index.tsx (02-03 — layout import, t() usage, app layout)
    - resources/js/components/ingredients/allergen-icons.tsx (02-03 — allergen rendering reference)
    - resources/js/types/ingredient.ts (02-03/02-04 — extend with IngredientDetail shape)
    - .planning/phases/02-ingredient-library/02-UI-SPEC.md (Component Inventory > Ingredient Detail Page, Nutrition panel grouping order, Interaction Contracts > Verify action, Responsive Contract)
  </read_first>
  <action>
**shadcn primitive** — run `npx shadcn@latest add tabs --yes`. Confirm `resources/js/components/ui/tabs.tsx` exists.

**Types** — extend `resources/js/types/ingredient.ts` with `IngredientDetail` (the full `IngredientDetailResource` shape: nutrition record, category {name, parent}, allergens, conversions, verified/verified_at/verified_by, prices), and a `CanFlags` type ({ verify: boolean, manage: boolean }).

**nutrition-panel.tsx** — `NutritionPanel`: a shadcn `Card` containing the 29 nutrient values grouped EXACTLY in the UI-SPEC order — 1 Energy, 2 Macros, 3 Fat detail, 4 Carb detail, 5 Minerals, 6 Vitamins, 7 Other. Each group has a small heading; each nutrient is a label-size (14px) name + body-size (16px) value row with its unit suffix (g / mg / µg / kcal). A null/missing value renders as `—` (em dash), NOT 0 (UI-SPEC). Responsive: single column mobile, two-column tablet, three-column desktop.

**allergen-panel.tsx** — `AllergenPanel`: renders a `Badge` per allergen present. "contains" allergens use `--destructive` foreground (text only, not a filled background); "may contain" use `--muted-foreground` on a `--muted` chip (UI-SPEC color contract). Allergens not present are not shown. If the ingredient has no allergens, show a neutral "no allergen data" line.

**verify-action.tsx** — `VerifyAction`: shown only when `can.verify` is true AND the ingredient is not yet verified. A `Button` variant="outline" labelled "Mark as Verified". Clicking opens a shadcn `Dialog` with `DialogTitle` + `DialogDescription` body "Confirm that all stored data for this ingredient is correct." and buttons "Confirm Verification" (submits a `router.post` to the `admin.ingredients.verify` Wayfinder route) and "Keep Unverified" (closes). On success the controller redirects, the badge appears, and a sonner toast "Ingredient verified." fires. When the ingredient IS verified, render instead the static text "Verified on {date} by {name}".

**show.tsx** — `IngredientShow` page wrapped in the app layout:
- Header: `h1` Display-size ingredient name in the user locale, with the verified `Badge` (accent background) inline when verified.
- A `Breadcrumb` showing Category > Subcategory.
- `<VerifyAction>` near the header (for Mod/Admin).
- If `can.manage` is true (owner of a private ingredient), an Edit button linking to `ingredients.edit` and a Delete button opening a destructive confirmation `Dialog` ("Delete {name}? This cannot be undone." / "Delete" / "Keep Ingredient") that `router.delete`s the `ingredients.destroy` route.
- A shadcn `Tabs` component with tabs Nutrition / Allergens / Conversions. (The Prices tab is added in Plan 02-06 — leave a clear extension point: render the three tabs now, structured so a fourth tab slots in.)
  - Nutrition tab: `<NutritionPanel>`.
  - Allergens tab: `<AllergenPanel>`.
  - Conversions tab: a shadcn `Table` with columns Amount / Unit / Gram weight / Source. Empty state: the conversions-empty copy from the contract.
- Responsive: tabs stack vertically on mobile (UI-SPEC).
- "Ingredient not found" handling — the controller 404s; rely on the standard error page (no extra work).

Use `cn()`, `useLaravelReactI18n` `t()` for all copy (all strings already exist in lang files from 02-03). Run `npm run build` and `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `resources/js/components/ui/tabs.tsx` exists
    - Grep `resources/js/components/ingredients/nutrition-panel.tsx` for `energy_kcal` and `vitamin_a_ug` both return matches
    - Grep `resources/js/components/ingredients/nutrition-panel.tsx` for `—` (em dash) returns a match
    - Grep `resources/js/components/ingredients/verify-action.tsx` for `Mark as Verified` or `verify_action` returns a match
    - Grep `resources/js/components/ingredients/allergen-panel.tsx` for `may_contain` or `destructive` returns a match
    - Grep `resources/js/pages/ingredients/show.tsx` for `Tabs` returns a match
    - All listed files exist
  </acceptance_criteria>
  <done>The detail page renders a grouped 29-nutrient panel, contains/may-contain allergen panel, conversions table, breadcrumb, verified badge, owner edit/delete, and the Mod/Admin verify flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the end-to-end ingredient library flow</name>
  <action>Pause for the user to verify the running application. This checkpoint follows the automated work in Tasks 1 and 2 — it does not replace it.</action>
  <what-built>The ingredient detail page (`/ingredients/{id}`) with tabbed Nutrition / Allergens / Conversions sections, the verified badge, the owner edit/delete controls, and the Moderator/Admin "Mark as Verified" flow with confirmation dialog. Also the search/browse page from Plan 02-03 and the create form from Plan 02-04 are now linkable end to end.</what-built>
  <how-to-verify>
    1. Run `php artisan migrate:fresh --seed` then import a sample: `php artisan ingredients:import-ciqual` (or with `--source-file=tests/fixtures/ingredients/ciqual-sample.xml`).
    2. Visit the app (Herd serves it at the project `.test` URL — use the get-absolute-url tool if unsure) and log in.
    3. Open "Ingredients" from the sidebar nav. Confirm the search page loads with a focal search input. Type a few characters and confirm results update live without a full page reload, and the URL query string updates.
    4. Toggle the Source, Allergen-free, and Verified-only filters and confirm results change.
    5. Click a result row. Confirm the detail page shows the grouped nutrition panel (missing values show "—"), the Allergens tab distinguishes "Contains" from "May contain", and the Conversions tab shows a table or its empty state.
    6. As a Moderator or Admin account, confirm the "Mark as Verified" button appears; click it, confirm the dialog, and confirm the verified badge appears and a toast shows. As a plain User, confirm the button does not appear.
    7. Confirm light and dark themes both render correctly, and the page is usable at mobile width.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter="IngredientDetailTest|IngredientVerificationTest"` passes.
- `npm run build` succeeds.
- The detail page shows nutrition, allergens, conversions, and category.
- Only Moderator/Admin can verify; verification records who and when.
- Human checkpoint confirms the end-to-end search → detail → verify flow.
</verification>

<success_criteria>
- Each ingredient has a dedicated tabbed detail page.
- Full 29-nutrient panel grouped per the UI spec; contains/may-contain allergen panel; conversions table.
- The verify action is permission-gated and one-way, recording verifier and timestamp.
- The verified badge appears in search results and on the detail page.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingredient-library/02-05-SUMMARY.md`
</output>
