---
phase: 02-ingredient-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/XXXX_create_ingredient_categories_table.php
  - database/migrations/XXXX_create_ingredients_table.php
  - database/migrations/XXXX_create_ingredient_translations_table.php
  - database/migrations/XXXX_create_ingredient_allergen_table.php
  - database/migrations/XXXX_create_ingredient_conversions_table.php
  - database/migrations/XXXX_create_ingredient_prices_table.php
  - app/Models/Ingredient.php
  - app/Models/IngredientCategory.php
  - app/Models/IngredientTranslation.php
  - app/Models/IngredientConversion.php
  - app/Models/IngredientPrice.php
  - app/Models/User.php
  - app/Models/Allergen.php
  - database/factories/IngredientFactory.php
  - database/factories/IngredientCategoryFactory.php
  - database/factories/IngredientTranslationFactory.php
  - database/factories/IngredientConversionFactory.php
  - database/factories/IngredientPriceFactory.php
  - database/seeders/IngredientCategorySeeder.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Ingredients/IngredientSchemaTest.php
  - tests/Feature/Ingredients/IngredientSearchTest.php
  - tests/Feature/Ingredients/ImportCiqualTest.php
  - tests/Feature/Ingredients/ImportUsdaTest.php
  - tests/Feature/Ingredients/ImportOpenFoodFactsTest.php
  - tests/Feature/Ingredients/IngredientDetailTest.php
  - tests/Feature/Ingredients/IngredientAllergenTest.php
  - tests/Feature/Ingredients/IngredientConversionTest.php
  - tests/Feature/Ingredients/IngredientTranslationTest.php
  - tests/Feature/Ingredients/PrivateIngredientTest.php
  - tests/Feature/Ingredients/IngredientPriceTest.php
  - tests/Feature/Ingredients/IngredientVerificationTest.php
autonomous: true
requirements: [INGR-03, INGR-04, INGR-05, INGR-06, INGR-08]

must_haves:
  truths:
    - "The ingredients table stores nutrition, allergen, conversion, and verification data"
    - "An ingredient can carry names in multiple languages (Greek and English minimum)"
    - "An ingredient is linked to a required 2-level category"
    - "An ingredient can carry contains / may_contain allergen flags"
    - "An ingredient can carry per-portion gram-weight conversion rows"
    - "A user can have private ingredients and price records distinct from official ones"
    - "The category tree is seeded with a default expandable set"
  artifacts:
    - path: "database/migrations/XXXX_create_ingredients_table.php"
      provides: "ingredients table — official + private, nutrition, verification, reserved fields"
      contains: "Schema::create('ingredients'"
    - path: "app/Models/Ingredient.php"
      provides: "Ingredient Eloquent model with all relationships"
      min_lines: 60
    - path: "database/seeders/IngredientCategorySeeder.php"
      provides: "Default 13-category / 41-subcategory tree"
      contains: "firstOrCreate"
    - path: "tests/Feature/Ingredients/IngredientSchemaTest.php"
      provides: "Wave 0 schema + relationship assertions"
      contains: "test("
  key_links:
    - from: "app/Models/Ingredient.php"
      to: "ingredient_translations table"
      via: "translations() hasMany"
      pattern: "function translations"
    - from: "app/Models/Ingredient.php"
      to: "allergens table (Phase 1)"
      via: "allergens() belongsToMany with state pivot"
      pattern: "belongsToMany.*Allergen"
    - from: "database/seeders/RolesAndPermissionsSeeder.php"
      to: "verify-ingredients permission"
      via: "Permission::firstOrCreate"
      pattern: "verify-ingredients"
---

<objective>
Build the complete ingredient data schema — six tables, five Eloquent models with relationships, factories, the default category-tree seeder, the `verify-ingredients` permission — and scaffold every Pest test file for the phase with real (initially failing) assertions.

Purpose: Every other plan in this phase (import pipeline, search UI, detail page, private CRUD, pricing) depends on this schema and these models existing. The Wave 0 test scaffold gives later waves concrete red-to-green targets per the Phase 1 established pattern.
Output: Migrations, models, factories, seeders, permission, and 11 scaffolded test files.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingredient-library/02-CONTEXT.md
@.planning/phases/02-ingredient-library/02-RESEARCH.md

<interfaces>
<!-- Phase 1 lookup tables this schema references. Already migrated and seeded. -->

units table (migration 2026_05_16_024902): columns id, name, symbol, type ('weight'|'volume'|'count'), base_factor DECIMAL(16,6) nullable, timestamps. Model App\Models\Unit, fillable ['name','symbol','type','base_factor'], cast base_factor decimal:6.

allergens table (migration 2026_05_16_024914): columns id, name, slug (unique), note nullable, timestamps. Model App\Models\Allergen, fillable ['name','slug','note']. Seeded with all 14 EU Reg. 1169/2011 allergens by slug.

users table: standard Laravel users with account_status enum and locale column added in Phase 1.

Phase 1 permission pattern (RolesAndPermissionsSeeder): permissions are flat strings created via Permission::firstOrCreate(['name' => $perm]); existing permissions are 'create-recipes', 'manage-own-ingredients', 'review-ingredients', 'manage-users'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the six ingredient migrations</name>
  <files>database/migrations/XXXX_create_ingredient_categories_table.php, database/migrations/XXXX_create_ingredients_table.php, database/migrations/XXXX_create_ingredient_translations_table.php, database/migrations/XXXX_create_ingredient_allergen_table.php, database/migrations/XXXX_create_ingredient_conversions_table.php, database/migrations/XXXX_create_ingredient_prices_table.php</files>
  <read_first>
    - database/migrations/2026_05_16_024902_create_units_table.php (existing migration style — anonymous class, up/down, unique key style)
    - database/migrations/2026_05_16_024914_create_allergens_table.php
    - .planning/phases/02-ingredient-library/02-RESEARCH.md (section "Proposed Database Schema" — authoritative column list)
  </read_first>
  <behavior>
    - Test: each of the 6 tables exists after migrate (Schema::hasTable).
    - Test: ingredients table has a unique index on (source, source_id).
    - Test: ingredient_translations has a unique index on (ingredient_id, locale).
    - Test: ingredient_allergen has a unique index on (ingredient_id, allergen_id).
    - Test: a FULLTEXT index on ingredient_translations.name is created only when the DB driver is mysql (SQLite test run must not error).
  </behavior>
  <action>
Create the migrations with `php artisan make:migration` (one per table) so timestamps order correctly: ingredient_categories first, then ingredients, then ingredient_translations, ingredient_allergen, ingredient_conversions, ingredient_prices. Use the anonymous-class style from the units migration.

**ingredient_categories** — `id`; `parent_id` nullable, `foreignId('parent_id')->nullable()->constrained('ingredient_categories')->nullOnDelete()` (2-level tree: NULL parent = top category, set = subcategory); `name` string; `slug` string unique; `sort_order` unsignedInteger default 0; `timestamps()`.

**ingredients** — `id`; `foreignId('user_id')->nullable()->constrained()->cascadeOnDelete()` (NULL = official, set = private); `foreignId('category_id')->constrained('ingredient_categories')` (required — restrict on delete); `string('source')` then a CHECK is unnecessary, store enum values 'ciqual'|'usda'|'off'|'user' as a plain string column; `string('source_id')` (source's stable key — alim_code / fdc_id / barcode / generated uuid for user); `string('usda_fdc_id')->nullable()`; `string('name_cache')` (denormalised primary-locale name for ORDER BY); `boolean('verified')->default(false)`; `foreignId('verified_by')->nullable()->constrained('users')->nullOnDelete()`; `timestamp('verified_at')->nullable()`; `string('data_hash', 32)->nullable()` (md5 of imported payload, gates verified reset); `string('foodex2_code')->nullable()` (reserved, unused this phase).
Nutrition columns — all `decimal(10,4)->nullable()`: `energy_kcal`, `protein_g`, `fat_g`, `saturated_fat_g`, `monounsaturated_fat_g`, `polyunsaturated_fat_g`, `carbs_g`, `sugars_g`, `starch_g`, `fibre_g`, `sodium_mg`, `calcium_mg`, `iron_mg`, `magnesium_mg`, `phosphorus_mg`, `potassium_mg`, `zinc_mg`, `vitamin_a_ug`, `vitamin_b1_mg`, `vitamin_b2_mg`, `vitamin_b3_mg`, `vitamin_b6_mg`, `vitamin_b9_ug`, `vitamin_b12_ug`, `vitamin_c_mg`, `vitamin_d_ug`, `vitamin_e_mg`, `vitamin_k_ug`, `cholesterol_mg` (29 nutrition columns total).
Reserved frozen-dessert columns — all `decimal(8,4)->nullable()`: `total_solids_pct`, `fat_pct`, `msnf_pct`, `sugar_pct`, `other_solids_pct`, `water_pct`, `pac_coefficient`, `pod_coefficient`, `de_value`, `brix`; plus `string('ingredient_class')->nullable()` (values: sugar|dairy|fat|fruit|stabilizer|emulsifier|cocoa|alcohol|egg — stored as plain string, unused this phase).
Finish with `timestamps()`, `softDeletes()`, `$table->unique(['source', 'source_id'])`, and `$table->index('category_id')`.

**ingredient_translations** — `id`; `foreignId('ingredient_id')->constrained()->cascadeOnDelete()`; `string('locale', 10)`; `string('name', 500)`; `text('description')->nullable()`; `timestamps()`; `$table->unique(['ingredient_id', 'locale'])`. After the `Schema::create` closure, add the MySQL-only FULLTEXT index guarded by driver check:
```php
if (DB::getDriverName() === 'mysql') {
    DB::statement('ALTER TABLE ingredient_translations ADD FULLTEXT INDEX ft_name (name)');
}
```
Import `use Illuminate\Support\Facades\DB;` at the top.

**ingredient_allergen** (pivot) — `id`; `foreignId('ingredient_id')->constrained()->cascadeOnDelete()`; `foreignId('allergen_id')->constrained()->cascadeOnDelete()`; `string('state')` (values 'contains'|'may_contain'); `timestamps()`; `$table->unique(['ingredient_id', 'allergen_id'])`.

**ingredient_conversions** — `id`; `foreignId('ingredient_id')->constrained()->cascadeOnDelete()`; `decimal('from_amount', 10, 4)`; `foreignId('from_unit_id')->constrained('units')`; `decimal('gram_weight', 10, 4)`; `string('modifier', 100)->nullable()`; `string('source')` (values 'usda'|'user'|'curated'); `string('source_ref', 50)->nullable()`; `timestamps()`; `$table->index('ingredient_id')`.

**ingredient_prices** — `id`; `foreignId('user_id')->constrained()->cascadeOnDelete()`; `foreignId('ingredient_id')->constrained()->cascadeOnDelete()`; `decimal('amount', 12, 4)`; `string('currency', 3)->default('EUR')`; `decimal('quantity', 10, 4)`; `foreignId('unit_id')->constrained('units')`; `decimal('per_gram_cost', 16, 8)->nullable()`; `date('recorded_at')`; `text('notes')->nullable()`; `timestamps()`; `$table->index(['user_id', 'ingredient_id', 'recorded_at'])`.

Each migration's `down()` calls `Schema::dropIfExists()` for its table. Run `php artisan migrate` to confirm they apply, then `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan migrate --no-interaction && php artisan test --compact --filter=IngredientSchemaTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate --no-interaction` exits 0
    - Grep `database/migrations` for `Schema::create('ingredients'` returns a match
    - Grep `database/migrations` for `unique(['source', 'source_id'])` returns a match
    - Grep `database/migrations` for `ADD FULLTEXT INDEX ft_name` returns a match
    - Grep the ingredients migration for `energy_kcal` and `pac_coefficient` both return matches
    - Grep the ingredients migration for `softDeletes` returns a match
  </acceptance_criteria>
  <done>All six tables migrate cleanly on SQLite; unique indexes present; FULLTEXT index guarded behind the mysql driver check so SQLite test runs do not error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the five models, factories, the verify-ingredients permission, and the category seeder</name>
  <files>app/Models/Ingredient.php, app/Models/IngredientCategory.php, app/Models/IngredientTranslation.php, app/Models/IngredientConversion.php, app/Models/IngredientPrice.php, app/Models/User.php, app/Models/Allergen.php, database/factories/IngredientFactory.php, database/factories/IngredientCategoryFactory.php, database/factories/IngredientTranslationFactory.php, database/factories/IngredientConversionFactory.php, database/factories/IngredientPriceFactory.php, database/seeders/IngredientCategorySeeder.php, database/seeders/RolesAndPermissionsSeeder.php, database/seeders/DatabaseSeeder.php</files>
  <read_first>
    - app/Models/Unit.php (model + casts() style)
    - app/Models/Allergen.php (existing model to extend)
    - database/seeders/RolesAndPermissionsSeeder.php (permission firstOrCreate pattern — DO NOT remove existing permissions)
    - database/seeders/UnitSeeder.php (idempotent seeder pattern)
    - database/seeders/DatabaseSeeder.php (seeder call order)
    - database/factories/UserFactory.php (factory style)
    - .planning/phases/02-ingredient-library/02-RESEARCH.md (section "Starter Category Tree" — exact names; section "Pattern 1/5")
  </read_first>
  <behavior>
    - Test: Ingredient::factory()->create() persists a row with a category and source.
    - Test: $ingredient->translations()->create([...]) and $ingredient->nameFor('el') returns the Greek name, falling back to 'en' then '—'.
    - Test: $ingredient->allergens()->sync([$id => ['state' => 'contains']]) stores the pivot state.
    - Test: $ingredient->conversions, $ingredient->prices, $ingredient->category, $ingredient->verifiedBy relationships resolve.
    - Test: IngredientCategorySeeder run twice creates exactly 13 top categories + 41 subcategories (no duplicates).
    - Test: Permission named 'verify-ingredients' exists and is assigned to Moderator and Admin roles after seeding.
  </behavior>
  <action>
Create models with `php artisan make:model` (use `-f` for factory where noted). Match the `App\Models\Unit` style — `protected $fillable`, `protected function casts(): array`, explicit return types on relationship methods.

**Ingredient** (`app/Models/Ingredient.php`) — use `SoftDeletes` trait and `HasFactory`. `$fillable`: user_id, category_id, source, source_id, usda_fdc_id, name_cache, verified, verified_by, verified_at, data_hash, foodex2_code, and all 29 nutrition columns, plus the 11 reserved frozen-dessert columns. `casts()`: `verified => 'boolean'`, `verified_at => 'datetime'`, and `decimal:4` for every nutrition column, `decimal:4` for the 10 reserved decimal columns.
Relationships:
```php
public function category(): BelongsTo { return $this->belongsTo(IngredientCategory::class, 'category_id'); }
public function user(): BelongsTo { return $this->belongsTo(User::class); }
public function verifiedBy(): BelongsTo { return $this->belongsTo(User::class, 'verified_by'); }
public function translations(): HasMany { return $this->hasMany(IngredientTranslation::class); }
public function conversions(): HasMany { return $this->hasMany(IngredientConversion::class); }
public function prices(): HasMany { return $this->hasMany(IngredientPrice::class); }
public function allergens(): BelongsToMany { return $this->belongsToMany(Allergen::class, 'ingredient_allergen')->withPivot('state')->withTimestamps(); }
```
Add helpers:
```php
public function nameFor(string $locale): string
{
    return $this->translations->firstWhere('locale', $locale)?->name
        ?? $this->translations->firstWhere('locale', 'en')?->name
        ?? '—';
}
public function isOfficial(): bool { return $this->user_id === null; }
public function isPrivate(): bool { return $this->user_id !== null; }
```

**IngredientCategory** — `$fillable`: parent_id, name, slug, sort_order. Relationships: `parent(): BelongsTo` to self, `children(): HasMany` to self via parent_id, `ingredients(): HasMany`.

**IngredientTranslation** — `$fillable`: ingredient_id, locale, name, description. `ingredient(): BelongsTo`.

**IngredientConversion** — `$fillable`: ingredient_id, from_amount, from_unit_id, gram_weight, modifier, source, source_ref. `casts()`: `from_amount => 'decimal:4'`, `gram_weight => 'decimal:4'`. `ingredient(): BelongsTo`, `unit(): BelongsTo` to Unit via from_unit_id.

**IngredientPrice** — `$fillable`: user_id, ingredient_id, amount, currency, quantity, unit_id, per_gram_cost, recorded_at, notes. `casts()`: `amount => 'decimal:4'`, `quantity => 'decimal:4'`, `per_gram_cost => 'decimal:8'`, `recorded_at => 'date'`. `ingredient(): BelongsTo`, `user(): BelongsTo`, `unit(): BelongsTo`.

**User model** (`app/Models/User.php`) — add two relationship methods: `ingredients(): HasMany` (private ingredients owned), `ingredientPrices(): HasMany`. Import the HasMany class.

Factories — create `IngredientFactory`, `IngredientCategoryFactory`, `IngredientTranslationFactory`, `IngredientConversionFactory`, `IngredientPriceFactory`. `IngredientFactory` defaults: `user_id => null` (official), `category_id => IngredientCategory::factory()`, `source => 'user'`, `source_id => fake()->unique()->uuid()`, `name_cache => fake()->words(2, true)`, `verified => false`, sensible random decimals for `energy_kcal`/`protein_g`/`fat_g`/`carbs_g`, nulls for the rest. Add a factory state `->private(User $user)` setting `user_id` and `source => 'user'`, and a state `->verified(User $verifier)` setting verified true + verified_by + verified_at. `IngredientTranslationFactory` defaults `locale => 'en'`. `IngredientCategoryFactory` defaults `parent_id => null`, slug from name.

**RolesAndPermissionsSeeder** — add `'verify-ingredients'` to BOTH `$moderatorPermissions` and (transitively) `$adminPermissions`. Concretely: change `$moderatorPermissions = array_merge($userPermissions, ['review-ingredients']);` to `$moderatorPermissions = array_merge($userPermissions, ['review-ingredients', 'verify-ingredients']);`. Do not remove any existing permission.

**IngredientCategorySeeder** (`database/seeders/IngredientCategorySeeder.php`) — seed the exact 13-category / 41-subcategory tree from RESEARCH.md "Starter Category Tree". For each top category: `IngredientCategory::firstOrCreate(['slug' => $slug], ['name' => $name, 'parent_id' => null, 'sort_order' => $i])`. For each subcategory: resolve the parent by slug, then `firstOrCreate(['slug' => $subSlug], ['name' => $subName, 'parent_id' => $parent->id, 'sort_order' => $j])`. Use a stable slug per row (kebab-case of the English name). The 13 top categories: Vegetables, Fruits, Grains & Starches, Dairy & Eggs, Meat & Poultry, Fish & Seafood, Oils Fats & Condiments, Herbs & Spices, Nuts & Seeds, Sweeteners & Sugar Products, Beverages, Prepared & Convenience Foods, Other / Uncategorised — with all subcategories listed under each in RESEARCH.md.

**DatabaseSeeder** — add `$this->call(IngredientCategorySeeder::class);` after `AllergenSeeder` and before the test-user creation.

Run `php artisan migrate:fresh --seed --no-interaction` then `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed --no-interaction && php artisan test --compact --filter=IngredientSchemaTest</automated>
  </verify>
  <acceptance_criteria>
    - Grep `app/Models/Ingredient.php` for `function translations`, `function allergens`, `function nameFor` — all return matches
    - Grep `app/Models/Ingredient.php` for `SoftDeletes` returns a match
    - Grep `app/Models/User.php` for `function ingredients` returns a match
    - Grep `database/seeders/RolesAndPermissionsSeeder.php` for `verify-ingredients` returns a match
    - Grep `database/seeders/RolesAndPermissionsSeeder.php` for `review-ingredients` still returns a match (existing permission preserved)
    - Grep `database/seeders/DatabaseSeeder.php` for `IngredientCategorySeeder` returns a match
    - `php artisan migrate:fresh --seed --no-interaction` exits 0
    - `php artisan tinker --execute 'echo App\Models\IngredientCategory::whereNull("parent_id")->count();'` outputs 13
  </acceptance_criteria>
  <done>Five models with all relationships exist; factories produce valid rows; the verify-ingredients permission is seeded onto Moderator + Admin; the 13/41 category tree seeds idempotently.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold all 11 phase test files with real failing assertions</name>
  <files>tests/Feature/Ingredients/IngredientSchemaTest.php, tests/Feature/Ingredients/IngredientSearchTest.php, tests/Feature/Ingredients/ImportCiqualTest.php, tests/Feature/Ingredients/ImportUsdaTest.php, tests/Feature/Ingredients/ImportOpenFoodFactsTest.php, tests/Feature/Ingredients/IngredientDetailTest.php, tests/Feature/Ingredients/IngredientAllergenTest.php, tests/Feature/Ingredients/IngredientConversionTest.php, tests/Feature/Ingredients/IngredientTranslationTest.php, tests/Feature/Ingredients/PrivateIngredientTest.php, tests/Feature/Ingredients/IngredientPriceTest.php, tests/Feature/Ingredients/IngredientVerificationTest.php</files>
  <read_first>
    - tests/Feature/Lookup (existing Phase 1 lookup tests — closure style, RefreshDatabase usage)
    - tests/Pest.php (how RefreshDatabase / uses() is wired)
    - .planning/phases/02-ingredient-library/02-RESEARCH.md (section "Phase Requirements → Test Map")
  </read_first>
  <action>
Create the 11 test files with `php artisan make:test --pest` (omit the Feature/ prefix; pass e.g. `Ingredients/IngredientSchemaTest`). Each file uses Pest closure style and the project's RefreshDatabase wiring from tests/Pest.php. Write REAL assertions (no `->skip()`), matching the Phase 1 decision that Wave 0 tests write concrete red-to-green targets. Tasks 1 and 2 of this plan make IngredientSchemaTest, IngredientAllergenTest, IngredientConversionTest, and IngredientTranslationTest pass; the other 7 stay red until their owning plan executes — that is expected and correct.

**IngredientSchemaTest** — assert all 6 tables exist (`Schema::hasTable`), the unique index on ingredients (source, source_id) prevents duplicates, an Ingredient factory row persists, the IngredientCategory tree has 13 roots after seeding the category seeder. (Passes after Task 2.)

**IngredientAllergenTest** — create an ingredient, sync an allergen with state 'contains' and another with 'may_contain', assert `$ingredient->allergens` returns both with the correct pivot `state`. (Passes after Task 2.)

**IngredientConversionTest** — create an ingredient with a conversion row (`1 cup -> 240 g`), assert `$ingredient->conversions->first()->gram_weight` equals '240.0000' and the `unit` relation resolves. (Passes after Task 2.)

**IngredientTranslationTest** — create an ingredient with en + el translations, assert `nameFor('el')` returns the Greek name, `nameFor('fr')` falls back to the English name, an ingredient with no translations returns '—'. (Passes after Task 2.)

**IngredientSearchTest** — assert `GET /ingredients` (authenticated) renders `ingredients/index`; searching by `?search=` a known English name returns that ingredient; searching by a known Greek name also returns it; an empty search returns the paginated full list. (Red until Plan 02-03.)

**IngredientDetailTest** — assert `GET /ingredients/{id}` renders `ingredients/show` with nutrition, allergen, conversion, and category props. (Red until Plan 02-05.)

**ImportCiqualTest / ImportUsdaTest / ImportOpenFoodFactsTest** — assert running the command (`ingredients:import-ciqual` etc.) with a fixture file argument creates ingredient rows, and running it a second time does NOT duplicate them (count is stable). For OFF, also assert a Greek translation is added to a matched ingredient. (Red until Plan 02-02.)

**PrivateIngredientTest** — assert a user can POST a private ingredient with name + category (minimum), it gets `user_id` set; another user does NOT see it in their library; the owner can update and delete it; a non-owner gets 403 on update/delete. (Red until Plan 02-04.)

**IngredientPriceTest** — assert a user can POST a price (`amount`, `quantity`, `unit_id`, `currency`, `recorded_at`) for an ingredient, `per_gram_cost` is computed, the price is visible only to its creator, full history is kept. (Red until Plan 02-06.)

**IngredientVerificationTest** — assert a Moderator/Admin can POST `/admin/ingredients/{id}/verify` and the ingredient becomes verified with `verified_by` + `verified_at` set; a plain User gets 403; re-running an import that changes `data_hash` resets `verified` to false. (Red until Plans 02-02 and 02-05.)

Run the schema/allergen/conversion/translation tests now to confirm they are green; the other 7 are expected red.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="IngredientSchemaTest|IngredientAllergenTest|IngredientConversionTest|IngredientTranslationTest"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 files exist under `tests/Feature/Ingredients/`
    - Grep `tests/Feature/Ingredients/` for `->skip(` returns NO matches (real assertions, not skipped)
    - `php artisan test --compact --filter="IngredientSchemaTest|IngredientAllergenTest|IngredientConversionTest|IngredientTranslationTest"` exits 0 (these 4 pass)
    - Grep each of the 11 files for `test(` returns at least one match
  </acceptance_criteria>
  <done>All 11 test files exist with real assertions; the 4 schema/model tests pass; the remaining 7 are red, awaiting their owning plans.</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh --seed --no-interaction` succeeds.
- The four schema/model tests (IngredientSchemaTest, IngredientAllergenTest, IngredientConversionTest, IngredientTranslationTest) pass.
- The ingredients table contains all 29 nutrition columns + 11 reserved frozen-dessert columns.
- The verify-ingredients permission is seeded onto Moderator and Admin.
- The category tree has 13 roots and 41 subcategories.
</verification>

<success_criteria>
- Six ingredient tables exist with correct columns and unique indexes.
- Five Eloquent models with full relationships and helper methods.
- Factories produce valid rows for every model.
- Default category tree seeds idempotently.
- All 11 Pest test files scaffolded with real assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingredient-library/02-01-SUMMARY.md`
</output>
