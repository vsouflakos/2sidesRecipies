---
phase: 02-ingredient-library
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/Http/Controllers/Ingredients/IngredientController.php
  - app/Http/Resources/IngredientListResource.php
  - routes/web.php
  - resources/js/pages/ingredients/index.tsx
  - resources/js/components/ingredients/ingredient-row.tsx
  - resources/js/components/ingredients/allergen-icons.tsx
  - resources/js/components/ingredients/ingredient-filters.tsx
  - resources/js/components/app-sidebar.tsx
  - resources/js/components/ui/scroll-area.tsx
  - resources/js/components/ui/popover.tsx
  - resources/js/types/ingredient.ts
  - lang/en/app.php
  - lang/el/app.php
  - tests/Feature/Ingredients/IngredientSearchTest.php
autonomous: true
requirements: [INGR-01, INGR-06]

must_haves:
  truths:
    - "An authenticated user can open the Ingredient Library page from the nav"
    - "A user can search the library by name and see live as-you-type results"
    - "Search matches both Greek and English names"
    - "Results show ingredient name in the user's locale, calories, and allergen icons"
    - "A user can filter by source, allergen-free, and verified-only"
    - "Result rows display a verified badge when the ingredient is verified"
  artifacts:
    - path: "app/Http/Controllers/Ingredients/IngredientController.php"
      provides: "index() search/browse action"
      contains: "function index"
    - path: "resources/js/pages/ingredients/index.tsx"
      provides: "Search + browse Inertia page"
      min_lines: 60
    - path: "resources/js/components/ingredients/ingredient-row.tsx"
      provides: "Compact 48px result row"
      min_lines: 20
  key_links:
    - from: "resources/js/pages/ingredients/index.tsx"
      to: "/ingredients route"
      via: "router.reload partial with only: ['ingredients']"
      pattern: "router.reload"
    - from: "app/Http/Controllers/Ingredients/IngredientController.php"
      to: "ingredient_translations table"
      via: "whereHas translations name search"
      pattern: "translations"
    - from: "resources/js/components/app-sidebar.tsx"
      to: "/ingredients"
      via: "Ingredients nav entry"
      pattern: "ingredients"
---

<objective>
Build the ingredient search and browse page — the `IngredientController::index` action with name search across Greek and English translations and the three launch filters, the live debounced Inertia search UI, the compact 48px result rows with allergen icons and verified badges, and the "Ingredients" nav entry.

Purpose: ROADMAP success criterion 1 — a user can search the official ingredient library by name and get results in Greek and English. This is the library's primary surface, a reference tool used constantly while building recipes.
Output: Search controller, list resource, the index page with rows/filters/allergen-icons components, two new shadcn primitives, nav entry, i18n strings.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ingredient-library/02-CONTEXT.md
@.planning/phases/02-ingredient-library/02-RESEARCH.md
@.planning/phases/02-ingredient-library/02-UI-SPEC.md
@.planning/phases/02-ingredient-library/02-01-SUMMARY.md

<interfaces>
<!-- Schema/models from Plan 02-01. Phase 1 patterns to mirror. -->

App\Models\Ingredient — translations() hasMany (locale, name), allergens() belongsToMany pivot 'state', category() belongsTo, nameFor(locale) helper, name_cache column for ORDER BY, verified boolean, user_id (null = official).
App\Models\Allergen (Phase 1) — id, name, slug; 14 EU allergens.

Phase 1 search reference — App\Http\Controllers\Admin\UserController::index():
  reads $request->string('search'), uses ->when($search, fn ($q) => $q->where('name','like',"%{$search}%")), ->paginate(15)->withQueryString()->through(fn ($u) => [...]), returns Inertia::render('admin/users', ['users' => ..., 'filters' => ['search' => ...]]).
  Inertia mutations redirect with back()->with('success', ...).

Phase 1 nav reference — resources/js/components/app-sidebar.tsx: builds mainNavItems array; gates the Users entry on permissions.includes('manage-users'); uses useLaravelReactI18n t() and lucide icons; imports route helpers from @/routes/admin/users.

i18n — lang/en/app.php and lang/el/app.php are PHP arrays returning nested keys ('nav' => [...], 'admin' => [...]). Strings referenced in React via useLaravelReactI18n t('app.section.key').

Web routes — routes/web.php; authenticated routes go inside Route::middleware(['auth','verified'])->group(). Wayfinder regenerates @/actions and @/routes on build — never hand-edit those.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two shadcn primitives, ingredient routes, and the search controller</name>
  <files>resources/js/components/ui/scroll-area.tsx, resources/js/components/ui/popover.tsx, routes/web.php, app/Http/Controllers/Ingredients/IngredientController.php, app/Http/Resources/IngredientListResource.php</files>
  <read_first>
    - routes/web.php (existing route group structure — admin prefix, auth+verified middleware)
    - app/Http/Controllers/Admin/UserController.php (index() search pattern to mirror)
    - .planning/phases/02-ingredient-library/02-UI-SPEC.md (Page Routing Map, Interaction Contracts > Live Search, Filter interactions)
    - .planning/phases/02-ingredient-library/02-RESEARCH.md (Pattern 3 debounced live search server side; Pitfall 1 whereFullText/SQLite)
  </read_first>
  <action>
**shadcn primitives** — run `npx shadcn@latest add scroll-area popover --yes` (shadcn official registry; UI-SPEC declares these new for this phase). Confirm `resources/js/components/ui/scroll-area.tsx` and `popover.tsx` are created. Do not add `tabs` or `command` here — those belong to Plans 02-05 / 02-04.

**Routes** — in `routes/web.php`, inside the existing `Route::middleware(['auth', 'verified'])->group(...)` block (the one containing `dashboard`), add a controller-grouped set. Place these BEFORE the `{id}` routes so `create` is not captured as an id later (full CRUD routes are added in Plan 02-04; here add only the index):
```php
Route::get('ingredients', [\App\Http\Controllers\Ingredients\IngredientController::class, 'index'])
    ->name('ingredients.index');
```
Add the `use` import for `App\Http\Controllers\Ingredients\IngredientController` at the top following the existing import ordering.

**Controller** — `php artisan make:controller Ingredients/IngredientController`. Add the `index(Request $request): Response` action mirroring `UserController::index`:
- Read inputs: `$search = $request->string('search')->toString();`, `$source = $request->string('source', 'all')->toString();` (values 'all'|'official'|'private'), `$verifiedOnly = $request->boolean('verified_only');`, `$allergenFree = $request->input('allergen_free', []);` (array of allergen slugs to exclude).
- Build the query:
```php
Ingredient::query()
    ->with(['translations', 'allergens', 'category'])
    ->when($search, fn ($q) => $q->whereHas('translations', fn ($t) => $t
        ->when(
            DB::getDriverName() !== 'sqlite',
            fn ($w) => $w->whereFullText('name', $search),
            fn ($w) => $w->where('name', 'like', "%{$search}%")
        )
    ))
    ->when($source === 'official', fn ($q) => $q->whereNull('user_id'))
    ->when($source === 'private', fn ($q) => $q->where('user_id', auth()->id()))
    ->when($verifiedOnly, fn ($q) => $q->where('verified', true))
    ->when($allergenFree, fn ($q) => $q->whereDoesntHave('allergens', fn ($a) =>
        $a->whereIn('slug', (array) $allergenFree)->wherePivot('state', 'contains')
    ))
    ->orderBy('name_cache')
    ->paginate(30)
    ->withQueryString()
    ->through(fn (Ingredient $i) => (new IngredientListResource($i))->resolve());
```
Note on private visibility: when `$source === 'all'`, the user must still NOT see other users' private ingredients. Add a base scope so the query always excludes private ingredients that are not the current user's: `->where(fn ($q) => $q->whereNull('user_id')->orWhere('user_id', auth()->id()))`. Apply this before the source filter.
- Return `Inertia::render('ingredients/index', ['ingredients' => $ingredients, 'filters' => ['search' => $search, 'source' => $source, 'verified_only' => $verifiedOnly, 'allergen_free' => (array) $allergenFree], 'allergens' => Allergen::orderBy('name')->get(['id','name','slug'])])`.
- Import `Illuminate\Support\Facades\DB`.

**IngredientListResource** — `php artisan make:resource IngredientListResource`. `toArray()` returns the compact row shape consumed by the list:
```php
'id' => $this->id,
'name' => $this->nameFor(app()->getLocale()),
'secondary_name' => $this->nameFor(app()->getLocale() === 'el' ? 'en' : 'el'),
'energy_kcal' => $this->energy_kcal !== null ? (float) $this->energy_kcal : null,
'verified' => (bool) $this->verified,
'is_private' => $this->user_id !== null,
'allergens' => $this->allergens->map(fn ($a) => ['slug' => $a->slug, 'name' => $a->name, 'state' => $a->pivot->state]),
```
Run `vendor/bin/pint --dirty --format agent` and `npm run build` to regenerate Wayfinder routes.
  </action>
  <verify>
    <automated>php artisan route:list --path=ingredients --except-vendor && php -l app/Http/Controllers/Ingredients/IngredientController.php</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list --path=ingredients` lists a GET `ingredients` route named `ingredients.index`
    - Grep `app/Http/Controllers/Ingredients/IngredientController.php` for `whereFullText` and `getDriverName` both return matches
    - Grep `app/Http/Controllers/Ingredients/IngredientController.php` for `whereDoesntHave` returns a match (allergen-free filter)
    - Grep `app/Http/Controllers/Ingredients/IngredientController.php` for `orWhere('user_id'` returns a match (private visibility scope)
    - `resources/js/components/ui/scroll-area.tsx` and `resources/js/components/ui/popover.tsx` both exist
    - `php -l` on the controller reports "No syntax errors"
  </acceptance_criteria>
  <done>The /ingredients route resolves to IngredientController::index; search across translations works with the SQLite/MySQL driver split; the three filters are applied; private ingredients of other users are never returned.</done>
</task>

<task type="auto">
  <name>Task 2: Build the search/browse page, row, filter, and allergen-icon components</name>
  <files>resources/js/pages/ingredients/index.tsx, resources/js/components/ingredients/ingredient-row.tsx, resources/js/components/ingredients/allergen-icons.tsx, resources/js/components/ingredients/ingredient-filters.tsx, resources/js/types/ingredient.ts</files>
  <read_first>
    - resources/js/pages/dashboard.tsx (an existing page — layout import, AppLayout usage)
    - resources/js/components/ui/scroll-area.tsx, resources/js/components/ui/popover.tsx (Task 1)
    - .planning/phases/02-ingredient-library/02-UI-SPEC.md (Component Inventory > Search & Browse Page, Search result row spec, Interaction Contracts, Copywriting Contract, Responsive Contract)
    - .planning/phases/02-ingredient-library/02-RESEARCH.md (Pattern 3 client-side debounced reload)
  </read_first>
  <action>
**Types** — `resources/js/types/ingredient.ts` defines TypeScript interfaces matching the controller props: `IngredientListItem` (id, name, secondary_name, energy_kcal: number | null, verified, is_private, allergens: { slug, name, state }[]), `IngredientFilters` (search, source, verified_only, allergen_free: string[]), `AllergenOption` (id, name, slug), and a Laravel paginator generic.

**allergen-icons.tsx** — `AllergenIcons` component: given an `allergens` array, render up to 14 small lucide icons (pick a sensible icon per slug; a single `WheatOff`/`Nut`/`Milk`/`Fish`/`Egg`-style mapping, fall back to `AlertTriangle`). Each icon wrapped in a shadcn `Tooltip` showing the allergen name. Each icon must have an `aria-label` with the allergen name (UI-SPEC accessibility). "contains" icons render at full opacity; "may contain" at reduced opacity (`opacity-50`). xs (4px) gap between icons.

**ingredient-row.tsx** — `IngredientRow` component, a `div` (NOT a table row, per UI-SPEC density spec): min-height 48px, 1px `border-border` bottom divider, `hover:bg-muted` transition. Left: ingredient name at body size (16px/400) plus the secondary-locale name below at label size (14px/400) `text-muted-foreground`. Middle: `{energy_kcal} kcal` at label size muted (show `—` when null). Right: `<AllergenIcons>`. Rightmost: a shadcn `Badge` variant="default" with `bg-accent text-accent-foreground` reading the "Verified" string when `verified` is true. The whole row is an Inertia `Link` to `/ingredients/{id}` (use the Wayfinder show route once Plan 02-05 adds it; for now link to `` `/ingredients/${id}` `` directly via `Link href`).

**ingredient-filters.tsx** — `IngredientFilters` component rendering the three launch filters:
1. Source — shadcn `Select` with options All ingredients / Official library / My ingredients.
2. Allergen-free — a shadcn `Popover` containing a `Checkbox` + `Label` per allergen (14 from the `allergens` prop). The reload fires on Popover CLOSE, not per checkbox tick (UI-SPEC Filter interactions).
3. Verified only — a shadcn `Checkbox` + `Label`, immediate reload on change.
On mobile (<768px) the filters collapse into a `Sheet` triggered by a "Filters" button (UI-SPEC Responsive Contract).

**index.tsx** — `IngredientIndex` page. Wrap in the app layout used by `dashboard.tsx`. Layout:
- Page title "Ingredient Library" (Display 28px) with an "Add Ingredient" CTA button (`variant="default"`) linking to `/ingredients/create` (route exists after Plan 02-04; link directly for now).
- A prominent full-width search `Input` with a leading lucide `SearchIcon`, `aria-label="Search ingredients"`, placeholder from the copy contract. This is the page's focal point.
- `<IngredientFilters>` below the search.
- Results: a `ScrollArea` containing `<IngredientRow>` per item, with `aria-busy` on the container while a reload is in flight.
- 8 `Skeleton` rows (48px each) shown during the debounced reload.
- shadcn `Pagination` (30 per page).
- Empty states: distinct copy for no-search vs no-results-for-query (use the exact strings from the Copywriting Contract).
Live search behaviour (RESEARCH Pattern 3): keep `search`/`source`/`verified_only`/`allergen_free` in component state seeded from the `filters` prop; a 300ms-debounced callback calls `router.reload({ data: {...params}, only: ['ingredients'], preserveState: true, replace: true })`. `replace: true` is mandatory so the URL stays in sync (RESEARCH Pitfall 5). Set the `aria-busy` / skeleton flag on reload start and clear it in the reload's `onFinish`.

Use `cn()` from `@/lib/utils`, `useLaravelReactI18n` `t()` for all copy. Run `npm run build` and `vendor/bin/pint --dirty --format agent` (no PHP changed here, but harmless).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Grep `resources/js/pages/ingredients/index.tsx` for `router.reload` returns a match
    - Grep `resources/js/pages/ingredients/index.tsx` for `only: ['ingredients']` returns a match
    - Grep `resources/js/pages/ingredients/index.tsx` for `replace: true` returns a match
    - Grep `resources/js/pages/ingredients/index.tsx` for `aria-busy` returns a match
    - Grep `resources/js/components/ingredients/ingredient-row.tsx` for `min-h-` or `48` returns a match
    - Grep `resources/js/components/ingredients/allergen-icons.tsx` for `aria-label` returns a match
    - All 5 files exist
  </acceptance_criteria>
  <done>The search page renders with a focal search input, three working filters, compact 48px rows with allergen icons + verified badges, skeleton loading state, and live debounced URL-synced search.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Ingredients nav entry, i18n strings, and pass IngredientSearchTest</name>
  <files>resources/js/components/app-sidebar.tsx, lang/en/app.php, lang/el/app.php, tests/Feature/Ingredients/IngredientSearchTest.php</files>
  <read_first>
    - resources/js/components/app-sidebar.tsx (existing nav structure — mainNavItems array, t() usage, route imports)
    - lang/en/app.php (existing key structure — 'nav', 'admin' sections)
    - lang/el/app.php (Greek translations to mirror)
    - tests/Feature/Ingredients/IngredientSearchTest.php (Plan 02-01 scaffold)
    - .planning/phases/02-ingredient-library/02-UI-SPEC.md (Copywriting Contract — full EN string table)
  </read_first>
  <action>
**Nav** — in `resources/js/components/app-sidebar.tsx`, add an "Ingredients" entry to `mainNavItems`. It is visible to ALL authenticated users (no permission gate — every user uses the library). Place it after Dashboard, before the permission-gated Users entry. Use a lucide food icon (e.g. `Carrot` or `Apple`), title `t('app.nav.ingredients')`, and `href` the `/ingredients` route (import the Wayfinder helper `index as ingredientsIndex from '@/routes/ingredients'` once it is generated by the Task 1 `npm run build`; if the helper path differs, use the generated path).

**i18n** — add an `'ingredients'` section to BOTH `lang/en/app.php` and `lang/el/app.php`, and an `'ingredients'` key under the existing `'nav'` section. Copy every English string from the UI-SPEC Copywriting Contract table into `lang/en/app.php` under nested keys (e.g. `'page_title' => 'Ingredient Library'`, `'cta_add' => 'Add Ingredient'`, `'search_placeholder' => 'Search ingredients…'`, `'filter_source' => 'Source'`, `'filter_source_all' => 'All ingredients'`, `'filter_source_official' => 'Official library'`, `'filter_source_private' => 'My ingredients'`, `'filter_allergen' => 'Allergen-free from'`, `'filter_verified' => 'Verified only'`, `'badge_verified' => 'Verified'`, `'empty_heading' => 'No ingredients yet'`, `'empty_body' => '...'`, `'empty_search_heading' => 'No results for ":query"'`, `'empty_search_body' => '...'`, `'error_state' => '...'`, and the rest of the table — nutrition/allergen/conversion/price/verify/create/delete strings too, since later plans consume them and it is cheaper to add them all once). Add EL translations for every key in `lang/el/app.php`. For the three context-specific dismiss labels ("Keep Unverified", "Back to Library", "Keep Ingredient") the EL string must name what the user returns to, not a generic "Cancel" (UI-SPEC note). Add `'nav' => ['ingredients' => 'Ingredients']` (EN) / `'Συστατικά'` or the correct Greek (EL).

**IngredientSearchTest** — finalise `tests/Feature/Ingredients/IngredientSearchTest.php` so it passes against the Task 1 controller:
- An authenticated user `GET /ingredients` gets a 200 and Inertia component `ingredients/index`.
- Seed two ingredients with translations: one English name "Olive oil" + Greek "Ελαιόλαδο", one English "Butter". `GET /ingredients?search=Olive` returns the olive oil only; `GET /ingredients?search=Ελαιόλαδο` also returns the olive oil (Greek match — INGR-01 + ROADMAP criterion 1).
- `GET /ingredients?source=official` excludes private ingredients; a private ingredient owned by another user never appears for the current user.
- `GET /ingredients?verified_only=1` returns only verified ingredients.
- An empty search returns the paginated full visible list.
Use `Inertia\Testing\AssertableInertia` (`$response->assertInertia(fn ($page) => $page->component('ingredients/index')->has('ingredients.data', N))`).

Run `npm run build`, `vendor/bin/pint --dirty --format agent`, then the test.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=IngredientSearchTest && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --compact --filter=IngredientSearchTest` exits 0
    - `npm run build` exits 0
    - Grep `resources/js/components/app-sidebar.tsx` for `ingredients` returns a match
    - Grep `lang/en/app.php` for `Ingredient Library` returns a match
    - Grep `lang/en/app.php` for `Search ingredients` returns a match
    - Grep `lang/el/app.php` for `'ingredients'` returns a match (Greek section exists)
    - Grep `lang/en/app.php` for `nav` block contains an `ingredients` key
  </acceptance_criteria>
  <done>Every authenticated user sees an Ingredients nav entry; all phase copy exists in EN + EL; IngredientSearchTest passes proving Greek/English name search and the three filters.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=IngredientSearchTest` passes.
- `npm run build` succeeds.
- The Ingredients nav entry appears for all authenticated users.
- Search matches both Greek and English names; the three filters work; private ingredients of other users are never shown.
</verification>

<success_criteria>
- /ingredients renders a search-first browse page with live debounced search.
- Compact 48px rows show locale name, calories, allergen icons, verified badge.
- Source / allergen-free / verified-only filters work and sync to the URL.
- All EN + EL copy is in place for the whole phase.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingredient-library/02-03-SUMMARY.md`
</output>
