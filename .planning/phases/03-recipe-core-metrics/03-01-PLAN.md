---
phase: 03-recipe-core-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - app/Enums/Difficulty.php
  - database/migrations/2026_05_17_000001_create_cuisines_table.php
  - database/migrations/2026_05_17_000002_create_tags_table.php
  - database/migrations/2026_05_17_000003_create_recipes_table.php
  - database/migrations/2026_05_17_000004_create_recipe_sections_table.php
  - database/migrations/2026_05_17_000005_create_recipe_ingredient_lines_table.php
  - database/migrations/2026_05_17_000006_create_recipe_steps_table.php
  - database/migrations/2026_05_17_000007_create_recipe_versions_table.php
  - database/migrations/2026_05_17_000008_create_recipe_drafts_table.php
  - database/migrations/2026_05_17_000009_create_recipe_draft_edits_table.php
  - database/migrations/2026_05_17_000010_create_recipe_tag_table.php
  - app/Models/Recipe.php
  - app/Models/RecipeSection.php
  - app/Models/RecipeIngredientLine.php
  - app/Models/RecipeStep.php
  - app/Models/RecipeVersion.php
  - app/Models/RecipeDraft.php
  - app/Models/RecipeDraftEdit.php
  - app/Models/Cuisine.php
  - app/Models/Tag.php
  - database/factories/RecipeFactory.php
  - database/factories/RecipeSectionFactory.php
  - database/factories/RecipeIngredientLineFactory.php
  - database/factories/RecipeStepFactory.php
  - database/factories/RecipeVersionFactory.php
  - database/factories/RecipeDraftFactory.php
  - database/factories/CuisineFactory.php
  - database/factories/TagFactory.php
  - database/seeders/CuisineSeeder.php
  - tests/Feature/Recipes/RecipeSchemaTest.php
  - tests/Feature/Recipes/RecipeCrudTest.php
  - tests/Feature/Recipes/RecipeDraftTest.php
  - tests/Feature/Recipes/RecipeVersionTest.php
  - tests/Feature/Recipes/RecipeSearchTest.php
  - tests/Feature/Recipes/SubRecipeTest.php
  - tests/Feature/Recipes/CircularReferenceTest.php
  - tests/Feature/Recipes/Metrics/NutritionCalculatorTest.php
  - tests/Feature/Recipes/Metrics/CostCalculatorTest.php
  - tests/Feature/Recipes/Metrics/ShrinkageCalculatorTest.php
  - tests/Feature/Recipes/Metrics/BakersPercentageCalculatorTest.php
  - tests/Feature/Recipes/Metrics/AllergenRollupTest.php
  - tests/Feature/Recipes/Metrics/MetricsRollupTest.php
autonomous: true
requirements: [RECIPE-04, RECIPE-13, VERSION-01]
must_haves:
  truths:
    - "A recipe row stores yield, portions, time, difficulty, cuisine, tags, hero image path, and notes"
    - "An ingredient line row stores prep_note and yield_pct"
    - "recipe_versions rows are append-only with a JSON snapshot column"
    - "All Phase 3 test files exist with real assertions and run red"
  artifacts:
    - path: "database/migrations/2026_05_17_000003_create_recipes_table.php"
      provides: "recipes table schema"
      contains: "difficulty"
    - path: "app/Models/Recipe.php"
      provides: "Recipe Eloquent model with relationships"
      min_lines: 40
    - path: "app/Enums/Difficulty.php"
      provides: "Easy/Medium/Hard/Expert enum"
      contains: "case Expert"
    - path: "database/factories/RecipeFactory.php"
      provides: "Recipe test fixture factory"
    - path: "tests/Feature/Recipes/RecipeSchemaTest.php"
      provides: "schema-level requirement tests"
  key_links:
    - from: "app/Models/Recipe.php"
      to: "app/Models/RecipeVersion.php"
      via: "hasMany versions relationship"
      pattern: "hasMany\\(RecipeVersion"
    - from: "app/Models/RecipeIngredientLine.php"
      to: "app/Models/Ingredient.php"
      via: "belongsTo ingredient"
      pattern: "belongsTo\\(Ingredient"
---

<objective>
Establish the complete Phase 3 data layer: nine recipe-domain tables, their Eloquent models, factories, the Difficulty enum, the seeded cuisine list, and the brick/math direct dependency. Also scaffold ALL Phase 3 test files with real (red) assertions so every later wave has concrete green targets.

Purpose: Every other Phase 3 plan — the metrics engine, the builder UI, search — reads from these tables. The draft/version structure underpins everything and must be solid first. Scaffolding the test suite here makes Wave 0 complete in a single plan.
Output: Migrations + models + factories + enum + cuisine seeder + 13 test files + composer.json updated.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-recipe-core-metrics/03-CONTEXT.md
@.planning/phases/03-recipe-core-metrics/03-RESEARCH.md
@.planning/phases/03-recipe-core-metrics/03-VALIDATION.md
@app/Models/Ingredient.php
@app/Models/Unit.php
@app/Models/IngredientConversion.php

<interfaces>
<!-- Existing Phase 1/2 contracts the recipe schema references. -->

Unit (app/Models/Unit.php): columns name, symbol, type ('weight'|'volume'|'count'), base_factor (decimal:6 grams-per-unit).

Ingredient (app/Models/Ingredient.php): nutrition columns all decimal:4 (energy_kcal, protein_g, fat_g, carbs_g, sugars_g, fibre_g, sodium_mg, ... full list in model). allergens() belongsToMany with pivot 'state'. user_id nullable (null = official). conversions() hasMany IngredientConversion.

AccountStatus enum pattern (app/Enums/AccountStatus.php) — backed string enum with a label() match — Difficulty mirrors this exactly.

Recipe schema target (from RESEARCH.md Architecture > Recommended Database Schema — copy verbatim):
- recipes: id, user_id, name, slug, hero_image_path (nullable), yield_amount (decimal:4), yield_unit_id (FK units nullable), portions (decimal:4), portion_size_g (decimal:4 nullable), prep_time_minutes (int nullable), cook_time_minutes (int nullable), difficulty (string, nullable), cuisine_id (FK cuisines nullable), notes (text nullable), current_version_id (FK recipe_versions nullable), selling_price (decimal:8 nullable), timestamps, softDeletes
- cuisines: id, name, slug (unique), timestamps
- tags: id, name (unique), timestamps
- recipe_tag: recipe_id, tag_id (composite pk)
- recipe_sections: id, recipe_id (FK cascade), name, order (int), timestamps
- recipe_ingredient_lines: id, recipe_id (FK cascade), section_id (FK recipe_sections cascade), ingredient_id (FK ingredients nullable), sub_recipe_version_id (FK recipe_versions nullable), quantity (decimal:6 nullable), unit_id (FK units nullable), quantity_g (decimal:6 nullable), prep_note (text nullable), yield_pct (decimal:4 nullable), is_flour_base (boolean default false), order (int), timestamps
- recipe_steps: id, recipe_id (FK cascade), section_id (FK recipe_sections cascade), instruction (text), order (int), step_image_path (string nullable), timestamps
- recipe_versions: id, recipe_id (FK cascade), version_number (int), committed_by (FK users), committed_at (timestamp), change_note (string nullable), snapshot (json), yield_g (decimal:10 nullable), cached_nutrition_json (json nullable), cached_cost_per_gram (decimal:12 nullable), cached_allergen_slugs (json nullable), timestamps. Unique index (recipe_id, version_number).
- recipe_drafts: id, recipe_id (FK cascade, unique), user_id (FK users), data (json), edit_sequence (int default 0), timestamps
- recipe_draft_edits: id, recipe_draft_id (FK cascade), sequence (int), action (string), before_snapshot (json), created_at (use timestamps but only created_at matters)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: brick/math dependency, Difficulty enum, migrations</name>
  <files>composer.json, app/Enums/Difficulty.php, database/migrations/2026_05_17_000001_create_cuisines_table.php, database/migrations/2026_05_17_000002_create_tags_table.php, database/migrations/2026_05_17_000003_create_recipes_table.php, database/migrations/2026_05_17_000004_create_recipe_sections_table.php, database/migrations/2026_05_17_000005_create_recipe_ingredient_lines_table.php, database/migrations/2026_05_17_000006_create_recipe_steps_table.php, database/migrations/2026_05_17_000007_create_recipe_versions_table.php, database/migrations/2026_05_17_000008_create_recipe_drafts_table.php, database/migrations/2026_05_17_000009_create_recipe_draft_edits_table.php, database/migrations/2026_05_17_000010_create_recipe_tag_table.php</files>
  <read_first>
    - composer.json (confirm brick/math is absent from the require block)
    - app/Enums/AccountStatus.php (the exact enum + label() pattern to mirror)
    - database/migrations/2026_05_16_140144_create_ingredients_table.php (DECIMAL precision + FK + nullable column conventions)
    - .planning/phases/03-recipe-core-metrics/03-RESEARCH.md (Architecture > Recommended Database Schema — the authoritative column list)
  </read_first>
  <action>
    1. Run `composer require brick/math` so it becomes an explicit direct dependency (RESEARCH.md Pitfall 6 — it is currently only transitive and could vanish).
    2. Create `app/Enums/Difficulty.php` — a backed string enum mirroring `App\Enums\AccountStatus`:
       ```php
       namespace App\Enums;
       enum Difficulty: string {
           case Easy = 'easy';
           case Medium = 'medium';
           case Hard = 'hard';
           case Expert = 'expert';
           public function label(): string { return match($this) {
               self::Easy => 'Easy', self::Medium => 'Medium',
               self::Hard => 'Hard', self::Expert => 'Expert',
           }; }
       }
       ```
    3. Create the ten migrations listed in files_modified using `php artisan make:migration --no-interaction`. Column definitions are the verbatim schema in the `<interfaces>` block above — use exactly those column names, types, nullability, and precisions. Specific notes:
       - `recipes.difficulty` is a `string('difficulty')->nullable()` column (cast to the enum on the model).
       - All `decimal` columns: `recipes.yield_amount`/`portions`/`portion_size_g` → `decimal(12,4)`; `recipe_ingredient_lines.quantity`/`quantity_g` → `decimal(14,6)`; `yield_pct` → `decimal(7,4)`; `recipe_versions.yield_g` → `decimal(14,4)`, `cached_cost_per_gram` → `decimal(16,8)`; `recipes.selling_price` → `decimal(12,4)`.
       - FK ordering matters: `recipes.current_version_id` and `recipe_ingredient_lines.sub_recipe_version_id` reference `recipe_versions`, so do NOT add those two foreign-key constraints inline in the create migration — add them as `nullableMorphs`-style deferred constraints in the LAST migration (000010, after recipe_versions exists) OR declare the columns as plain `unsignedBigInteger(...)->nullable()` without `->constrained()` to avoid a chicken-and-egg ordering failure. Use the plain-column approach.
       - `recipe_versions` migration adds `$table->unique(['recipe_id', 'version_number'])`.
       - `recipe_drafts` migration adds `$table->unique('recipe_id')`.
       - `recipe_ingredient_lines`: ingredient_id and sub_recipe_version_id are both nullable (a line is EITHER an ingredient OR a sub-recipe). unit_id nullable (null for sub-recipe lines).
    4. Run `php artisan migrate` and confirm all ten tables are created without error.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed && php artisan migrate:status</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"brick/math"' composer.json` succeeds (brick/math in require block)
    - `app/Enums/Difficulty.php` exists and `grep -q 'case Expert' app/Enums/Difficulty.php` succeeds
    - Ten migration files exist matching the pattern `database/migrations/2026_05_17_0000*`
    - `php artisan migrate:status` lists `recipes`, `recipe_versions`, `recipe_drafts`, `recipe_draft_edits`, `recipe_sections`, `recipe_ingredient_lines`, `recipe_steps`, `cuisines`, `tags`, `recipe_tag` as Ran
    - `grep -q "decimal('quantity', 14, 6)" database/migrations/2026_05_17_000005_create_recipe_ingredient_lines_table.php` or equivalent decimal precision present
  </acceptance_criteria>
  <done>composer.json has brick/math; Difficulty enum exists; all ten tables migrate cleanly with the exact column shapes from RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 2: Recipe-domain models, factories, cuisine seeder</name>
  <files>app/Models/Recipe.php, app/Models/RecipeSection.php, app/Models/RecipeIngredientLine.php, app/Models/RecipeStep.php, app/Models/RecipeVersion.php, app/Models/RecipeDraft.php, app/Models/RecipeDraftEdit.php, app/Models/Cuisine.php, app/Models/Tag.php, app/Models/User.php, database/factories/RecipeFactory.php, database/factories/RecipeSectionFactory.php, database/factories/RecipeIngredientLineFactory.php, database/factories/RecipeStepFactory.php, database/factories/RecipeVersionFactory.php, database/factories/RecipeDraftFactory.php, database/factories/CuisineFactory.php, database/factories/TagFactory.php, database/seeders/CuisineSeeder.php, database/seeders/DatabaseSeeder.php</files>
  <read_first>
    - app/Models/Ingredient.php (the relationship + casts + fillable convention to mirror)
    - app/Models/IngredientConversion.php (a small belongsTo model example)
    - database/factories/IngredientFactory.php (factory convention — definition() shape, faker usage)
    - database/seeders/AllergenSeeder.php OR UnitSeeder.php (idempotent seeder pattern — firstOrCreate)
  </read_first>
  <action>
    Create the nine models in files_modified via `php artisan make:model --no-interaction` (one Recipe model can use `-mf` already done in Task 1; here just the classes). Each model: `protected $fillable` listing all writable columns, `protected function casts()` returning the decimal/json/enum casts, and relationships. Concrete contracts:

    - `Recipe`: use `HasFactory, SoftDeletes`. casts: `difficulty` => Difficulty::class, `yield_amount`/`portions`/`portion_size_g`/`selling_price` => 'decimal:4'. Relationships: `user()` belongsTo User; `cuisine()` belongsTo Cuisine; `tags()` belongsToMany Tag (table 'recipe_tag'); `sections()` hasMany RecipeSection; `ingredientLines()` hasMany RecipeIngredientLine; `steps()` hasMany RecipeStep; `versions()` hasMany RecipeVersion; `currentVersion()` belongsTo RecipeVersion 'current_version_id'; `draft()` hasOne RecipeDraft.
    - `RecipeSection`: fillable recipe_id, name, order. Relationships: `recipe()`, `ingredientLines()` hasMany, `steps()` hasMany.
    - `RecipeIngredientLine`: fillable recipe_id, section_id, ingredient_id, sub_recipe_version_id, quantity, unit_id, quantity_g, prep_note, yield_pct, is_flour_base, order. casts: quantity/quantity_g => 'decimal:6', yield_pct => 'decimal:4', is_flour_base => 'boolean'. Relationships: `recipe()`, `section()`, `ingredient()` belongsTo Ingredient, `unit()` belongsTo Unit, `subRecipeVersion()` belongsTo RecipeVersion 'sub_recipe_version_id'. Add `isSubRecipe(): bool` returning `$this->sub_recipe_version_id !== null`.
    - `RecipeStep`: fillable recipe_id, section_id, instruction, order, step_image_path. Relationships: `recipe()`, `section()`.
    - `RecipeVersion`: fillable recipe_id, version_number, committed_by, committed_at, change_note, snapshot, yield_g, cached_nutrition_json, cached_cost_per_gram, cached_allergen_slugs. casts: committed_at => 'datetime', snapshot/cached_nutrition_json/cached_allergen_slugs => 'array', yield_g => 'decimal:4', cached_cost_per_gram => 'decimal:8'. Relationships: `recipe()`, `committedBy()` belongsTo User 'committed_by'.
    - `RecipeDraft`: fillable recipe_id, user_id, data, edit_sequence. casts: data => 'array', edit_sequence => 'integer'. Relationships: `recipe()`, `user()`, `edits()` hasMany RecipeDraftEdit.
    - `RecipeDraftEdit`: fillable recipe_draft_id, sequence, action, before_snapshot. casts: before_snapshot => 'array'. `public $timestamps = true;` Relationship: `draft()` belongsTo RecipeDraft 'recipe_draft_id'.
    - `Cuisine`: fillable name, slug. Relationship: `recipes()` hasMany.
    - `Tag`: fillable name. Relationship: `recipes()` belongsToMany Recipe (table 'recipe_tag').

    Add a `recipes()` hasMany relationship to `app/Models/User.php` (belongsTo User -> hasMany Recipe).

    Create factories (`database/factories/`): RecipeFactory, RecipeSectionFactory, RecipeIngredientLineFactory, RecipeStepFactory, RecipeVersionFactory, RecipeDraftFactory, CuisineFactory, TagFactory. RecipeFactory definition: name fake()->words(3, true), user_id User::factory(), portions 8, yield_amount 1000, difficulty Difficulty::Medium. RecipeVersionFactory: version_number 1, committed_at now(), snapshot [], yield_g 1000. RecipeDraftFactory: data the JSON snapshot shape, edit_sequence 0.

    Create `database/seeders/CuisineSeeder.php` — idempotent (`firstOrCreate` on slug), seeding this starter list: Greek, Italian, French, Spanish, Mediterranean, American, Mexican, Middle Eastern, Indian, Chinese, Japanese, Thai, Other. Register it in `database/seeders/DatabaseSeeder.php`.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed --no-interaction && php artisan test --compact --filter=RecipeSchemaTest</automated>
  </verify>
  <acceptance_criteria>
    - All nine model files exist under `app/Models/` (Recipe, RecipeSection, RecipeIngredientLine, RecipeStep, RecipeVersion, RecipeDraft, RecipeDraftEdit, Cuisine, Tag)
    - `grep -q "hasMany(RecipeVersion" app/Models/Recipe.php` succeeds
    - `grep -q "belongsTo(Ingredient" app/Models/RecipeIngredientLine.php` succeeds
    - `grep -q "Difficulty::class" app/Models/Recipe.php` succeeds
    - `grep -q "function recipes" app/Models/User.php` succeeds
    - `grep -q "CuisineSeeder" database/seeders/DatabaseSeeder.php` succeeds
    - `php artisan tinker --execute 'echo \App\Models\Cuisine::count();'` outputs 13
    - Eight factory files exist under `database/factories/`
  </acceptance_criteria>
  <done>All nine models created with correct relationships and casts; User has recipes(); 13 cuisines seeded idempotently; eight factories exist.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 — scaffold all 13 Phase 3 test files with real red assertions</name>
  <files>tests/Feature/Recipes/RecipeSchemaTest.php, tests/Feature/Recipes/RecipeCrudTest.php, tests/Feature/Recipes/RecipeDraftTest.php, tests/Feature/Recipes/RecipeVersionTest.php, tests/Feature/Recipes/RecipeSearchTest.php, tests/Feature/Recipes/SubRecipeTest.php, tests/Feature/Recipes/CircularReferenceTest.php, tests/Feature/Recipes/Metrics/NutritionCalculatorTest.php, tests/Feature/Recipes/Metrics/CostCalculatorTest.php, tests/Feature/Recipes/Metrics/ShrinkageCalculatorTest.php, tests/Feature/Recipes/Metrics/BakersPercentageCalculatorTest.php, tests/Feature/Recipes/Metrics/AllergenRollupTest.php, tests/Feature/Recipes/Metrics/MetricsRollupTest.php, .planning/phases/03-recipe-core-metrics/03-VALIDATION.md</files>
  <read_first>
    - tests/Feature/Ingredients/IngredientVerificationTest.php (Pest feature-test convention — actingAs, factory usage, assertion style)
    - tests/Pest.php (shared base test case + RefreshDatabase wiring)
    - .planning/phases/03-recipe-core-metrics/03-RESEARCH.md (Validation Architecture > Phase Requirements -> Test Map and Wave 0 Gaps — the authoritative per-requirement test list)
  </read_first>
  <action>
    Create the 13 test files listed in files_modified using `php artisan make:test --pest --no-interaction`. Each test file MUST contain real assertions covering its mapped requirements (per RESEARCH.md Test Map) — NOT `->skip()` or empty bodies. Per the Phase 1 precedent (STATE.md: "Wave 0 test files write real assertions rather than skip()"), these tests are expected to FAIL now (red) and turn green as later waves implement the code.

    Concrete test coverage per file (write at least the named tests):
    - `RecipeSchemaTest.php` (RECIPE-04, RECIPE-13, VERSION-01): a recipe persists difficulty/cuisine/tags/yield/portions/time/notes; an ingredient line persists prep_note and yield_pct; a recipe_version row cannot be updated after creation (assert no `updated` change is intended — assert version_number + snapshot remain after a second save).
    - `RecipeCrudTest.php` (RECIPE-01,02,03,07,09,10,11): POST /recipes creates a recipe + commits v1; ingredient lines accept weight, volume, AND count units; steps persist in order; duplicate creates an independent recipe with its own v1 and no lineage FK; hero image + step image paths persist; chef notes persist.
    - `RecipeDraftTest.php` (VERSION-02,03,04): PUT /recipes/{id}/draft updates the draft without creating a version; POST /recipes/{id}/versions commits the draft as a new numbered version; POST /recipes/{id}/draft/recall removes the last edit and restores prior state.
    - `RecipeVersionTest.php` (VERSION-01,05, RECIPE-08): versions are append-only; two versions return diff data; changing portion count in a view request does NOT create a new version.
    - `RecipeSearchTest.php` (RECIPE-12): GET /recipes filters by tag, cuisine, allergen, ingredient, difficulty, and time range.
    - `SubRecipeTest.php` (RECIPE-05, VERSION-06): a sub-recipe line attaches to a specific recipe_version; the pin stays on that version after the component gets a newer version.
    - `CircularReferenceTest.php` (RECIPE-06): adding recipe B as a sub-recipe of A then A as sub-recipe of B is rejected with a 422; a 3-node cycle A->B->C->A is rejected.
    - `Metrics/NutritionCalculatorTest.php` (METRIC-01,04,07,09,10): nutrition per portion and per 100 g for known values; weight/volume/count units normalize to grams; no float drift across 20 lines (assert exact decimal string).
    - `Metrics/CostCalculatorTest.php` (METRIC-02,03): cost per portion + total; food cost % = cost/selling*100.
    - `Metrics/ShrinkageCalculatorTest.php` (METRIC-05): cooking loss derived from per-line yield_pct.
    - `Metrics/BakersPercentageCalculatorTest.php` (METRIC-06): baker's % against one flour line and against multiple flour lines summed; hydration ratio.
    - `Metrics/AllergenRollupTest.php` (ALLG-01,02,03): recipe allergens derived from ingredient allergens; contains vs may_contain distinguished; allergens roll up through a sub-recipe with contains beating may_contain.
    - `Metrics/MetricsRollupTest.php` (METRIC-08): a sub-recipe contributing 250 g of a 500 g-yield component scales metrics by exactly 0.5.

    Use the factories from Task 2 to build fixtures. For tests targeting not-yet-built endpoints/services, write the assertion against the intended contract — they fail now, that is correct.

    Finally update `.planning/phases/03-recipe-core-metrics/03-VALIDATION.md`: replace the "TBD" row in the Per-Task Verification Map with one row per test file (Task/Plan/Wave/Requirement/Command), tick the Wave 0 Requirements checkboxes, and set `wave_0_complete: true` and `nyquist_compliant: true` in its frontmatter.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/Recipes/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 13 test files exist (`ls tests/Feature/Recipes/*.php tests/Feature/Recipes/Metrics/*.php` shows 13)
    - `grep -rL "->skip(" tests/Feature/Recipes/` — every file has real assertions (no file is purely skipped)
    - `grep -rq "assert" tests/Feature/Recipes/Metrics/NutritionCalculatorTest.php` succeeds
    - `php artisan test --compact tests/Feature/Recipes/` runs and reports failing tests (red is expected — the suite executes without fatal/parse errors)
    - `grep -q "wave_0_complete: true" .planning/phases/03-recipe-core-metrics/03-VALIDATION.md` succeeds
    - `grep -q "nyquist_compliant: true" .planning/phases/03-recipe-core-metrics/03-VALIDATION.md` succeeds
  </acceptance_criteria>
  <done>All 13 Phase 3 test files exist with real assertions, the suite executes without parse errors, and 03-VALIDATION.md records Wave 0 complete.</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh --seed` runs clean
- All nine recipe-domain models load and their relationships resolve in tinker
- `php artisan test --compact tests/Feature/Recipes/` executes the whole new suite (red expected — code not built yet)
- `composer.json` declares brick/math directly
</verification>

<success_criteria>
- Ten migrations, nine models, eight factories, the Difficulty enum, and the CuisineSeeder exist and migrate/seed cleanly
- 13 Phase 3 test files exist with real red assertions — Wave 0 is complete
- 03-VALIDATION.md updated: wave_0_complete + nyquist_compliant true
</success_criteria>

<output>
After completion, create `.planning/phases/03-recipe-core-metrics/03-01-SUMMARY.md`
</output>
