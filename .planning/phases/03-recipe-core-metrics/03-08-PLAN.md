---
phase: 03-recipe-core-metrics
plan: 08
type: execute
wave: 6
depends_on: ["03-06", "03-07"]
files_modified:
  - resources/js/pages/recipes/index.tsx
  - resources/js/components/recipes/recipe-card.tsx
  - resources/js/components/recipes/recipe-filters.tsx
  - resources/js/components/app-sidebar.tsx
  - resources/js/types/recipe.ts
  - lang/en.json
  - lang/el.json
autonomous: false
requirements: [RECIPE-12]
must_haves:
  truths:
    - "The recipe list is a grid of visual cards sorted recently-updated-first"
    - "Each card shows hero image, name, cuisine, time, difficulty, cost/portion, calories/portion, allergen icons"
    - "Live name search and a collapsible six-filter panel filter the list"
    - "A Recipes entry appears in the app sidebar"
    - "An end-to-end recipe build verifies the whole phase"
  artifacts:
    - path: "resources/js/pages/recipes/index.tsx"
      provides: "recipe list grid page"
      min_lines: 60
    - path: "resources/js/components/recipes/recipe-card.tsx"
      provides: "recipe grid card"
    - path: "resources/js/components/recipes/recipe-filters.tsx"
      provides: "collapsible six-filter panel"
  key_links:
    - from: "resources/js/pages/recipes/index.tsx"
      to: "/recipes"
      via: "debounced router.reload"
      pattern: "router.reload|recipes"
    - from: "resources/js/components/app-sidebar.tsx"
      to: "/recipes"
      via: "nav entry"
      pattern: "recipes|Recipes"
---

<objective>
Build the recipe list: a visual card grid with live name search, a collapsible six-filter panel (tag, cuisine, allergen, ingredient, difficulty, time), the "Recipes" sidebar nav entry, and a final human-verify checkpoint exercising the whole phase end to end.

Purpose: The list is the chef's home for actively-developed recipes (default sort: recently updated). It is the last UI surface. The checkpoint verifies the integrated phase — builder, draft auto-save, metrics roll-up, sub-recipes, versioning — works visually.
Output: index page + recipe-card + recipe-filters + sidebar nav + i18n + a human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-recipe-core-metrics/03-CONTEXT.md
@.planning/phases/03-recipe-core-metrics/03-RESEARCH.md
@.planning/phases/03-recipe-core-metrics/03-UI-SPEC.md
@resources/js/pages/ingredients/index.tsx
@resources/js/components/ingredients/ingredient-filters.tsx
@resources/js/components/app-sidebar.tsx

<interfaces>
<!-- Plan 04 list contract + Phase 2 list/filter patterns. -->

GET /recipes (recipes.index, Plan 04 RecipeController@index) props:
- `recipes` — paginated, each item via RecipeListResource: { id, name, slug, hero_image_path, cuisine, total_time, difficulty, cost_per_portion, calories_per_portion, allergen_slugs }
- `filters` — { search, tags[], cuisine, allergen_free[], ingredient, difficulty, time_range }
- `cuisines`, `allergens`, `tags` — filter option lists

UI-SPEC contracts (authoritative): card grid 1-col mobile / 2-col tablet / 3-col desktop; hero image 16:9, neutral `bg-muted` placeholder + centered `UtensilsCrossedIcon` 32px when absent; card shows name (Heading 20px), cuisine `Badge` secondary, metadata row (total time + difficulty badge, Label 14px muted), metrics row (`€N.NN / portion` + `N kcal`), up to 14 allergen icons with `Tooltip`; card hover = `shadow-md` lift + name underline. Search `Input` with leading `SearchIcon`, 300ms debounce, `router.reload({ only:['recipes'], preserveState:true, replace:true })`. "Filters" `Button variant="outline"` with active-count `Badge`; opens a `Sheet` on mobile / `Collapsible` on desktop. Six filters: Tags `Command` multi-select, Cuisine `Select`, Allergen `Popover`+`Checkbox` (reload on Popover close — Phase 2 contract), Ingredient `Command` combobox, Difficulty `Select`, Time `Select` (Under 30 / 30–60 / 1–2h / Over 2h). Empty states + all copy in the UI-SPEC Copywriting Contract. Skeleton: 6 skeleton cards while reloading; grid container `aria-busy`.

Phase 2 precedent: ingredients/index.tsx debounced useRef-timer reload; ingredient-filters.tsx Popover-close allergen filter; app-sidebar.tsx `mainNavItems: NavItem[]` array — add the Recipes item next to Ingredients.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: recipe-card component + Recipes sidebar nav entry</name>
  <files>resources/js/components/recipes/recipe-card.tsx, resources/js/components/app-sidebar.tsx, resources/js/types/recipe.ts</files>
  <read_first>
    - resources/js/components/ingredients/ingredient-row.tsx (compact-row + allergen-icon + Tooltip composition precedent)
    - resources/js/components/ui/card.tsx and badge.tsx and tooltip.tsx
    - resources/js/components/app-sidebar.tsx (the mainNavItems array — Ingredients entry to mirror)
    - resources/js/types/recipe.ts (add a RecipeCardData type)
    - .planning/phases/03-recipe-core-metrics/03-UI-SPEC.md (Recipe card spec, Color, Typography, Copywriting Contract card rows)
  </read_first>
  <action>
    Add a `RecipeCardData` interface to `resources/js/types/recipe.ts` matching the `RecipeListResource` shape (id, name, slug, hero_image_path, cuisine, total_time, difficulty, cost_per_portion, calories_per_portion, allergen_slugs).

    `resources/js/components/recipes/recipe-card.tsx` — a `Card` linking to `recipes.show`. Hero image at 16:9 (`aspect-video`) at the card top; when `hero_image_path` is null show a `bg-muted` placeholder with a centered `UtensilsCrossedIcon` (lucide, 32px, `text-muted-foreground`). Below: recipe name at Heading size (20px/600, 1-line clamp); cuisine `Badge variant="secondary"` (Label 14px); a metadata row with total time (prep+cook, formatted) + difficulty `Badge` (Label 14px, `text-muted-foreground`); a metrics row "{currency}{amount} / portion" + "{N} kcal / portion" (Label 14px); up to 14 allergen icons with `xs` (4px) gaps and a `Tooltip` per icon. Card hover: `hover:shadow-md` lift + name underline. Use `cn()`, `useLaravelReactI18n()`.

    Edit `resources/js/components/app-sidebar.tsx` — add a "Recipes" entry to the `mainNavItems` array, placed next to the existing "Ingredients" entry, linking to `recipes.index` (Wayfinder route), with an appropriate lucide icon (e.g. `BookOpenIcon` or `ChefHatIcon`). Title via i18n key "Recipes".
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/components/recipes/recipe-card.tsx` exists
    - `grep -q "aspect-video\|16:9\|aspect-\[16" resources/js/components/recipes/recipe-card.tsx` succeeds
    - `grep -q "UtensilsCrossedIcon" resources/js/components/recipes/recipe-card.tsx` succeeds
    - `grep -q "kcal" resources/js/components/recipes/recipe-card.tsx` succeeds (calories row)
    - `grep -q "recipes" resources/js/components/app-sidebar.tsx` succeeds (nav entry added)
    - `npm run build` completes with no TypeScript errors
  </acceptance_criteria>
  <done>The recipe card renders hero/placeholder, name, cuisine, time, difficulty, cost, calories, allergen icons; a Recipes nav entry sits alongside Ingredients.</done>
</task>

<task type="auto">
  <name>Task 2: recipe-filters panel + recipe list index page</name>
  <files>resources/js/components/recipes/recipe-filters.tsx, resources/js/pages/recipes/index.tsx, lang/en.json, lang/el.json</files>
  <read_first>
    - resources/js/pages/ingredients/index.tsx (debounced search reload + skeleton + pagination + empty-state pattern)
    - resources/js/components/ingredients/ingredient-filters.tsx (Popover allergen filter, reload-on-close)
    - resources/js/components/recipes/recipe-card.tsx (from Task 1 — the card to grid)
    - resources/js/components/ui/sheet.tsx, collapsible.tsx, command.tsx, select.tsx
    - .planning/phases/03-recipe-core-metrics/03-UI-SPEC.md (Recipe List Page inventory, Filter interactions, Responsive Contract, Copywriting Contract list rows)
  </read_first>
  <action>
    `resources/js/components/recipes/recipe-filters.tsx` — the collapsible six-filter panel. `Collapsible` on desktop, `Sheet` on mobile, opened by a "Filters" `Button variant="outline"` carrying an active-filter-count `Badge`. The six filters: Tags `Command` multi-select; Cuisine `Select` (options from `cuisines`); Allergen `Popover` + a `Checkbox` per allergen, reloading on Popover CLOSE (Phase 2 contract — STATE.md decision); Ingredient `Command` combobox (searches the ingredient library to filter recipes containing it); Difficulty `Select` (Easy/Medium/Hard/Expert); Time `Select` (Under 30 min / 30–60 min / 1–2 hours / Over 2 hours). Each filter change triggers `router.reload({ only:['recipes'], preserveState:true, replace:true })` per the UI-SPEC Filter Interactions contract. Use exact filter labels from the Copywriting Contract.

    `resources/js/pages/recipes/index.tsx` — the recipe list page. `Head` title + page title "My Recipes"; a "New Recipe" primary CTA linking to `recipes.create`; an always-visible search `Input` with a leading `SearchIcon`, 300 ms debounced (`useRef` timer mirroring ingredients/index.tsx) calling `router.reload({ only:['recipes'], preserveState:true, replace:true })`; the `recipe-filters` panel; a responsive card grid (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3`, gap-`lg`/24px) of `recipe-card`s; 6 `Skeleton` cards while a reload is in flight with `aria-busy` on the grid container; pagination (reuse the shadcn `Pagination` pattern from ingredients/index.tsx); empty states — no recipes ("No recipes yet" / "Create your first recipe to start tracking metrics." / "New Recipe" CTA) and no search match ("No recipes match '{query}'" / "Try different filters, or create a new recipe."). Default order is server-side recently-updated-first (Plan 04). Use `AppLayout`.

    Add all recipe-list EN strings from the UI-SPEC Copywriting Contract to `lang/en.json` and EL translations to `lang/el.json`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && php artisan test --compact --filter=RecipeSearchTest</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/components/recipes/recipe-filters.tsx` and `resources/js/pages/recipes/index.tsx` exist
    - `grep -qE "Tags|Cuisine|Difficulty" resources/js/components/recipes/recipe-filters.tsx` and the file references all six filters
    - `grep -q "only" resources/js/components/recipes/recipe-filters.tsx` succeeds (partial reload)
    - `grep -q "RecipeCard\|recipe-card" resources/js/pages/recipes/index.tsx` succeeds
    - `grep -q "aria-busy" resources/js/pages/recipes/index.tsx` succeeds
    - `grep -q "grid-cols-3\|lg:grid-cols-3" resources/js/pages/recipes/index.tsx` succeeds
    - `grep -q "My Recipes" lang/en.json` succeeds; EL has matching keys
    - `npm run build` completes with no TypeScript errors; `php artisan test --compact --filter=RecipeSearchTest` is green
  </acceptance_criteria>
  <done>The recipe list grid renders cards with live search + the six-filter collapsible panel; RecipeSearchTest passes (RECIPE-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end phase verification</name>
  <files>resources/js/pages/recipes/index.tsx, resources/js/pages/recipes/show.tsx</files>
  <what-built>
    The complete Phase 3 recipe system: the single-page builder with named sections, inline ingredient search + quick-create, prep/yield fields, the live sticky metrics panel (nutrition toggle, cost + food cost %, allergens, baker's %, scaling, gap banner), working-draft auto-save + Recall, Save Version + version history + side-by-side compare, nested sub-recipes with version pinning + update cue, recipe duplication, and the recipe list grid with search and the six-filter panel.
  </what-built>
  <action>Human-verify checkpoint — no code changes are made in this task. The verifier exercises the integrated phase in a browser after a build.</action>
  <how-to-verify>
    Run `npm run build` (or have `composer run dev` running), then in a browser:
    1. Visit `/recipes` — confirm the "Recipes" sidebar entry, the card grid, and live search work. Click "New Recipe".
    2. In the builder: set a recipe name, add a section ("Dough"), add an ingredient line by typing in the inline search, set quantity + a unit, add a prep note and a yield %. Confirm the "Saving…" -> "Saved" indicator appears.
    3. Mark a flour-category line as "Flour base" — confirm the Baker's Percentages section appears in the metrics panel.
    4. Confirm the metrics panel shows nutrition (toggle per-portion / per-100 g), cost, and allergens. Enter a selling price — confirm food cost % updates. Add a line with no price/nutrition — confirm the missing-data gap banner lists it.
    5. Add a step to the section. Use the scaling controls (Scale x 2) — confirm quantities recompute on screen in italic and an "Apply to Draft" button appears.
    6. Click "Save Version" (add a note) — confirm the version badge advances and a toast shows. Make another edit, click "Recall" — confirm the last edit is undone.
    7. Create a second recipe, then in the first recipe add the second as a sub-recipe via the inline search — confirm the pinned-version badge. Try to add the first recipe as a sub-recipe of the second — confirm the circular-reference error.
    8. Open version history, compare two versions — confirm the side-by-side diff highlights changes.
    9. Duplicate a recipe — confirm it opens a fresh independent copy.
    10. Toggle light/dark theme and check mobile width — confirm the metrics panel collapses to a bottom summary bar.
  </how-to-verify>
  <verify>Human confirms each of the 10 verification steps behaves as described; `php artisan test --compact` full suite is green.</verify>
  <done>The verifier types "approved"; any reported issues are logged for a gap-closure plan.</done>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` clean
- `php artisan test --compact` — full suite green
- Human checkpoint approved
</verification>

<success_criteria>
- recipe list grid + recipe-card + six-filter panel + Recipes nav entry exist
- RECIPE-12 search/filter test passes
- End-to-end human verification of the integrated phase approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-recipe-core-metrics/03-08-SUMMARY.md`
</output>
