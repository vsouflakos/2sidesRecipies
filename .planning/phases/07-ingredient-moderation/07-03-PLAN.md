---
phase: 07-ingredient-moderation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - resources/js/types/navigation.ts
  - resources/js/types/ingredient.ts
  - resources/js/components/nav-main.tsx
  - resources/js/components/app-sidebar.tsx
  - resources/js/components/ingredients/submission-status-badge.tsx
  - resources/js/components/ingredients/submit-action.tsx
  - resources/js/components/ingredients/submission-completeness.tsx
  - resources/js/components/ingredient-notifications.tsx
  - resources/js/pages/admin/ingredients.tsx
  - resources/js/pages/admin/ingredients/show.tsx
  - resources/js/pages/ingredients/show.tsx
  - app/Http/Resources/IngredientDetailResource.php
  - app/Http/Controllers/Ingredients/IngredientController.php
  - lang/en/app.php
  - lang/el/app.php
autonomous: false
requirements: [INGR-09, INGR-10, INGR-11]
must_haves:
  truths:
    - "An ingredient owner sees a Submit for Inclusion CTA and can submit through a confirmation dialog with a duplicate warning"
    - "A submitted ingredient shows an Under Review badge, a Withdraw button, and a frozen-data banner"
    - "A moderator sees a FIFO review queue with a data-completeness signal and a pending-count nav badge"
    - "A moderator can open a per-submission review screen and approve or reject with notes"
    - "The submitter sees an in-app notification when a moderator decides; a promoted ingredient shows a Contributed by credit"
  artifacts:
    - path: "resources/js/pages/admin/ingredients.tsx"
      provides: "Full review queue table replacing the stub"
      contains: "completeness"
    - path: "resources/js/pages/admin/ingredients/show.tsx"
      provides: "Per-submission review screen with approve/reject panel"
      contains: "Approve"
    - path: "resources/js/components/ingredients/submit-action.tsx"
      provides: "Submit/Withdraw CTA + status badge on the ingredient detail page"
      contains: "Submit"
    - path: "resources/js/components/nav-main.tsx"
      provides: "Nav item badge rendering"
      contains: "badge"
    - path: "lang/en/app.php"
      provides: "EN moderation copy keys"
      contains: "submit"
  key_links:
    - from: "resources/js/components/app-sidebar.tsx"
      to: "usePage().props.pendingIngredientReviewCount"
      via: "Ingredient Review nav item with badge"
      pattern: "pendingIngredientReviewCount"
    - from: "resources/js/components/ingredients/submit-action.tsx"
      to: "ingredients.submit / ingredients.withdraw routes"
      via: "router.post / router.delete"
      pattern: "router\\.(post|delete)"
    - from: "resources/js/pages/admin/ingredients/show.tsx"
      to: "admin.ingredient-submissions.approve / reject routes"
      via: "router.post in dialog confirm"
      pattern: "ingredient-submissions"
---

<objective>
Build the complete moderation frontend: the owner-facing Submit/Withdraw CTA and
status badge on the ingredient detail page (with the duplicate-warning dialog and
frozen banner), the moderator review queue page with the data-completeness
signal, the per-submission review screen with the approve/reject panel, the
pending-count nav badge, the in-app notification surface, the "Contributed by"
credit, and EN/EL translations. Ends with an end-to-end human-verify checkpoint.

Purpose: Delivers the user-visible behavior for all three Phase 7 success
criteria — submit, review, promote.
Output: 4 new components, 2 rewritten admin pages, ingredient detail page
updates, nav badge, type updates, IngredientDetailResource update, EN/EL copy.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ingredient-moderation/07-CONTEXT.md
@.planning/phases/07-ingredient-moderation/07-UI-SPEC.md
@.planning/phases/07-ingredient-moderation/07-02-SUMMARY.md

@resources/js/pages/ingredients/show.tsx
@resources/js/pages/admin/ingredients.tsx
@resources/js/components/app-sidebar.tsx
@resources/js/components/nav-main.tsx

<interfaces>
<!-- Contracts from 07-02 + existing frontend. Do NOT re-explore. -->

Shared Inertia props (07-02 — HandleInertiaRequests::share()):
- `pendingIngredientReviewCount: number | null` — null when user lacks `review-ingredients`
- `ingredientNotifications: Array<{ id: string; data: { ingredient_id: number; ingredient_name: string; decision: 'approved'|'rejected'; notes: string|null }; created_at: string }> | null`
- `auth.permissions: string[]` — includes `review-ingredients` for moderators

Routes (07-02 — generate Wayfinder imports from `@/routes`):
- `ingredients.submit` — POST `ingredients/{ingredient}/submit`
- `ingredients.withdraw` — DELETE `ingredients/{ingredient}/submit`
- `ingredients.duplicate-check` — GET `ingredients/{ingredient}/duplicate-check` → JSON `{ matches: Array<{id, name}> }`
- `admin.ingredients.index` — GET `admin/ingredients` (the queue)
- `admin.ingredient-submissions.show` — GET `admin/ingredient-submissions/{submission}`
- `admin.ingredient-submissions.approve` — POST `.../{submission}/approve`
- `admin.ingredient-submissions.reject` — POST `.../{submission}/reject`

Queue prop (admin/ingredients page, 07-02 IngredientSubmissionResource):
`submissions: Array<{ id, submission_number, status, submitted_at, submitter: {name}, ingredient: {id, name, category: {name, parent}}, completeness: {nutrition_filled: bool, allergens_set: bool, conversions_added: bool}, prior_rejections: Array<{notes, reviewed_at, reviewer}> }>`

Review screen props (admin/ingredients/show page, 07-02):
- `submission` — same IngredientSubmissionResource shape (incl. `prior_rejections`)
- `ingredient` — IngredientDetailResource shape (nutrition columns, `allergens`, `conversions`, `category`, `name`)

IngredientDetailResource (existing, ingredients/show page) — relevant fields:
`id, name, name_en, name_el, is_private, category, energy_kcal...cholesterol_mg, allergens[], conversions[], verified, verified_by`.
NOTE: 07-02 did NOT add submission_status to IngredientDetailResource — Task 3
of THIS plan adds `submission_status` and `contributed_by` (the submitter name
when approved) to that resource, consistent with the existing `verified` field.

Existing patterns:
- shadcn components in `resources/js/components/ui/`: Badge, Dialog, Textarea, Table, Button, Tabs, Tooltip, Skeleton, Alert, Sonner.
- `useLaravelReactI18n` `t()` for all strings; sonner `toast` for feedback.
- `router` from `@inertiajs/react` for mutations; mutations return redirects (Phase 1 lesson) — use `onSuccess` for toast.
- `nav-main.tsx` currently renders `SidebarMenuButton` with title + icon; `NavItem` type has `title, href, icon?, isActive?`.
- `app-sidebar.tsx` builds `mainNavItems`; `canManageUsers` gates the Users nav item — mirror this for the moderator review item.
- `fetch` for the duplicate-check GET (it returns JSON, not an Inertia response).
- `app-sidebar.tsx` imports route helpers like `index as adminUsersIndex` from `@/routes/admin/users`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NavItem badge support + Ingredient Review nav entry with pending-count badge + notification surface</name>
  <files>resources/js/types/navigation.ts, resources/js/components/nav-main.tsx, resources/js/components/app-sidebar.tsx, resources/js/components/ingredient-notifications.tsx</files>
  <read_first>
    - resources/js/types/navigation.ts (the NavItem type being extended)
    - resources/js/components/nav-main.tsx (the file being modified — current SidebarMenuButton render)
    - resources/js/components/app-sidebar.tsx (the file being modified — mainNavItems + canManageUsers gating pattern)
    - resources/js/components/ui/badge.tsx (Badge variants available)
    - resources/js/components/ui/sidebar.tsx (SidebarMenuBadge if it exists — check before hand-rolling)
    - resources/js/components/ui/alert.tsx (Alert for the notification list rendering)
    - .planning/phases/07-ingredient-moderation/07-UI-SPEC.md (section 2 nav badge; section 4 in-app notification)
  </read_first>
  <action>
    Edit `resources/js/types/navigation.ts` — add `badge?: string;` to the
    `NavItem` type (a numeric badge count rendered as a string, e.g. `"3"`).

    Edit `resources/js/components/nav-main.tsx` — when `item.badge` is set, render
    a shadcn `Badge` with `variant="destructive"` next to the `<span>{item.title}</span>`.
    If `resources/js/components/ui/sidebar.tsx` exports `SidebarMenuBadge`, use it
    as the wrapper; otherwise render the `Badge` inline with `className="ml-auto"`.
    Do not change icon/title rendering.

    Edit `resources/js/components/app-sidebar.tsx`:
    - Read `pendingIngredientReviewCount` from `usePage().props` (typed `number | null`).
    - Compute `canReviewIngredients = permissions.includes('review-ingredients')`.
    - Add a `mainNavItems` entry (gated like the existing `canManageUsers` Users
      entry — only when `canReviewIngredients`):
      `{ title: t('app.nav.ingredient_review'), href: adminIngredientsIndex().url, icon: ClipboardList, badge: pendingIngredientReviewCount && pendingIngredientReviewCount > 0 ? String(pendingIngredientReviewCount) : undefined }`
    - Import `ClipboardList` from `lucide-react` and `index as adminIngredientsIndex` from `@/routes/admin/ingredients`.

    Create `resources/js/components/ingredient-notifications.tsx` — a component
    that reads `usePage().props.ingredientNotifications` and renders unread
    decision notifications. Per UI-SPEC section 4, use a bell-icon
    (`Bell` from lucide-react) trigger with a `Badge variant="destructive"` count
    when there are unread notifications; clicking opens a popover/dropdown listing
    each notification as an `Alert`:
    - approved: t('app.ingredients.notif_approved', { name }) — "[name] was approved and is now in the official library."
    - rejected: t('app.ingredients.notif_rejected', { name, note }) — "[name] was rejected. [note]. You can update it and resubmit."
    Each entry links to `ingredients.show` for that `ingredient_id` (navigating
    there marks it read server-side — no client mutation needed). If
    `ingredientNotifications` is null or empty, render nothing (no red dot).
    Mount `<IngredientNotifications />` in `app-sidebar.tsx` SidebarFooter (above
    `<NavUser />`) to keep it out of the primary nav (UI-SPEC: must not block
    primary navigation).

    Run `npm run build` to confirm the bundle compiles.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep "badge?: string" resources/js/types/navigation.ts` matches.
    - `grep "item.badge" resources/js/components/nav-main.tsx` matches; `grep "destructive" resources/js/components/nav-main.tsx` matches.
    - `grep "pendingIngredientReviewCount" resources/js/components/app-sidebar.tsx` matches.
    - `grep "review-ingredients" resources/js/components/app-sidebar.tsx` matches.
    - `grep "ClipboardList" resources/js/components/app-sidebar.tsx` matches.
    - `resources/js/components/ingredient-notifications.tsx` exists; `grep "ingredientNotifications"` matches; `grep "export default\|export function"` matches.
    - `<IngredientNotifications` is referenced in `app-sidebar.tsx`.
    - `npm run build` completes with exit code 0 (no TypeScript or Vite errors).
  </acceptance_criteria>
  <done>The sidebar shows an "Ingredient Review" entry (moderators only) with a destructive pending-count badge; an in-app notification bell surfaces decision notifications; the bundle builds.</done>
</task>

<task type="auto">
  <name>Task 2: Review queue page + per-submission review screen + completeness signal component</name>
  <files>resources/js/components/ingredients/submission-status-badge.tsx, resources/js/components/ingredients/submission-completeness.tsx, resources/js/pages/admin/ingredients.tsx, resources/js/pages/admin/ingredients/show.tsx</files>
  <read_first>
    - resources/js/pages/admin/ingredients.tsx (the stub page being replaced — current default export shape)
    - resources/js/pages/admin/ingredients/show.tsx (the stub created in 07-01 being replaced)
    - resources/js/pages/ingredients/show.tsx (existing ingredient detail — reuse its Tabs / nutrition / allergen / conversion rendering for the review screen)
    - resources/js/pages/admin (any users.tsx admin table page if present — admin Table layout + AppLayout usage)
    - resources/js/components/ui/table.tsx, dialog.tsx, textarea.tsx, tooltip.tsx, alert.tsx, badge.tsx
    - .planning/phases/07-ingredient-moderation/07-UI-SPEC.md (section 2 queue columns + empty state; section 3 review screen layout + approve/reject flows; Color status badge mapping; Spacing scale)
    - .planning/phases/07-ingredient-moderation/07-02-SUMMARY.md (the submissions / submission / ingredient prop shapes)
  </read_first>
  <action>
    Create `resources/js/components/ingredients/submission-status-badge.tsx` — a
    component taking `status: 'private'|'submitted'|'approved'|'rejected'` and
    rendering a shadcn `Badge`. Per UI-SPEC Color mapping:
    - `private` → render nothing (no badge)
    - `submitted` → `variant="secondary"`, text `t('app.ingredients.status_submitted')` ("Under Review")
    - `rejected` → `variant="destructive"`, text `t('app.ingredients.status_rejected')` ("Rejected")
    - `approved` → `variant="default"`, text `t('app.ingredients.status_approved')` ("Official")

    Create `resources/js/components/ingredients/submission-completeness.tsx` —
    takes `completeness: {nutrition_filled, allergens_set, conversions_added}` and
    renders three 16px `Tooltip`-wrapped dots with `gap-1` (8-point scale `sm`).
    Filled = `bg-green-500`, absent = `bg-muted`. Tooltip text keys:
    `app.ingredients.completeness_nutrition`, `..._allergens`, `..._conversions`.

    Replace `resources/js/pages/admin/ingredients.tsx` with the real review queue.
    Default-export a page component receiving `submissions` prop (shape from
    interfaces). Layout: page heading `t('app.ingredients.queue_heading')`
    ("Ingredient Review Queue", `text-2xl font-semibold`), then a full-width
    shadcn `Table` with columns per UI-SPEC section 2:
    - Ingredient Name (`ingredient.name`); if `submission_number > 1` render a
      `Badge variant="secondary"` "Resubmit #N" (`t('app.ingredients.resubmit_badge', {n})`) with a Tooltip.
    - Submitter (`submitter.name`)
    - Category (`ingredient.category.name`)
    - Completeness (`<SubmissionCompleteness completeness={row.completeness} />`)
    - Submitted (relative date)
    - Action — a link-button "Review" navigating to
      `adminIngredientSubmissionsShow({ submission: row.id }).url`.
    Rows are already FIFO-ordered server-side — render in array order.
    Empty state when `submissions.length === 0`: centered `text-muted-foreground`
    heading `t('app.ingredients.queue_empty_heading')` + body
    `t('app.ingredients.queue_empty_body')`. Use the same `AppLayout` the existing
    admin pages use — match the sibling admin page.

    Replace `resources/js/pages/admin/ingredients/show.tsx` with the per-submission
    review screen. Default-export a component receiving `submission` and
    `ingredient` props. Layout per UI-SPEC section 3 — two-column on desktop
    (`grid lg:grid-cols-[1fr_320px]`), single-column stacked on mobile:
    - LEFT: ingredient name heading, category/source line, then a shadcn `Tabs`
      block (Nutrition | Allergens | Conversions) reusing the same tab structure
      and rendering as `resources/js/pages/ingredients/show.tsx` (read it and
      replicate the nutrition table / allergen list / conversion list — do not
      invent a new layout).
    - RIGHT (sticky on desktop — `lg:sticky lg:top-16`): the moderation panel:
      1. "Submitted by [name] on [date]" — `text-sm text-muted-foreground`.
      2. If `submission.submission_number > 1` OR `prior_rejections.length > 0`:
         render each `prior_rejections` entry as an `Alert variant="default"`
         reading "Rejected on [date] by [reviewer]: [notes]".
      3. "Approve Ingredient" button (`variant="default"`, full-width) → opens an
         approve `Dialog` (title/body/confirm/dismiss copy from the Copywriting
         Contract; an optional notes `Textarea` pre-expanded). Confirm posts
         `router.post(adminIngredientSubmissionsApprove({submission: submission.id}).url, { notes })` with `onSuccess` toast `t('app.ingredients.approve_toast')` and `onError` toast `t('app.ingredients.approve_error')`.
      4. "Reject Ingredient" button (`variant="destructive"`, full-width) → opens a
         reject `Dialog` with a REQUIRED notes `Textarea` (min-height 80px). Show
         inline error `t('app.ingredients.reject_note_error')` if confirmed empty.
         Confirm posts `router.post(adminIngredientSubmissionsReject({submission: submission.id}).url, { notes })`; on 422 keep the dialog open and surface the error; `onSuccess` toast `t('app.ingredients.reject_toast')`.
    Both successful actions redirect server-side to the queue — no client redirect needed.

    Import Wayfinder route helpers from `@/routes/admin/ingredient-submissions`.
    Run `npm run build`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/components/ingredients/submission-status-badge.tsx` exists; `grep "destructive"` and `grep "secondary"` both match.
    - `resources/js/components/ingredients/submission-completeness.tsx` exists; `grep "bg-green-500"` and `grep "bg-muted"` both match; `grep "Tooltip"` matches.
    - `resources/js/pages/admin/ingredients.tsx` — `grep "submissions"` matches; `grep "resubmit_badge\|Resubmit"` matches; `grep "queue_empty"` matches; the file no longer contains the stub string "arrives in Phase 7".
    - `resources/js/pages/admin/ingredients/show.tsx` — `grep "Approve"` and `grep "Reject"` match; `grep "ingredient-submissions"` matches (route helper usage); `grep "prior_rejections"` matches.
    - `grep "lg:grid-cols\|lg:sticky" resources/js/pages/admin/ingredients/show.tsx` matches (two-column desktop layout).
    - `npm run build` completes with exit code 0.
  </acceptance_criteria>
  <done>The review queue table renders FIFO rows with completeness dots and resubmit badges; the per-submission review screen shows full ingredient data plus a sticky approve/reject panel with required-notes-on-reject; the bundle builds.</done>
</task>

<task type="auto">
  <name>Task 3: Submit/Withdraw action on ingredient detail page + Contributed-by credit + EN/EL copy</name>
  <files>app/Http/Resources/IngredientDetailResource.php, app/Http/Controllers/Ingredients/IngredientController.php, resources/js/types/ingredient.ts, resources/js/components/ingredients/submit-action.tsx, resources/js/pages/ingredients/show.tsx, lang/en/app.php, lang/el/app.php</files>
  <read_first>
    - resources/js/pages/ingredients/show.tsx (the file being modified — current action area, VerifyAction placement, `can` prop usage)
    - resources/js/types/ingredient.ts (the type being extended — current IngredientDetail shape)
    - app/Http/Resources/IngredientDetailResource.php (must add submission_status + contributed_by)
    - app/Http/Controllers/Ingredients/IngredientController.php (show() — must add `can.submit` / `can.withdraw` flags and eager-load latestSubmission)
    - resources/js/components/ui/dialog.tsx, alert.tsx, button.tsx
    - lang/en/app.php and lang/el/app.php (the files being extended — existing app.ingredients.* namespace)
    - .planning/phases/07-ingredient-moderation/07-UI-SPEC.md (section 1 submit/withdraw flows + frozen banner; section 5 contributed-by; full Copywriting Contract table)
  </read_first>
  <action>
    Backend resource update — edit `app/Http/Resources/IngredientDetailResource.php`:
    add `'submission_status' => $this->submission_status?->value` and
    `'contributed_by' => $this->submission_status === \App\Enums\SubmissionStatus::Approved ? $this->latestSubmission?->submittedBy?->name : null`
    to the returned array. Edit `app/Http/Controllers/Ingredients/IngredientController.php`
    `show()`: add `'latestSubmission.submittedBy'` to the `$ingredient->load([...])`
    call, and add `'submit' => $request->user()->can('submit', $ingredient)` and
    `'withdraw' => $request->user()->can('withdraw', $ingredient)` to the `can` array.
    Run `vendor/bin/pint --dirty --format agent`.

    Edit `resources/js/types/ingredient.ts` — add to the ingredient detail type:
    `submission_status?: 'private' | 'submitted' | 'approved' | 'rejected' | null;`
    and `contributed_by?: string | null;`. Add `submit?: boolean` and
    `withdraw?: boolean` to the `can` type for the show page.

    Create `resources/js/components/ingredients/submit-action.tsx` — the owner CTA.
    Props: `ingredient` (id, name, submission_status, contributed_by) and `can`
    (submit, withdraw). Behavior per UI-SPEC section 1:
    - `submission_status` `private` or `rejected` AND `can.submit` → render a
      "Submit for Inclusion" `Button variant="default"` (min 44px height on mobile).
      On click: GET `ingredients.duplicate-check` via `fetch` (it returns JSON).
      If `matches.length > 0` → open a `Dialog` listing up to 5 matches with
      "Submit Anyway" + "Keep Editing"; else open the plain confirmation `Dialog`
      ("Submit [name] for inclusion..." copy). Confirm →
      `router.post(submit({ingredient: id}).url, {}, { onSuccess: () => toast(t('app.ingredients.submit_toast')), onError: () => toast(t('app.ingredients.submit_error')) })`.
    - `submission_status` `submitted` → render `<SubmissionStatusBadge status="submitted" />`
      + a "Withdraw Submission" `Button variant="outline"` → confirmation `Dialog`
      → `router.delete(withdraw({ingredient: id}).url, { onSuccess: () => toast(...) })`.
    - `submission_status` `approved` → render `<SubmissionStatusBadge status="approved" />`
      and nothing else (the credit is rendered separately, see below).
    Use Wayfinder helpers from `@/routes/ingredients`.

    Edit `resources/js/pages/ingredients/show.tsx`:
    - Render `<SubmitAction ingredient={ingredient} can={can} />` in the action area
      alongside the existing VerifyAction.
    - When `ingredient.submission_status === 'submitted'`: render an
      `Alert variant="default"` with `t('app.ingredients.frozen_banner')` and HIDE
      the Edit button (the `can.manage` flag already returns false server-side for
      submitted ingredients via the 07-01 policy guard — confirm the Edit button is
      already gated on `can.manage`; if it is, no extra change needed beyond the banner).
    - When `ingredient.submission_status === 'approved'` AND
      `ingredient.contributed_by`: render `<p className="text-sm text-muted-foreground">{t('app.ingredients.contributed_by', { name: ingredient.contributed_by })}</p>`
      below the ingredient name heading.

    Edit `lang/en/app.php` and `lang/el/app.php` — add ALL keys under the
    `ingredients` array (matching the existing nested structure). Copy values
    VERBATIM from the UI-SPEC Copywriting Contract table. Required keys
    (EN value / EL value):
    - `submit_cta`: "Submit for Inclusion" / "Υποβολή για Ένταξη"
    - `submit_dialog_title`: "Submit for inclusion?" / "Υποβολή για ένταξη;"
    - `submit_dialog_body`: "Submit :name for inclusion in the official library. A moderator will review your submission." / "Υποβάλετε :name για ένταξη στην επίσημη βιβλιοθήκη. Ένας συντονιστής θα εξετάσει την υποβολή σας."
    - `submit_dialog_body_duplicates`: "Similar ingredients already exist in the official library. Review them before submitting." / "Παρόμοια συστατικά υπάρχουν ήδη στην επίσημη βιβλιοθήκη. Ελέγξτε τα πριν υποβάλετε."
    - `submit_confirm`: "Submit for Inclusion" / "Υποβολή για Ένταξη"
    - `submit_dismiss`: "Keep Editing" / "Συνέχεια Επεξεργασίας"
    - `submit_anyway`: "Submit Anyway" / "Υποβολή Ούτως ή Άλλως"
    - `submit_toast`: "Your ingredient has been submitted for review." / "Το συστατικό σας υποβλήθηκε για έλεγχο."
    - `submit_error`: "Could not submit your ingredient. Please try again or contact support." / "Δεν ήταν δυνατή η υποβολή του συστατικού σας. Δοκιμάστε ξανά ή επικοινωνήστε με την υποστήριξη."
    - `withdraw_cta`: "Withdraw Submission" / "Ανάκληση Υποβολής"
    - `withdraw_dialog_title`: "Withdraw submission?" / "Ανάκληση υποβολής;"
    - `withdraw_dialog_body`: ":name will return to private. You can update it and submit again." / "Το :name θα επιστρέψει σε ιδιωτικό. Μπορείτε να το ενημερώσετε και να το υποβάλετε ξανά."
    - `withdraw_confirm`: "Withdraw" / "Ανάκληση"
    - `withdraw_dismiss`: "Keep Submitted" / "Διατήρηση Υποβολής"
    - `withdraw_toast`: "Submission withdrawn." / "Η υποβολή ανακλήθηκε."
    - `withdraw_error`: "Could not withdraw your submission. Please try again or contact support." / "Δεν ήταν δυνατή η ανάκληση της υποβολής σας. Δοκιμάστε ξανά ή επικοινωνήστε με την υποστήριξη."
    - `frozen_banner`: "This ingredient is under review. Data is locked until a moderator decides." / "Αυτό το συστατικό βρίσκεται υπό έλεγχο. Τα δεδομένα είναι κλειδωμένα έως ότου αποφανθεί συντονιστής."
    - `status_submitted`: "Under Review" / "Υπό Έλεγχο"
    - `status_rejected`: "Rejected" / "Απορρίφθηκε"
    - `status_approved`: "Official" / "Επίσημο"
    - `resubmit_badge`: "Resubmit #:n" / "Εκ νέου Υποβολή #:n"
    - `contributed_by`: "Contributed by :name" / "Συνεισφορά από :name"
    - `queue_heading`: "Ingredient Review Queue" / "Ουρά Ελέγχου Συστατικών"
    - `queue_empty_heading`: "No pending submissions" / "Δεν υπάρχουν εκκρεμείς υποβολές"
    - `queue_empty_body`: "All ingredient submissions have been reviewed. Check back later." / "Όλες οι υποβολές συστατικών έχουν εξεταστεί. Ελέγξτε ξανά αργότερα."
    - `approve_cta`: "Approve Ingredient" / "Έγκριση Συστατικού"
    - `approve_dialog_title`: "Approve for official library?" / "Έγκριση για επίσημη βιβλιοθήκη;"
    - `approve_dialog_body`: "This action is permanent. The ingredient will be promoted to the official library and marked as verified." / "Αυτή η ενέργεια είναι μόνιμη. Το συστατικό θα προαχθεί στην επίσημη βιβλιοθήκη και θα επισημανθεί ως επαληθευμένο."
    - `approve_confirm`: "Approve & Promote" / "Έγκριση & Προαγωγή"
    - `approve_dismiss`: "Keep Reviewing" / "Συνέχεια Ελέγχου"
    - `approve_note_label`: "Add a note (optional)" / "Προσθήκη σημείωσης (προαιρετικό)"
    - `approve_toast`: "Ingredient approved and promoted to the official library." / "Το συστατικό εγκρίθηκε και προήχθη στην επίσημη βιβλιοθήκη."
    - `approve_error`: "Could not approve this submission. Please try again or contact support." / "Δεν ήταν δυνατή η έγκριση αυτής της υποβολής. Δοκιμάστε ξανά ή επικοινωνήστε με την υποστήριξη."
    - `reject_cta`: "Reject Ingredient" / "Απόρριψη Συστατικού"
    - `reject_dialog_title`: "Reject submission?" / "Απόρριψη υποβολής;"
    - `reject_dialog_body`: "Provide feedback so the submitter knows what to fix before resubmitting." / "Παρέχετε σχόλια ώστε ο υποβάλλων να γνωρίζει τι να διορθώσει πριν υποβάλει εκ νέου."
    - `reject_note_label`: "Rejection notes (required)" / "Σημειώσεις απόρριψης (υποχρεωτικό)"
    - `reject_note_error`: "Rejection notes are required." / "Οι σημειώσεις απόρριψης είναι υποχρεωτικές."
    - `reject_confirm`: "Reject with Notes" / "Απόρριψη με Σημειώσεις"
    - `reject_dismiss`: "Keep Reviewing" / "Συνέχεια Ελέγχου"
    - `reject_toast`: "Ingredient rejected. The submitter has been notified." / "Το συστατικό απορρίφθηκε. Ο υποβάλλων ενημερώθηκε."
    - `reject_error`: "Could not reject this submission. Please try again or contact support." / "Δεν ήταν δυνατή η απόρριψη αυτής της υποβολής. Δοκιμάστε ξανά ή επικοινωνήστε με την υποστήριξη."
    - `notif_approved`: ":name was approved and is now in the official library." / "Το :name εγκρίθηκε και είναι πλέον στην επίσημη βιβλιοθήκη."
    - `notif_rejected`: ":name was rejected. :note. You can update it and resubmit." / "Το :name απορρίφθηκε. :note. Μπορείτε να το ενημερώσετε και να το υποβάλετε εκ νέου."
    - `completeness_nutrition`: "Nutrition data complete" / "Πλήρη δεδομένα διατροφής"
    - `completeness_allergens`: "Allergens declared" / "Δηλωμένα αλλεργιογόνα"
    - `completeness_conversions`: "Unit conversions added" / "Προστέθηκαν μετατροπές μονάδων"
    Also add to the `nav` array: `ingredient_review`: "Ingredient Review" / "Έλεγχος Συστατικών"
    and `nav_review_tooltip`: ":n ingredients awaiting review" / ":n συστατικά αναμένουν έλεγχο".

    Run `vendor/bin/pint --dirty --format agent` then `npm run build`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8 && php artisan test --compact tests/Feature/Ingredients/ 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/components/ingredients/submit-action.tsx` exists; `grep "duplicate-check"` matches; `grep "router.post"` and `grep "router.delete"` both match.
    - `app/Http/Resources/IngredientDetailResource.php` — `grep "submission_status"` and `grep "contributed_by"` both match.
    - `app/Http/Controllers/Ingredients/IngredientController.php` — `grep "'submit' =>"` and `grep "latestSubmission"` both match.
    - `resources/js/pages/ingredients/show.tsx` — `grep "SubmitAction"` matches; `grep "frozen_banner"` matches; `grep "contributed_by"` matches.
    - `resources/js/types/ingredient.ts` — `grep "submission_status"` matches.
    - `lang/en/app.php` — `grep "submit_toast"`, `grep "approve_toast"`, `grep "reject_note_error"`, `grep "contributed_by"`, `grep "ingredient_review"` all match.
    - `lang/el/app.php` — `grep "Υποβολή για Ένταξη"`, `grep "Συνεισφορά από"`, `grep "Έλεγχος Συστατικών"` all match.
    - Every Phase 7 key present in `lang/en/app.php` is also present in `lang/el/app.php` (matching key count for the new keys).
    - `npm run build` exits 0; `php artisan test --compact tests/Feature/Ingredients/` — all tests still GREEN.
  </acceptance_criteria>
  <done>The ingredient detail page shows the Submit/Withdraw CTA, status badge, frozen banner, and contributed-by credit; IngredientDetailResource carries submission_status + contributed_by; all EN/EL moderation copy exists with matching keys; the bundle builds and the test suite stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end human verification of the submission to review to promotion flow</name>
  <files>none — verification only</files>
  <read_first>
    - .planning/phases/07-ingredient-moderation/07-UI-SPEC.md (the full interaction contract being verified)
  </read_first>
  <what-built>
    The complete Phase 7 ingredient moderation workflow: owner submit/withdraw with
    duplicate warning and frozen banner, the moderator FIFO review queue with
    completeness signal and pending-count nav badge, the per-submission review
    screen with approve/reject, convert-in-place promotion, and in-app
    notifications — backed by the full GREEN test suite (07-01 + 07-02 + 07-03).
  </what-built>
  <action>
    This is a human-verification checkpoint — no code is written. The executor
    must ensure dev assets are running (ask the user to run `npm run dev` or
    `composer run dev` if the site does not reflect changes), then present the
    verification steps below to the user and pause for the resume signal.
  </action>
  <how-to-verify>
    1. Ensure dev assets are running (`npm run dev` or `composer run dev`) and the
       site is reachable at the Herd URL (use the get-absolute-url tool).
    2. As a regular User: open one of your private ingredients' detail page. Confirm
       a "Submit for Inclusion" button is visible. Click it.
    3. Confirm the submit confirmation dialog appears (with a duplicate-warning list
       if a similarly-named official ingredient exists). Confirm submission.
    4. Confirm a success toast appears, the badge changes to "Under Review", the
       Edit button is gone, and a frozen banner reads "This ingredient is under
       review. Data is locked until a moderator decides."
    5. Confirm a "Withdraw Submission" button is now present. (Optionally test
       withdraw → it reverts to private and the Edit button returns.)
    6. Log in as a Moderator. Confirm the sidebar shows an "Ingredient Review" entry
       with a red pending-count badge. Open the review queue — confirm the
       submitted ingredient appears, oldest-first, with three completeness dots.
    7. Click "Review" → confirm the per-submission screen shows the ingredient's
       full nutrition/allergen/conversion data plus a sticky approve/reject panel.
    8. Click "Reject Ingredient" — confirm the dialog requires a note (empty submit
       shows an inline error). Reject one submission with a note.
    9. As the original User: confirm an in-app notification reports the rejection
       with the note; the ingredient badge shows "Rejected" and it is editable again.
    10. Resubmit the rejected ingredient. As the Moderator, open it again — confirm
        a "Resubmit #2" badge and the prior rejection note are shown on the review
        screen. Click "Approve Ingredient", confirm the permanence warning, approve.
    11. Confirm the ingredient is promoted: it now appears in the official library
        for ALL users, shows an "Official" badge and a "Contributed by [name]"
        credit, and the original user's recipes referencing it still work.
    12. Confirm the EN/EL language switch translates all moderation copy.
  </how-to-verify>
  <verify>
    <automated>npm run build 2>&1 | tail -4 && php artisan test --compact tests/Feature/Ingredients/ 2>&1 | tail -6</automated>
  </verify>
  <done>The human-verifier confirms the full submit to review to promote flow works in both EN and EL; the bundle builds and the Ingredients suite is GREEN.</done>
  <resume-signal>Type "approved" if the full submit to review to promote flow works in both languages, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes with no TypeScript or Vite errors.
- `php artisan test --compact tests/Feature/Ingredients/` — entire suite GREEN.
- `vendor/bin/pint --test --format agent` — no style issues on changed PHP files.
- The human-verify checkpoint confirms the end-to-end flow in both EN and EL.
</verification>

<success_criteria>
- Owner sees Submit/Withdraw CTA, status badge, frozen banner, and contributed-by credit (INGR-09).
- Moderator sees a FIFO review queue with completeness signal + pending-count nav badge, and a per-submission review screen with approve/reject (INGR-10).
- Approving promotes the ingredient to the official library visible to all; rejecting reverts it to private; submitter is notified in-app (INGR-11).
- All copy exists in EN and EL with matching keys.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingredient-moderation/07-03-SUMMARY.md`
</output>
