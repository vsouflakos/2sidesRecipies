---
phase: 07-ingredient-moderation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/Http/Controllers/Ingredients/IngredientSubmissionController.php
  - app/Http/Controllers/Admin/IngredientReviewController.php
  - app/Http/Controllers/Admin/IngredientSubmissionController.php
  - app/Http/Requests/Ingredients/SubmitIngredientRequest.php
  - app/Http/Requests/Admin/ApproveIngredientRequest.php
  - app/Http/Requests/Admin/RejectIngredientRequest.php
  - app/Notifications/IngredientDecisionNotification.php
  - app/Http/Resources/IngredientSubmissionResource.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - routes/web.php
tests_turned_green:
  - tests/Feature/Ingredients/IngredientSubmissionTest.php
  - tests/Feature/Ingredients/IngredientModerationTest.php
  - tests/Feature/Ingredients/IngredientPromotionTest.php
autonomous: true
requirements: [INGR-09, INGR-10, INGR-11]
must_haves:
  truths:
    - "A user can submit a private ingredient for inclusion; its status becomes submitted and a history row is created"
    - "A user can withdraw a pending submission; the ingredient reverts to private and is unfrozen"
    - "A moderator can open a FIFO-ordered review queue of submitted ingredients"
    - "A moderator can approve a submission, promoting the ingredient to official and verified in one atomic transaction"
    - "A moderator can reject a submission with required notes; the ingredient reverts to private"
    - "The submitter receives a database notification when a moderator approves or rejects"
  artifacts:
    - path: "app/Http/Controllers/Ingredients/IngredientSubmissionController.php"
      provides: "Owner submit (store) + withdraw (destroy) + duplicate-check actions"
      contains: "function store"
    - path: "app/Http/Controllers/Admin/IngredientSubmissionController.php"
      provides: "Moderator approve + reject actions with convert-in-place promotion"
      contains: "function approve"
    - path: "app/Http/Controllers/Admin/IngredientReviewController.php"
      provides: "FIFO review queue index + per-submission review show"
      contains: "function show"
    - path: "app/Notifications/IngredientDecisionNotification.php"
      provides: "Database-channel notification for approve/reject decisions"
      contains: "via"
    - path: "routes/web.php"
      provides: "submit/withdraw owner routes + queue/review/approve/reject admin routes"
      contains: "ingredient-submissions"
  key_links:
    - from: "app/Http/Controllers/Admin/IngredientSubmissionController.php"
      to: "App\\Notifications\\IngredientDecisionNotification"
      via: "submitter->notify() AFTER DB::transaction commits"
      pattern: "notify\\(new IngredientDecisionNotification"
    - from: "app/Http/Middleware/HandleInertiaRequests.php"
      to: "ingredients.submission_status column"
      via: "pendingIngredientReviewCount shared prop"
      pattern: "pendingIngredientReviewCount"
    - from: "routes/web.php"
      to: "IngredientSubmissionController"
      via: "ingredients/{ingredient}/submit declared before ingredients/{ingredient} wildcard"
      pattern: "ingredients/\\{ingredient\\}/submit"
---

<objective>
Build the entire moderation backend: the owner submit/withdraw actions with a
submit-time duplicate-name check, the moderator review queue + per-submission
review screen data, the approve/reject decision actions with atomic
convert-in-place promotion, the database notification, and the shared Inertia
props (pending review count + submitter notifications).

Purpose: Turns the 07-01 RED test suite GREEN — delivers all backend behavior
for INGR-09, INGR-10, INGR-11.
Output: 3 controllers, 3 FormRequests, 1 notification, 1 resource, route
declarations, shared-prop additions.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ingredient-moderation/07-CONTEXT.md
@.planning/phases/07-ingredient-moderation/07-RESEARCH.md
@.planning/phases/07-ingredient-moderation/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from 07-01 + existing Phase 2 code. Do NOT re-explore. -->

From app/Enums/SubmissionStatus.php (07-01):
- `enum SubmissionStatus: string` — cases `Private`, `Submitted`, `Approved`, `Rejected`, `Withdrawn`

From app/Models/IngredientSubmission.php (07-01):
- `$fillable`: ingredient_id, submitted_by, reviewed_by, status, notes, submission_number, submitted_at, reviewed_at
- casts: status => SubmissionStatus, submitted_at/reviewed_at => datetime
- relations: `ingredient()`, `submittedBy()`, `reviewedBy()`

From app/Models/Ingredient.php (07-01 updated):
- cast `submission_status => SubmissionStatus::class`; helper `isPendingReview(): bool`
- relations `submissions(): HasMany`, `latestSubmission(): HasOne`
- `name_cache` column holds a display name; `nameFor(string $locale): string` helper
- relations `translations()`, `allergens()` (pivot `state`), `conversions()`, `category()`, `user()`

From app/Policies/IngredientPolicy.php (07-01 updated):
- `submit(User,Ingredient): bool`, `withdraw(User,Ingredient): bool` — use via `Gate::authorize('submit'|'withdraw', $ingredient)`

Gate pattern (base Controller has NO AuthorizesRequests trait — Phase 2 lesson):
```php
use Illuminate\Support\Facades\Gate;
Gate::authorize('submit', $ingredient);
```

Inertia mutation pattern (Phase 1 lesson): controllers return `back()` /
`redirect()->route(...)->with('success', __(...))`, never JSON. FormRequest
authorization failure → 403; validation failure → 422.

From app/Http/Controllers/Admin/IngredientVerificationController.php (the verify write pattern to mirror):
```php
DB::transaction(function () use ($ingredient) {
    $ingredient->verified = true;
    $ingredient->verified_by = auth()->id();
    $ingredient->verified_at = now();
    $ingredient->save();
});
return back()->with('success', __('app.ingredients.verify_toast'));
```

From app/Http/Requests/Admin/VerifyIngredientRequest.php (FormRequest authorize pattern):
```php
public function authorize(): bool { return $this->user()->can('verify-ingredients'); }
```

From routes/web.php — current ordering (static segments BEFORE the wildcard — Phase 2/3 lesson):
```php
Route::get('ingredients/create', ...)->name('ingredients.create');
Route::post('ingredients', ...)->name('ingredients.store');
Route::get('ingredients/{ingredient}/edit', ...)->name('ingredients.edit');
Route::put('ingredients/{ingredient}', ...)->name('ingredients.update');
Route::delete('ingredients/{ingredient}', ...)->name('ingredients.destroy');
Route::post('ingredients/{ingredient}/prices', ...)->name('ingredients.prices.store');
Route::get('ingredients/{ingredient}', ...)->name('ingredients.show');  // wildcard LAST
```
Admin review group (line 83): `['auth','verified','permission:review-ingredients']`, prefix `admin`, name `admin.`.

From app/Http/Middleware/HandleInertiaRequests.php — current share() returns:
`name`, `auth` (user + permissions), `locale`, `sidebarOpen`. New props are added to this array.

Duplicate-name search (reuse the proven IngredientController::index pattern):
```php
->whereHas('translations', fn ($t) => $t->when(
    DB::getDriverName() !== 'sqlite',
    fn ($w) => $w->whereFullText('name', $search),
    fn ($w) => $w->where('name', 'like', "%{$search}%"),
))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owner submit/withdraw — FormRequest, controller, duplicate-check, routes</name>
  <files>app/Http/Requests/Ingredients/SubmitIngredientRequest.php, app/Http/Controllers/Ingredients/IngredientSubmissionController.php, routes/web.php</files>
  <read_first>
    - routes/web.php (the file being modified — current ingredient route block, lines 33-40)
    - app/Http/Controllers/Ingredients/PrivateIngredientController.php (sibling controller — DB::transaction, redirect pattern, Gate::authorize usage)
    - app/Http/Controllers/Ingredients/IngredientController.php (the index() duplicate-search whereHas+whereFullText pattern to reuse)
    - app/Http/Requests/Ingredients/UpdateIngredientRequest.php (FormRequest authorize() pattern)
    - app/Http/Requests/Admin/VerifyIngredientRequest.php (FormRequest authorize via can())
    - app/Models/IngredientSubmission.php (07-01 — fillable + relations)
    - app/Policies/IngredientPolicy.php (07-01 — submit/withdraw abilities)
    - .planning/phases/07-ingredient-moderation/07-RESEARCH.md (Pattern 6 — duplicate warning strategy; Pitfall 4 — route ordering; Pitfall 6 — resubmission number)
  </read_first>
  <action>
    Create `app/Http/Requests/Ingredients/SubmitIngredientRequest.php` — namespace
    `App\Http\Requests\Ingredients`. `authorize()`:
    `return $this->user()->can('submit', $this->route('ingredient'));`
    `rules()`: `['confirmed_duplicates' => ['nullable', 'boolean']]` (the client
    sends this flag once the user has acknowledged the duplicate-warning list; the
    backend does not block on duplicates — the warning is advisory only).

    Create `app/Http/Controllers/Ingredients/IngredientSubmissionController.php`
    in namespace `App\Http\Controllers\Ingredients`. Three methods, all with
    explicit return types:

    - `duplicateCheck(Ingredient $ingredient): JsonResponse` — `Gate::authorize('submit', $ingredient)`. Search OFFICIAL ingredients only (`whereNull('user_id')`) by name using the `IngredientController::index` whereHas/whereFullText pattern with `$ingredient->name_cache` as the search term; exclude the ingredient itself with `where('id', '!=', $ingredient->id)`; `limit(5)`; eager-load `translations`. Return `response()->json(['matches' => $matches->map(fn ($m) => ['id' => $m->id, 'name' => $m->nameFor(app()->getLocale())])])`. This is the one endpoint that legitimately returns JSON (a fetch-only advisory check, not an Inertia mutation).

    - `store(SubmitIngredientRequest $request, Ingredient $ingredient): RedirectResponse` — wrap in `DB::transaction()`:
      1. Compute `$submissionNumber = IngredientSubmission::where('ingredient_id', $ingredient->id)->count() + 1;` (count EXISTING rows BEFORE inserting — Pitfall 6).
      2. Create an `IngredientSubmission` row: `ingredient_id`, `submitted_by => auth()->id()`, `status => SubmissionStatus::Submitted->value`, `submission_number`, `submitted_at => now()`.
      3. `$ingredient->update(['submission_status' => SubmissionStatus::Submitted]);`
      Return `redirect()->route('ingredients.show', $ingredient)->with('success', __('app.ingredients.submit_toast'));`

    - `destroy(Ingredient $ingredient): RedirectResponse` — withdraw. `Gate::authorize('withdraw', $ingredient);` then `DB::transaction()`:
      1. Update the open submission row (latest with `status = 'submitted'`) to `status => SubmissionStatus::Withdrawn->value`, `reviewed_at => now()`.
      2. `$ingredient->update(['submission_status' => SubmissionStatus::Private]);`
      Return `redirect()->route('ingredients.show', $ingredient)->with('success', __('app.ingredients.withdraw_toast'));`

    Edit `routes/web.php` — inside the `['auth','verified']` group, declare the
    submit/withdraw/duplicate-check routes BEFORE the `ingredients/{ingredient}`
    wildcard show route (line 40). Place them right after the
    `ingredients/{ingredient}/prices` line:
    ```php
    Route::get('ingredients/{ingredient}/duplicate-check', [IngredientSubmissionController::class, 'duplicateCheck'])->name('ingredients.duplicate-check');
    Route::post('ingredients/{ingredient}/submit', [IngredientSubmissionController::class, 'store'])->name('ingredients.submit');
    Route::delete('ingredients/{ingredient}/submit', [IngredientSubmissionController::class, 'destroy'])->name('ingredients.withdraw');
    ```
    Add the `use App\Http\Controllers\Ingredients\IngredientSubmissionController;`
    import at the top of `routes/web.php` (alphabetical with the other Ingredients imports).

    Run `vendor/bin/pint --dirty --format agent`. Then run
    `php artisan test --compact --filter=IngredientSubmissionTest` — the INGR-09
    submit/withdraw/freeze/resubmit tests must now pass.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=IngredientSubmissionTest 2>&1 | tail -12 && php artisan route:list --path=ingredients --except-vendor 2>&1 | grep -E "submit|duplicate-check"</automated>
  </verify>
  <acceptance_criteria>
    - `app/Http/Controllers/Ingredients/IngredientSubmissionController.php` exists; `grep "function store"`, `grep "function destroy"`, `grep "function duplicateCheck"` all match.
    - `app/Http/Requests/Ingredients/SubmitIngredientRequest.php` exists; `grep "can('submit'"` matches.
    - `php artisan route:list --path=ingredients` lists routes named `ingredients.submit`, `ingredients.withdraw`, `ingredients.duplicate-check`.
    - In `routes/web.php`, the line containing `ingredients/{ingredient}/submit` appears at a LOWER line number than the line for the `ingredients/{ingredient}` show route — verify with `grep -n`.
    - `grep "count() + 1"` matches in the controller (resubmission number computed from existing rows).
    - `php artisan test --compact --filter=IngredientSubmissionTest` — ALL tests in `IngredientSubmissionTest.php` PASS.
  </acceptance_criteria>
  <done>Owner can submit and withdraw; submit creates a numbered history row and freezes the ingredient; withdraw unfreezes it; duplicate-check endpoint returns up to 5 official matches; IngredientSubmissionTest is fully GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Moderator queue + review screen + approve/reject decision actions + notification</name>
  <files>app/Http/Requests/Admin/ApproveIngredientRequest.php, app/Http/Requests/Admin/RejectIngredientRequest.php, app/Http/Resources/IngredientSubmissionResource.php, app/Http/Controllers/Admin/IngredientReviewController.php, app/Http/Controllers/Admin/IngredientSubmissionController.php, app/Notifications/IngredientDecisionNotification.php, routes/web.php</files>
  <read_first>
    - routes/web.php (the file being modified — admin review group at lines 83-88)
    - app/Http/Controllers/Admin/IngredientReviewController.php (the stub being expanded)
    - app/Http/Controllers/Admin/IngredientVerificationController.php (the verified-write transaction pattern to mirror in approve)
    - app/Http/Requests/Admin/VerifyIngredientRequest.php (FormRequest authorize via can('review-ingredients') pattern)
    - app/Http/Resources/IngredientDetailResource.php (resource shape — review screen reuses this data shape)
    - app/Models/IngredientSubmission.php and app/Models/Ingredient.php (07-01 — relations, isPendingReview)
    - app/Enums/SubmissionStatus.php (07-01)
    - .planning/phases/07-ingredient-moderation/07-RESEARCH.md (Pattern 3 — convert-in-place transaction; Pattern 5 — notification class; Pattern 7 — completeness signal; Pitfall 1 — unique index; Pitfall 2 — notify after transaction)
    - .planning/phases/07-ingredient-moderation/07-UI-SPEC.md (queue row columns + review screen data sections)
  </read_first>
  <action>
    Create `app/Http/Requests/Admin/ApproveIngredientRequest.php` and
    `app/Http/Requests/Admin/RejectIngredientRequest.php`, namespace
    `App\Http\Requests\Admin`. Both `authorize()`:
    `return $this->user()->can('review-ingredients');`
    Approve `rules()`: `['notes' => ['nullable', 'string', 'max:1000']]`.
    Reject `rules()`: `['notes' => ['required', 'string', 'max:1000']]` (notes
    required on reject — locked CONTEXT decision).

    Create `app/Http/Resources/IngredientSubmissionResource.php` — a `JsonResource`
    used for queue rows AND the per-submission review payload. `toArray()` returns:
    - `id` (submission id), `submission_number`, `status`, `submitted_at` (ISO),
    - `submitter` => `['name' => $this->submittedBy?->name]`,
    - `ingredient` — the related ingredient's id, `name` (via `nameFor(locale)`), `category` (`['name' => ..., 'parent' => ...]`),
    - `completeness` => `['nutrition_filled' => $i->energy_kcal !== null && $i->protein_g !== null && $i->fat_g !== null && $i->carbs_g !== null, 'allergens_set' => $i->allergens->isNotEmpty(), 'conversions_added' => $i->conversions->isNotEmpty()]` (Pattern 7),
    - `prior_rejections` => the ingredient's prior `rejected`-status submissions mapped to `['notes' => ..., 'reviewed_at' => ISO, 'reviewer' => reviewedBy?->name]` in chronological order (for the resubmission context panel).
    Guard nested null relations.

    Expand `app/Http/Controllers/Admin/IngredientReviewController.php`:
    - `index(): Response` — query `IngredientSubmission::where('status', SubmissionStatus::Submitted->value)` eager-loading `submittedBy`, `ingredient.category`, `ingredient.allergens`, `ingredient.conversions`; order `orderBy('submitted_at')` (FIFO — oldest first). Render `Inertia::render('admin/ingredients', ['submissions' => IngredientSubmissionResource::collection($rows)->resolve()])`.
    - `show(IngredientSubmission $submission): Response` — the route permission middleware covers authorization. Eager-load `$submission->load(['submittedBy','reviewedBy','ingredient.translations','ingredient.allergens','ingredient.conversions.unit','ingredient.category.parent','ingredient.submissions.reviewedBy'])`. Render `Inertia::render('admin/ingredients/show', ['submission' => (new IngredientSubmissionResource($submission))->resolve(), 'ingredient' => (new IngredientDetailResource($submission->ingredient))->resolve()])`.

    Create `app/Notifications/IngredientDecisionNotification.php` via
    `php artisan make:notification IngredientDecisionNotification --no-interaction`.
    Constructor uses PHP 8 promotion with PRIMITIVE values only (not models — avoids
    queue serialization issues, RESEARCH note):
    `public function __construct(public readonly int $ingredientId, public readonly string $ingredientName, public readonly string $decision, public readonly ?string $notes) {}`
    `via()`: `return ['database'];`
    `toDatabase(object $notifiable): array`: return
    `['ingredient_id' => $this->ingredientId, 'ingredient_name' => $this->ingredientName, 'decision' => $this->decision, 'notes' => $this->notes]`.

    Create `app/Http/Controllers/Admin/IngredientSubmissionController.php` in
    namespace `App\Http\Controllers\Admin` (distinct from the Ingredients-namespace
    one in Task 1 of 07-02). Two methods:

    - `approve(ApproveIngredientRequest $request, IngredientSubmission $submission): RedirectResponse` —
      Inside `DB::transaction()` (Pattern 3):
      1. `$ingredient = $submission->ingredient;`
      2. `$ingredient->update(['user_id' => null, 'submission_status' => SubmissionStatus::Approved, 'verified' => true, 'verified_by' => auth()->id(), 'verified_at' => now()]);` — convert-in-place; approval IS verification (CONTEXT decision). The `unique(source, source_id)` index is unaffected — Pitfall 1.
      3. `$submission->update(['status' => SubmissionStatus::Approved->value, 'reviewed_by' => auth()->id(), 'reviewed_at' => now(), 'notes' => $request->validated('notes')]);`
      AFTER the transaction commits (Pitfall 2 — never notify inside the transaction):
      `$submission->submittedBy?->notify(new IngredientDecisionNotification($submission->ingredient->id, $submission->ingredient->name_cache, 'approved', $request->validated('notes')));`
      Return `redirect()->route('admin.ingredients.index')->with('success', __('app.ingredients.approve_toast'));`

    - `reject(RejectIngredientRequest $request, IngredientSubmission $submission): RedirectResponse` —
      Inside `DB::transaction()`:
      1. `$submission->ingredient->update(['submission_status' => SubmissionStatus::Rejected]);` — reverts to private (user_id UNCHANGED).
      2. `$submission->update(['status' => SubmissionStatus::Rejected->value, 'reviewed_by' => auth()->id(), 'reviewed_at' => now(), 'notes' => $request->validated('notes')]);`
      AFTER the transaction:
      `$submission->submittedBy?->notify(new IngredientDecisionNotification($submission->ingredient->id, $submission->ingredient->name_cache, 'rejected', $request->validated('notes')));`
      Return `redirect()->route('admin.ingredients.index')->with('success', __('app.ingredients.reject_toast'));`

    Edit `routes/web.php` — inside the existing
    `['auth','verified','permission:review-ingredients']` admin group (lines 83-88),
    add after the `ingredients` index route:
    ```php
    Route::get('ingredient-submissions/{submission}', [IngredientReviewController::class, 'show'])->name('ingredient-submissions.show');
    Route::post('ingredient-submissions/{submission}/approve', [AdminIngredientSubmissionController::class, 'approve'])->name('ingredient-submissions.approve');
    Route::post('ingredient-submissions/{submission}/reject', [AdminIngredientSubmissionController::class, 'reject'])->name('ingredient-submissions.reject');
    ```
    Use `{submission}` (the IngredientSubmission id) so the URL is tied to the
    moderation event, not the ingredient (RESEARCH Open Question 3). Because the
    Admin-namespace controller shares its class name with the Ingredients-namespace
    one from Task 1, import it with an alias:
    `use App\Http\Controllers\Admin\IngredientSubmissionController as AdminIngredientSubmissionController;`

    Run `vendor/bin/pint --dirty --format agent`, then
    `php artisan test --compact --filter=IngredientModerationTest` and
    `--filter=IngredientPromotionTest` — both suites must pass.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=IngredientModerationTest 2>&1 | tail -10 && php artisan test --compact --filter=IngredientPromotionTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/Http/Controllers/Admin/IngredientSubmissionController.php` exists; `grep "function approve"` and `grep "function reject"` match.
    - `app/Http/Controllers/Admin/IngredientReviewController.php` — `grep "function show"` matches; `grep "orderBy('submitted_at')"` matches (FIFO).
    - `app/Http/Requests/Admin/RejectIngredientRequest.php` — `grep "'required'"` matches in `rules()`.
    - `app/Http/Requests/Admin/ApproveIngredientRequest.php` — `grep "'nullable'"` matches in `rules()`.
    - `app/Notifications/IngredientDecisionNotification.php` — `grep "return \['database'\]"` matches.
    - In `approve()`, `grep "'user_id' => null"` and `grep "'verified' => true"` both match — convert-in-place + verification.
    - `notify(new IngredientDecisionNotification` appears AFTER the closing `});` of `DB::transaction` — confirm by line ordering with `grep -n`.
    - `php artisan route:list` lists `admin.ingredient-submissions.show`, `admin.ingredient-submissions.approve`, `admin.ingredient-submissions.reject`.
    - `php artisan test --compact --filter=IngredientModerationTest` and `--filter=IngredientPromotionTest` — ALL tests PASS.
  </acceptance_criteria>
  <done>Moderator queue is FIFO-ordered; review screen returns full ingredient data + prior-rejection context; approve promotes in place + verifies + notifies; reject reverts to private + notifies; reject requires notes; IngredientModeration and IngredientPromotion suites are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Shared Inertia props — pending review count + submitter notifications</name>
  <files>app/Http/Middleware/HandleInertiaRequests.php</files>
  <read_first>
    - app/Http/Middleware/HandleInertiaRequests.php (the file being modified — current share() array)
    - app/Models/Ingredient.php (07-01 — submission_status column)
    - app/Enums/SubmissionStatus.php (07-01)
    - app/Notifications/IngredientDecisionNotification.php (created in Task 2 — the notification type to filter on)
    - .planning/phases/07-ingredient-moderation/07-RESEARCH.md (Pattern 5 — shared props; Pitfall 3 — gate the count query)
  </read_first>
  <action>
    Edit `app/Http/Middleware/HandleInertiaRequests.php`. Add two new entries to
    the array returned by `share()`, after `'sidebarOpen'`. Add imports
    `use App\Enums\SubmissionStatus;`, `use App\Models\Ingredient;`,
    `use App\Notifications\IngredientDecisionNotification;`.

    Assign `$user = $request->user();` at the top of `share()` and reuse it for the
    existing `auth` entry as well (refactor the two `$request->user()` calls to use
    `$user`).

    Add:
    ```php
    'pendingIngredientReviewCount' => $user && $user->can('review-ingredients')
        ? Ingredient::where('submission_status', SubmissionStatus::Submitted->value)->count()
        : null,
    'ingredientNotifications' => $user
        ? $user->unreadNotifications()
            ->where('type', IngredientDecisionNotification::class)
            ->latest()
            ->take(5)
            ->get(['id', 'data', 'created_at'])
            ->map(fn ($n) => [
                'id' => $n->id,
                'data' => $n->data,
                'created_at' => $n->created_at?->toISOString(),
            ])
            ->values()
        : null,
    ```
    The count query is GATED behind `review-ingredients` so it never runs for
    regular users or guests (Pitfall 3) — returns `null`, which the frontend reads
    as "hide the badge". `ingredientNotifications` runs for any authenticated user.

    Run `vendor/bin/pint --dirty --format agent`. Then run the full Ingredients
    feature suite to confirm nothing regressed:
    `php artisan test --compact tests/Feature/Ingredients/`.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/Ingredients/ 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pendingIngredientReviewCount" app/Http/Middleware/HandleInertiaRequests.php` matches.
    - `grep "ingredientNotifications" app/Http/Middleware/HandleInertiaRequests.php` matches.
    - `grep "can('review-ingredients')" app/Http/Middleware/HandleInertiaRequests.php` matches (count query is permission-gated).
    - `grep "unreadNotifications" app/Http/Middleware/HandleInertiaRequests.php` matches.
    - `php artisan test --compact tests/Feature/Ingredients/` — ALL tests PASS (the full 07-01 RED suite is now GREEN).
    - `vendor/bin/pint --test --format agent` reports no issues on `HandleInertiaRequests.php`.
  </acceptance_criteria>
  <done>Every authenticated request carries `pendingIngredientReviewCount` (moderators only) and `ingredientNotifications` (up to 5 unread decision notifications) as shared Inertia props; the full Ingredients feature suite is GREEN.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact tests/Feature/Ingredients/` — the entire 07-01 RED suite (≥15 tests) is GREEN.
- `php artisan route:list` lists all submit/withdraw/duplicate-check (owner) and queue/review/approve/reject (admin) routes.
- `vendor/bin/pint --test --format agent` reports no style issues on changed PHP files.
- Approve flows are atomic (single DB::transaction) and notify AFTER commit.
</verification>

<success_criteria>
- INGR-09 backend complete: submit + withdraw + duplicate-check, frozen-while-pending enforced.
- INGR-10 backend complete: FIFO review queue + per-submission review data + approve/reject (reject requires notes).
- INGR-11 backend complete: convert-in-place promotion (user_id null + verified) + revert-on-reject + database notification.
- Shared props expose the pending-count badge source and submitter notifications.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingredient-moderation/07-02-SUMMARY.md`
</output>
