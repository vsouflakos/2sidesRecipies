---
phase: 07-ingredient-moderation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/SubmissionStatus.php
  - app/Models/IngredientSubmission.php
  - app/Models/Ingredient.php
  - app/Policies/IngredientPolicy.php
  - database/migrations/2026_05_18_000002_create_notifications_table.php
  - database/migrations/2026_05_18_000003_create_ingredient_submissions_table.php
  - database/migrations/2026_05_18_000004_add_submission_status_to_ingredients_table.php
  - database/factories/IngredientFactory.php
  - database/factories/IngredientSubmissionFactory.php
  - resources/js/pages/admin/ingredients/show.tsx
  - tests/Feature/Ingredients/IngredientSubmissionTest.php
  - tests/Feature/Ingredients/IngredientModerationTest.php
  - tests/Feature/Ingredients/IngredientPromotionTest.php
autonomous: true
requirements: [INGR-09, INGR-10, INGR-11]
must_haves:
  truths:
    - "An ingredient carries a submission_status of private | submitted | approved | rejected"
    - "Every submission attempt and moderator decision is recorded as an immutable history row"
    - "A private ingredient under review (submitted) cannot be edited or deleted by its owner"
    - "The RED test suite for INGR-09/10/11 exists and fails for the right reasons (routes/controllers absent)"
  artifacts:
    - path: "app/Enums/SubmissionStatus.php"
      provides: "Four-case backed enum (private|submitted|approved|rejected|withdrawn)"
      contains: "enum SubmissionStatus"
    - path: "app/Models/IngredientSubmission.php"
      provides: "Eloquent model for ingredient_submissions table"
      contains: "class IngredientSubmission"
    - path: "database/migrations/2026_05_18_000003_create_ingredient_submissions_table.php"
      provides: "Submission history table schema"
      contains: "ingredient_submissions"
    - path: "database/migrations/2026_05_18_000004_add_submission_status_to_ingredients_table.php"
      provides: "submission_status column on ingredients"
      contains: "submission_status"
    - path: "app/Policies/IngredientPolicy.php"
      provides: "submit/withdraw abilities + frozen-while-pending guard on update/delete"
      contains: "public function submit"
  key_links:
    - from: "app/Models/Ingredient.php"
      to: "app/Enums/SubmissionStatus.php"
      via: "casts() entry submission_status => SubmissionStatus::class"
      pattern: "submission_status.*SubmissionStatus"
    - from: "app/Policies/IngredientPolicy.php"
      to: "app/Models/Ingredient.php isPendingReview()"
      via: "frozen-while-pending guard"
      pattern: "isPendingReview"
---

<objective>
Lay the schema and test foundation for the ingredient moderation workflow: the
`SubmissionStatus` enum, the `ingredient_submissions` history table, the
`submission_status` column on `ingredients`, the Laravel `notifications` table,
the `IngredientSubmission` model, factory states, the policy abilities
(submit/withdraw) and the frozen-while-pending guard, plus a Wave 0 RED test
suite covering INGR-09 / INGR-10 / INGR-11.

Purpose: Every downstream plan (backend in 07-02, frontend in 07-03) depends on
this schema and these contracts existing. The RED tests give 07-02 a concrete
GREEN target.
Output: 3 migrations, 1 enum, 1 model, 2 factory updates, 1 policy update, 3
RED test files, 1 stub Inertia page.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ingredient-moderation/07-CONTEXT.md
@.planning/phases/07-ingredient-moderation/07-RESEARCH.md
@.planning/phases/07-ingredient-moderation/07-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor must respect. Do NOT re-explore. -->

From app/Models/Ingredient.php (Phase 2):
- `protected $fillable` array — add `'submission_status'` to it
- `protected function casts(): array` — add `'submission_status' => SubmissionStatus::class`
- Helpers: `isOfficial(): bool` (user_id === null), `isPrivate(): bool` (user_id !== null)
- Relations: `category()`, `user()`, `verifiedBy()`, `translations()`, `conversions()`, `prices()`, `allergens()`, `nutrients()`
- `verified` boolean, `verified_by`, `verified_at` columns exist (Phase 2)
- `source = 'user'`, `source_id = UUID` for private ingredients; `unique(['source','source_id'])` index

From app/Policies/IngredientPolicy.php (current — only update + delete):
```php
public function update(User $user, Ingredient $ingredient): bool
{
    return $ingredient->user_id !== null && $ingredient->user_id === $user->id;
}
public function delete(User $user, Ingredient $ingredient): bool
{
    return $ingredient->user_id !== null && $ingredient->user_id === $user->id;
}
```

From database/factories/IngredientFactory.php (existing states):
- `definition()` — user_id null, source 'user', source_id uuid, verified false
- `private(User $user): static` — sets user_id, source 'user'
- `verified(User $verifier): static` — sets verified true, verified_by, verified_at

From database/migrations/2026_05_16_140144_create_ingredients_table.php:
- `unique(['source', 'source_id'])` — adding a column does NOT affect this index

Route group (routes/web.php line 83) the queue lives in:
```php
Route::middleware(['auth', 'verified', 'permission:review-ingredients'])
    ->prefix('admin')->name('admin.')->group(function () {
        Route::get('ingredients', [IngredientReviewController::class, 'index'])->name('ingredients.index');
    });
```
`review-ingredients` permission is granted to Moderator + Admin (RolesAndPermissionsSeeder).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SubmissionStatus enum, three migrations, IngredientSubmission model, Ingredient + factory updates</name>
  <files>app/Enums/SubmissionStatus.php, app/Models/IngredientSubmission.php, app/Models/Ingredient.php, database/migrations/2026_05_18_000002_create_notifications_table.php, database/migrations/2026_05_18_000003_create_ingredient_submissions_table.php, database/migrations/2026_05_18_000004_add_submission_status_to_ingredients_table.php, database/factories/IngredientSubmissionFactory.php, database/factories/IngredientFactory.php</files>
  <read_first>
    - app/Models/Ingredient.php (fillable + casts arrays must be edited, not rewritten)
    - app/Enums/Difficulty.php (existing backed-enum convention to mirror)
    - app/Enums/TestType.php (existing backed-enum convention to mirror)
    - database/migrations/2026_05_16_140144_create_ingredients_table.php (ingredients schema + the source/source_id unique index)
    - database/migrations/2026_05_17_000011_create_recipe_tests_table.php (recent migration style, FK + index conventions)
    - database/factories/IngredientFactory.php (existing factory states to extend)
    - .planning/phases/07-ingredient-moderation/07-RESEARCH.md (Pattern 1 + Pattern 2 — enum + submissions table shape)
  </read_first>
  <action>
    Create `app/Enums/SubmissionStatus.php` — a `string`-backed PHP 8.3 enum in
    namespace `App\Enums`, mirroring `Difficulty.php` style:
    ```php
    enum SubmissionStatus: string
    {
        case Private   = 'private';
        case Submitted = 'submitted';
        case Approved  = 'approved';
        case Rejected  = 'rejected';
        case Withdrawn = 'withdrawn';
    }
    ```
    NOTE: `Private` is allowed as an enum case name in PHP — keep it exactly `Private`.

    Generate the notifications table migration via Artisan (do NOT hand-write it):
    `php artisan notifications:table --no-interaction`. This creates a migration
    matching `database/migrations/*_create_notifications_table.php` with columns
    `id` (uuid PK), `type`, `notifiable` (morphs), `data` (text), `read_at`
    (nullable timestamp), `created_at`/`updated_at`. If a notifications migration
    already exists, skip generation.

    Create migration `create_ingredient_submissions_table` via
    `php artisan make:migration create_ingredient_submissions_table --no-interaction`.
    Schema (use `2026_05_18_000003` ordering — rename the generated file if needed):
    - `id()` — bigint PK
    - `foreignId('ingredient_id')->constrained()->cascadeOnDelete()`
    - `foreignId('submitted_by')->constrained('users')->nullOnDelete()`
    - `foreignId('reviewed_by')->nullable()->constrained('users')->nullOnDelete()`
    - `string('status')` — stores SubmissionStatus value (submitted|approved|rejected|withdrawn)
    - `text('notes')->nullable()` — moderator decision note
    - `unsignedTinyInteger('submission_number')->default(1)`
    - `timestamp('submitted_at')`
    - `timestamp('reviewed_at')->nullable()`
    - `timestamps()`
    - `$table->index(['ingredient_id', 'status'])`
    - `$table->index(['status', 'submitted_at'])` — supports FIFO queue ordering

    Create migration `add_submission_status_to_ingredients_table` via
    `php artisan make:migration add_submission_status_to_ingredients_table --table=ingredients --no-interaction`
    (use `2026_05_18_000004` ordering). In `up()`:
    `$table->string('submission_status')->default('private')->after('verified_at');`
    In `down()`: `$table->dropColumn('submission_status');`.

    Create `app/Models/IngredientSubmission.php` via
    `php artisan make:model IngredientSubmission --no-interaction` then edit:
    - `$fillable`: `['ingredient_id','submitted_by','reviewed_by','status','notes','submission_number','submitted_at','reviewed_at']`
    - `casts()`: `'status' => SubmissionStatus::class`, `'submitted_at' => 'datetime'`, `'reviewed_at' => 'datetime'`
    - Relations: `ingredient(): BelongsTo` → `Ingredient`; `submittedBy(): BelongsTo` → `User::class, 'submitted_by'`; `reviewedBy(): BelongsTo` → `User::class, 'reviewed_by'`
    - Use explicit return type declarations on every method.

    Edit `app/Models/Ingredient.php`:
    - Add `'submission_status'` to the `$fillable` array (after `'verified_at'`).
    - Add `'submission_status' => SubmissionStatus::class` to the `casts()` return array.
    - Add `use App\Enums\SubmissionStatus;` import.
    - Add relation `public function submissions(): HasMany { return $this->hasMany(IngredientSubmission::class); }`
    - Add helper `public function isPendingReview(): bool { return $this->submission_status === SubmissionStatus::Submitted; }`
    - Add helper `public function latestSubmission(): HasOne { return $this->hasOne(IngredientSubmission::class)->latestOfMany(); }` (import `HasOne`).

    Create `database/factories/IngredientSubmissionFactory.php` via
    `php artisan make:factory IngredientSubmissionFactory --no-interaction` with a
    `definition()` returning: `ingredient_id` => `Ingredient::factory()`,
    `submitted_by` => `User::factory()`, `reviewed_by` => null, `status` =>
    `SubmissionStatus::Submitted->value`, `notes` => null, `submission_number` => 1,
    `submitted_at` => `now()`, `reviewed_at` => null.

    Edit `database/factories/IngredientFactory.php` — add two states after `verified()`:
    ```php
    public function submitted(): static
    {
        return $this->state(fn (array $attributes) => [
            'submission_status' => SubmissionStatus::Submitted->value,
        ]);
    }
    public function rejected(): static
    {
        return $this->state(fn (array $attributes) => [
            'submission_status' => SubmissionStatus::Rejected->value,
        ]);
    }
    ```
    Add `use App\Enums\SubmissionStatus;` import to the factory.

    Run `php artisan migrate --no-interaction` and then
    `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed --no-interaction && php -r "require 'vendor/autoload.php'; var_dump(enum_exists('App\\Enums\\SubmissionStatus'));"</automated>
  </verify>
  <acceptance_criteria>
    - `app/Enums/SubmissionStatus.php` exists; `grep "enum SubmissionStatus: string"` matches; cases `Private`, `Submitted`, `Approved`, `Rejected`, `Withdrawn` all present.
    - `php artisan migrate:fresh --no-interaction` runs with no errors.
    - `database/migrations/*_create_ingredient_submissions_table.php` exists; `grep "submission_number"` and `grep "cascadeOnDelete"` both match.
    - `database/migrations/*_add_submission_status_to_ingredients_table.php` exists; `grep "submission_status"` matches; default is `'private'`.
    - A `notifications` migration exists; `grep -rl "create_notifications_table" database/migrations/` returns a file.
    - `app/Models/IngredientSubmission.php` exists; `grep "class IngredientSubmission"` matches; `grep "SubmissionStatus::class"` matches in its casts.
    - `app/Models/Ingredient.php` — `grep "submission_status' => SubmissionStatus::class"` matches; `grep "public function isPendingReview"` matches; `grep "public function submissions"` matches.
    - `database/factories/IngredientFactory.php` — `grep "public function submitted"` and `grep "public function rejected"` both match.
    - `vendor/bin/pint --test --format agent` reports no issues on changed PHP files.
  </acceptance_criteria>
  <done>The enum, three migrations, IngredientSubmission model, factory states, and Ingredient model updates all exist; `migrate:fresh` succeeds; the submission_status column casts to the enum.</done>
</task>

<task type="auto">
  <name>Task 2: IngredientPolicy submit/withdraw abilities + frozen-while-pending guard</name>
  <files>app/Policies/IngredientPolicy.php</files>
  <read_first>
    - app/Policies/IngredientPolicy.php (the file being modified — current update/delete methods)
    - app/Models/Ingredient.php (isPendingReview() added in Task 1; submission_status cast)
    - app/Enums/SubmissionStatus.php (created in Task 1)
    - app/Policies/RecipePolicy.php (sibling policy conventions — return types, nullable User handling)
    - .planning/phases/07-ingredient-moderation/07-RESEARCH.md (Pattern 4 — frozen-while-pending policy code)
  </read_first>
  <action>
    Edit `app/Policies/IngredientPolicy.php`. Add `use App\Enums\SubmissionStatus;`.

    Modify `update()` to block edits while pending — the frozen-while-pending rule:
    ```php
    public function update(User $user, Ingredient $ingredient): bool
    {
        if ($ingredient->isPendingReview()) {
            return false;
        }

        return $ingredient->user_id !== null && $ingredient->user_id === $user->id;
    }
    ```

    Modify `delete()` the same way (cannot delete an ingredient under review):
    ```php
    public function delete(User $user, Ingredient $ingredient): bool
    {
        if ($ingredient->isPendingReview()) {
            return false;
        }

        return $ingredient->user_id !== null && $ingredient->user_id === $user->id;
    }
    ```

    Add a `submit()` ability — only the owner of a private ingredient currently in
    `private` OR `rejected` state may submit/resubmit. Use explicit parenthesisation
    (the RESEARCH draft had a precedence bug — fix it here):
    ```php
    public function submit(User $user, Ingredient $ingredient): bool
    {
        if ($ingredient->user_id === null || $ingredient->user_id !== $user->id) {
            return false;
        }

        return in_array(
            $ingredient->submission_status,
            [SubmissionStatus::Private, SubmissionStatus::Rejected],
            true,
        );
    }
    ```

    Add a `withdraw()` ability — only the owner, only while pending review:
    ```php
    public function withdraw(User $user, Ingredient $ingredient): bool
    {
        return $ingredient->user_id !== null
            && $ingredient->user_id === $user->id
            && $ingredient->isPendingReview();
    }
    ```

    Keep all methods with explicit `bool` return types and PHPDoc blocks consistent
    with the existing two methods. Run `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php -r "require 'vendor/autoload.php'; \$r = new ReflectionClass('App\\Policies\\IngredientPolicy'); echo implode(',', array_map(fn(\$m) => \$m->name, \$r->getMethods())), PHP_EOL;"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public function submit"` in `app/Policies/IngredientPolicy.php` matches.
    - `grep "public function withdraw"` in `app/Policies/IngredientPolicy.php` matches.
    - `grep "isPendingReview"` appears in both `update()` and `delete()` bodies (at least 2 matches across the file).
    - `grep "use App\\\\Enums\\\\SubmissionStatus"` matches.
    - The PHP reflection command lists all four methods: `update,delete,submit,withdraw`.
    - `submit()` uses `in_array(..., [SubmissionStatus::Private, SubmissionStatus::Rejected], true)` — confirm with `grep "SubmissionStatus::Rejected"`.
  </acceptance_criteria>
  <done>IngredientPolicy exposes submit + withdraw abilities; update + delete return false while the ingredient is in submitted state; owner-only checks intact.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 RED test suite (INGR-09/10/11) + admin review stub page</name>
  <files>tests/Feature/Ingredients/IngredientSubmissionTest.php, tests/Feature/Ingredients/IngredientModerationTest.php, tests/Feature/Ingredients/IngredientPromotionTest.php, resources/js/pages/admin/ingredients/show.tsx</files>
  <read_first>
    - tests/Feature/Ingredients/ImportUsdaTest.php (existing Pest test style for the Ingredients feature dir)
    - tests/Feature/Recipes (any RecipePublish/test file — for Inertia assertInertia + permission-gate test patterns)
    - app/Policies/IngredientPolicy.php (the abilities tests must exercise)
    - database/factories/IngredientFactory.php (private/submitted/rejected states from Task 1)
    - database/factories/IngredientSubmissionFactory.php (created in Task 1)
    - resources/js/pages/admin/ingredients.tsx (existing stub page — model the new show.tsx stub on it)
    - .planning/phases/07-ingredient-moderation/07-RESEARCH.md (Phase Requirements → Test Map table — exact behaviors to assert)
    - .planning/phases/07-ingredient-moderation/07-VALIDATION.md (sampling + Wave 0 requirements)
  </read_first>
  <action>
    Create three Pest feature test files. These are RED tests — they MUST contain
    real assertions (no `skip()`, no `markTestIncomplete()`). They will fail
    because routes/controllers do not exist yet (07-02 makes them GREEN). Use
    `RefreshDatabase`. Build users with roles via the existing role seeder pattern
    (`User::factory()` then `assignRole('Moderator')` / `assignRole('User')` — the
    `Moderator` role holds `review-ingredients`).

    Create `tests/Feature/Ingredients/IngredientSubmissionTest.php` (INGR-09) with
    tests asserting:
    - Owner submitting a private ingredient: POST `route('ingredients.submit', $ingredient)` → redirect; `$ingredient->fresh()->submission_status === SubmissionStatus::Submitted`; an `ingredient_submissions` row exists with `status = 'submitted'`, `submitted_by = owner->id`, `submission_number = 1`.
    - Non-owner cannot submit another user's private ingredient → POST returns 403.
    - Owner cannot submit an already-submitted ingredient (factory `submitted()` state) → POST returns 403.
    - Frozen-while-pending: owner cannot edit a submitted ingredient — PUT `route('ingredients.update', $ingredient)` returns 403 when `submission_status` is `submitted`.
    - Owner can withdraw a pending submission: DELETE `route('ingredients.withdraw', $ingredient)` → redirect; `submission_status` reverts to `private`; the open submission row is recorded as `withdrawn` (assert the ingredient is unfrozen and editable again).
    - Resubmission after rejection: starting from factory `rejected()` state with one existing `IngredientSubmission` row, POST submit again → new submission row has `submission_number = 2` and status `submitted`; ingredient `submission_status` is `submitted`.

    Create `tests/Feature/Ingredients/IngredientModerationTest.php` (INGR-10) with
    tests asserting:
    - Moderator can GET `route('admin.ingredients.index')` → 200 and `assertInertia` renders `admin/ingredients` with a `submissions` (queue) prop that is an array.
    - Queue items are FIFO ordered — create two submitted ingredients with distinct `submitted_at`; assert the older one appears first in the `submissions` prop.
    - Moderator can approve: POST `route('admin.ingredient-submissions.approve', $submission)` with optional `notes` → redirect; submission `status = 'approved'`, `reviewed_by` set.
    - Moderator cannot reject without notes: POST `route('admin.ingredient-submissions.reject', $submission)` with no `notes` → 422.
    - Moderator can reject with notes: POST reject with `notes` → redirect; submission `status = 'rejected'`, `notes` persisted.
    - Plain User (role `User`, no `review-ingredients`) GET `route('admin.ingredients.index')` → 403.

    Create `tests/Feature/Ingredients/IngredientPromotionTest.php` (INGR-11) with
    tests asserting:
    - Approval promotes in place: after approve, the ingredient `user_id` is null, `submission_status === SubmissionStatus::Approved`, `verified === true`, `verified_by === moderator->id`, `verified_at` not null.
    - Promoted ingredient is visible to a different user — GET `route('ingredients.show', $ingredient)` as another user returns 200 (not 404).
    - Submitter's recipe referencing the ingredient still resolves after promotion (create a recipe line / draft referencing the ingredient id; assert the id is unchanged so existing references hold).
    - Rejection reverts: after reject, ingredient `submission_status === SubmissionStatus::Rejected`, `user_id` unchanged (still the submitter), `verified` still false.
    - Notification on decision: wrap with `Notification::fake()`; after approve assert `Notification::assertSentTo($submitter, IngredientDecisionNotification::class)`; separately after reject assert the same.

    Create stub Inertia page `resources/js/pages/admin/ingredients/show.tsx` —
    minimal default-export React component (model it on the existing stub
    `resources/js/pages/admin/ingredients.tsx`). It only needs to exist so
    `assertInertia()` can resolve it in the Vite manifest; 07-03 builds the real page.

    Run `php artisan test --compact tests/Feature/Ingredients/` and confirm the new
    tests are RED for the right reason (route/controller missing — NOT parse
    errors, NOT missing-column errors). If any test errors on a missing column or
    enum, that is a Task 1 defect — fix Task 1, do not weaken the test.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/Ingredients/IngredientSubmissionTest.php tests/Feature/Ingredients/IngredientModerationTest.php tests/Feature/Ingredients/IngredientPromotionTest.php 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist: `tests/Feature/Ingredients/IngredientSubmissionTest.php`, `IngredientModerationTest.php`, `IngredientPromotionTest.php`.
    - `grep -c "skip\|markTestIncomplete" tests/Feature/Ingredients/IngredientSubmissionTest.php` returns 0 (no skipped tests).
    - `php artisan test` of the three files runs WITHOUT PHP parse/fatal errors — failures are assertion/route failures only (RED for the right reason).
    - `grep "Notification::fake"` matches in `IngredientPromotionTest.php`.
    - `grep "ingredients.submit"` matches in `IngredientSubmissionTest.php`; `grep "admin.ingredient-submissions"` matches in `IngredientModerationTest.php`.
    - `resources/js/pages/admin/ingredients/show.tsx` exists and has a default export (`grep "export default"`).
    - Test output shows at least 15 tests collected across the three files.
  </acceptance_criteria>
  <done>Three RED Pest test files cover all 15 behaviors from the RESEARCH test map; they fail only because 07-02 routes/controllers are absent; the admin review stub page exists for Vite manifest resolution.</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh --seed --no-interaction` succeeds with no errors.
- `php artisan test --compact tests/Feature/Ingredients/` runs cleanly (RED tests fail on missing routes, not on schema/parse errors).
- `vendor/bin/pint --test --format agent` reports no style issues on changed PHP files.
- The `SubmissionStatus` enum, `IngredientSubmission` model, and `submission_status` column all exist and are usable.
</verification>

<success_criteria>
- Schema foundation complete: enum, submissions table, status column, notifications table all migrated.
- IngredientPolicy exposes submit + withdraw and enforces frozen-while-pending on update + delete.
- Factory states `submitted()` / `rejected()` and the `IngredientSubmissionFactory` exist for downstream tests.
- A RED test suite of ≥15 tests covers INGR-09/10/11 and fails only because 07-02 work is not yet done.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingredient-moderation/07-01-SUMMARY.md`
</output>
