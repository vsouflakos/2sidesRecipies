---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - resources/css/app.css
  - routes/web.php
  - app/Http/Controllers/Dev/StyleguideController.php
  - resources/js/pages/dev/styleguide.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Every page renders with the warm-minimal palette in both light and dark themes"
    - "The dev-only styleguide page renders at /dev/styleguide in the local environment"
    - "The styleguide visually proves the token set and shadcn component look"
  artifacts:
    - path: "resources/css/app.css"
      provides: "Warm-minimal OKLCH token values for :root and .dark"
      contains: "oklch(0.981 0.007 60)"
    - path: "resources/js/pages/dev/styleguide.tsx"
      provides: "Component + token showcase page"
      min_lines: 60
    - path: "app/Http/Controllers/Dev/StyleguideController.php"
      provides: "Styleguide route handler"
  key_links:
    - from: "routes/web.php"
      to: "StyleguideController"
      via: "local-env-gated /dev/styleguide route"
      pattern: "dev/styleguide"
---

<objective>
Replace the neutral OKLCH palette in `resources/css/app.css` with the APPROVED warm-minimal token set (warm off-white background, soft taupe accent) for both `:root` and `.dark`, and build a dev-only styleguide page at `/dev/styleguide` that renders every token swatch and shadcn component in both themes as the design-system verification artifact.

Purpose: UI-01 (shadcn/ui on Tailwind v4), UI-02 (warm-minimal aesthetic across light + dark), UI-03 (responsive). The `@theme` mapping block in app.css is unchanged — only the CSS custom property values change, so the whole app (dashboard, settings, auth) inherits the new look automatically.

Output: Warm-minimal token set in app.css, StyleguideController, styleguide.tsx showcase page, local-only route.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing assets. -->
resources/js/hooks/use-appearance.tsx — light/dark/system theme hook, persists via cookie.
resources/js/pages/dashboard.tsx — existing Inertia page pattern (layout import, props).
resources/css/app.css — already uses OKLCH custom properties in :root + .dark, with a Tailwind v4 @theme mapping block.
Existing shadcn primitives in resources/js/components/ui/: button, badge, card, dialog, dropdown-menu,
  input, label, select, separator, skeleton, sonner, spinner, toggle, tooltip, checkbox, avatar.
Plan 04 adds: table, pagination (this plan may reference them in the styleguide; if absent at run time,
  omit the Data Display section gracefully — do not block on them).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the OKLCH palette in app.css with the warm-minimal token set</name>
  <read_first>
    - resources/css/app.css (current :root and .dark blocks + the @theme mapping block — read the whole file)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Color section — full Token Map for light + dark; the authoritative values)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 9 — OKLCH warm token rationale)
  </read_first>
  <action>
    Edit `resources/css/app.css`. Replace ONLY the CSS custom property VALUES inside the `:root` block and
    the `.dark` block. Do NOT touch the `@theme` mapping block, `@import` lines, `@layer` blocks, or any
    Tailwind directives — the variable-to-utility mapping is already correct.

    Set the `:root` block tokens to these exact values (from 01-UI-SPEC Color → Token Map Light Mode):

    ```css
    --background: oklch(0.981 0.007 60);
    --foreground: oklch(0.168 0.010 50);
    --card: oklch(0.981 0.007 60);
    --card-foreground: oklch(0.168 0.010 50);
    --popover: oklch(0.981 0.007 60);
    --popover-foreground: oklch(0.168 0.010 50);
    --primary: oklch(0.210 0.010 50);
    --primary-foreground: oklch(0.981 0.007 60);
    --secondary: oklch(0.948 0.010 60);
    --secondary-foreground: oklch(0.210 0.010 50);
    --muted: oklch(0.948 0.010 60);
    --muted-foreground: oklch(0.530 0.018 55);
    --accent: oklch(0.550 0.035 60);
    --accent-foreground: oklch(0.981 0.007 60);
    --destructive: oklch(0.577 0.245 27.325);
    --destructive-foreground: oklch(0.577 0.245 27.325);
    --border: oklch(0.900 0.012 58);
    --input: oklch(0.900 0.012 58);
    --ring: oklch(0.820 0.018 58);
    --radius: 0.5rem;
    --sidebar: oklch(0.970 0.009 60);
    --sidebar-foreground: oklch(0.168 0.010 50);
    --sidebar-primary: oklch(0.210 0.010 50);
    --sidebar-primary-foreground: oklch(0.981 0.007 60);
    --sidebar-accent: oklch(0.920 0.015 58);
    --sidebar-accent-foreground: oklch(0.210 0.010 50);
    --sidebar-border: oklch(0.900 0.012 58);
    --sidebar-ring: oklch(0.820 0.018 58);
    ```

    Set the `.dark` block tokens to these exact values (from 01-UI-SPEC Color → Token Map Dark Mode):

    ```css
    --background: oklch(0.148 0.008 50);
    --foreground: oklch(0.940 0.008 60);
    --card: oklch(0.148 0.008 50);
    --card-foreground: oklch(0.940 0.008 60);
    --popover: oklch(0.148 0.008 50);
    --popover-foreground: oklch(0.940 0.008 60);
    --primary: oklch(0.940 0.008 60);
    --primary-foreground: oklch(0.210 0.010 50);
    --secondary: oklch(0.225 0.010 52);
    --secondary-foreground: oklch(0.940 0.008 60);
    --muted: oklch(0.225 0.010 52);
    --muted-foreground: oklch(0.650 0.015 56);
    --accent: oklch(0.550 0.035 60);
    --accent-foreground: oklch(0.940 0.008 60);
    --destructive: oklch(0.396 0.141 25.723);
    --destructive-foreground: oklch(0.637 0.237 25.331);
    --border: oklch(0.235 0.010 52);
    --input: oklch(0.235 0.010 52);
    --ring: oklch(0.395 0.015 54);
    --sidebar: oklch(0.188 0.008 50);
    --sidebar-foreground: oklch(0.940 0.008 60);
    --sidebar-primary: oklch(0.940 0.008 60);
    --sidebar-primary-foreground: oklch(0.940 0.008 60);
    --sidebar-accent: oklch(0.255 0.010 52);
    --sidebar-accent-foreground: oklch(0.940 0.008 60);
    --sidebar-border: oklch(0.235 0.010 52);
    --sidebar-ring: oklch(0.395 0.015 54);
    ```

    If `:root` or `.dark` declares a token NOT listed above (e.g. chart colors), leave it unchanged.
    If a token listed above is missing from the current file, add it inside the correct block.

    Run `npm run build` to confirm the CSS compiles without error.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "built in|error" && Select-String -Path resources/css/app.css -Pattern "oklch\(0.981 0.007 60\)" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `resources/css/app.css` `:root` block contains `--background: oklch(0.981 0.007 60);` and `--accent: oklch(0.550 0.035 60);`
    - `resources/css/app.css` `:root` block contains `--radius: 0.5rem;`
    - `resources/css/app.css` `.dark` block contains `--background: oklch(0.148 0.008 50);`
    - The `@theme` mapping block in app.css is unchanged (still maps the same variable names to utilities)
    - `npm run build` completes without a CSS compilation error
  </acceptance_criteria>
  <done>The warm-minimal palette is applied in both :root and .dark; the whole app inherits it via the unchanged @theme mapping; the build compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dev-only styleguide route and showcase page</name>
  <read_first>
    - routes/web.php (current routes — where to add the dev route group)
    - app/Http/Controllers/Settings/ProfileController.php (thin controller / Inertia::render pattern)
    - resources/js/pages/dashboard.tsx (Inertia page pattern — layout import, head)
    - resources/js/hooks/use-appearance.tsx (the theme hook — used by the styleguide light/dark toggle)
    - resources/js/components/ui/button.tsx, badge.tsx, input.tsx, select.tsx, dialog.tsx, tooltip.tsx, separator.tsx, skeleton.tsx, toggle.tsx, checkbox.tsx (the primitives the gallery showcases)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Surface 6 — full styleguide section list; Spacing Scale; Typography table)
    - .planning/phases/01-foundation/01-RESEARCH.md (Open Question 2 — dev-only route protection via app()->isLocal())
  </read_first>
  <action>
    Create the controller via `php artisan make:controller Dev/StyleguideController --no-interaction`:

    ```php
    <?php

    namespace App\Http\Controllers\Dev;

    use App\Http\Controllers\Controller;
    use Inertia\Inertia;
    use Inertia\Response;

    class StyleguideController extends Controller
    {
        public function index(): Response
        {
            return Inertia::render('dev/styleguide');
        }
    }
    ```

    Edit `routes/web.php`. Add a dev-only route gated to the local environment so it never ships in
    production (RESEARCH Open Question 2):

    ```php
    if (app()->isLocal()) {
        Route::middleware('auth')
            ->get('dev/styleguide', [\App\Http\Controllers\Dev\StyleguideController::class, 'index'])
            ->name('dev.styleguide');
    }
    ```

    Create `resources/js/pages/dev/styleguide.tsx` — the showcase page. Use the same app layout as
    `dashboard.tsx`. Build the sections in this order, per 01-UI-SPEC Surface 6:

    1. Top-right corner: a "Light / Dark" `Toggle` that flips the page theme using the `useAppearance` hook.
    2. **Color Palette** — a responsive grid of swatch cards (responsive: `grid-cols-2 md:grid-cols-3
       lg:grid-cols-4`). One card per CSS variable: a 64px-tall color preview block (background set via the
       Tailwind utility for that token, e.g. `bg-background`, `bg-primary`, `bg-accent`), the variable name
       label, and its OKLCH value as text. Show these variables: background, foreground, card, primary,
       secondary, muted, muted-foreground, accent, destructive, border, input, ring, sidebar, sidebar-primary,
       sidebar-accent. Per 01-UI-SPEC accent reservation: the accent swatch card label and its preview tile
       border use the accent color.
    3. **Typography Scale** — four rows (Display 28px/600, Heading 20px/600, Body 16px/400, Label 14px/400);
       each row: role name + sample text "The quick brown fox jumps over the lazy dog" + a size/weight annotation.
    4. **Buttons** — every variant (default, secondary, outline, ghost, destructive, link) × sizes
       (default, sm, lg, icon) × states (normal, disabled, loading-with-Spinner).
    5. **Form Elements** — Input (normal, focused, disabled, error), Select (open), Checkbox (checked,
       unchecked, indeterminate), Toggle (on, off).
    6. **Feedback** — Badge variants (default, secondary, outline, destructive); three Sonner toast trigger
       buttons (info, success, error); one Skeleton example row.
    7. **Overlays** — a Dialog trigger + example dialog; a Tooltip trigger example.
    8. **Data Display** — IF `resources/js/components/ui/table.tsx` exists (installed by Plan 04), render a
       Table with 3 sample rows and a Pagination example. If it does not exist yet, render a short note
       "Table / Pagination — installed in Plan 04" instead. Do not import a missing module.
    9. **Spacing Scale** — a visual ruler: 7 colored blocks sized to the spacing tokens (4 / 8 / 16 / 24 /
       32 / 48 / 64 px) labelled xs / sm / md / lg / xl / 2xl / 3xl.

    Separate sections with the `Separator` primitive. Use `cn()` for conditional classes. Match the
    4-space Prettier style and import ordering of sibling pages.

    Run `npx prettier --write resources/js/pages/dev/` then `npm run build`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=Styleguide && npx tsc --noEmit 2>&1 | Select-String -Pattern "error TS" -Quiet -NotMatch</automated>
  </verify>
  <acceptance_criteria>
    - `app/Http/Controllers/Dev/StyleguideController.php` exists and contains `Inertia::render('dev/styleguide')`
    - `routes/web.php` contains `app()->isLocal()` and the `dev/styleguide` route named `dev.styleguide`
    - `resources/js/pages/dev/styleguide.tsx` exists, is at least 60 lines, and references `useAppearance`
    - `styleguide.tsx` contains the section headings: Color, Typography, Buttons, Spacing
    - `php artisan test --compact --filter=Styleguide` — the styleguide-renders test passes
    - `npx tsc --noEmit` reports no TypeScript errors
    - `npm run build` completes without error
  </acceptance_criteria>
  <done>The dev-only /dev/styleguide route renders a full token + component showcase that proves the warm-minimal design system in both themes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the warm-minimal design system</name>
  <what-built>
    The warm-minimal OKLCH palette is applied to every page (the @theme mapping propagates it app-wide),
    and a dev-only styleguide page at /dev/styleguide showcases all token swatches and shadcn components
    in both light and dark themes. UI-02 token correctness is perceptual — it cannot be asserted by an
    automated test, so this checkpoint is the verification artifact (per 01-VALIDATION.md Manual-Only
    Verifications).
  </what-built>
  <how-to-verify>
    1. Start the dev server (`composer run dev` or `npm run dev`) and log in.
    2. Visit /dev/styleguide.
    3. Confirm the page background reads as a warm off-white (paper-like) — NOT a clinical pure white and
       NOT a salmon/pink tint.
    4. In the Color Palette section, confirm the `--accent` swatch is a soft, low-saturation warm taupe —
       muted, barely distinct from the neutrals, NOT a bold or saturated color.
    5. Use the Light / Dark toggle in the top-right — confirm dark mode is a warm dark (not pure black),
       text stays legible, and the accent still reads as a soft taupe.
    6. Confirm the Typography section shows clear contrast between Display / Heading / Body / Label using
       weight and size only (one typeface — Instrument Sans).
    7. Confirm the Buttons, Form Elements, Feedback, and Overlays sections all render correctly in both themes.
    8. Visit the dashboard and a settings page — confirm they have also picked up the warm palette
       (the whole app inherits it).
    9. Resize the browser to ~768px and ~375px on /dev/styleguide and the dashboard — confirm the swatch
       grid reflows and there is no horizontal overflow.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (e.g. "background looks pink", "accent too saturated").</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` — CSS + JS compile clean
- `php artisan test --compact --filter=Styleguide` — styleguide route renders
- `php artisan test --compact --filter=PageRender` — dashboard/settings still render with the new palette
- `npx tsc --noEmit` — no TypeScript errors
- Human verification of the warm-minimal aesthetic passes (Task 3)
</verification>

<success_criteria>
- The warm-minimal palette is applied across the whole app in light and dark themes
- The dev-only styleguide page renders and proves the token set + component look
- The styleguide and app pages are responsive
- Token correctness is visually confirmed (warm off-white background, soft taupe accent)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
