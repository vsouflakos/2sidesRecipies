---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - composer.json
  - package.json
  - config/permission.php
  - tests/Pest.php
  - vite.config.ts
  - lang/en/app.php
  - lang/el/app.php
  - database/seeders/RolesAndPermissionsSeeder.php
  - tests/Feature/Access/RoleAssignmentTest.php
  - tests/Feature/Access/AccessControlTest.php
  - tests/Feature/Access/AdminUserManagementTest.php
  - tests/Feature/Access/DeactivatedUserTest.php
  - tests/Feature/Access/AdminSelfProtectionTest.php
  - tests/Feature/Localization/SetLocaleTest.php
  - tests/Feature/Localization/LocaleUpdateTest.php
  - tests/Feature/Lookup/LookupTableSeedTest.php
  - tests/Feature/Ui/StyleguideTest.php
  - tests/Feature/Ui/PageRenderTest.php
  - .gitignore
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, I18N-01, I18N-02, UI-01, UI-03]
must_haves:
  truths:
    - "spatie/laravel-permission v7 and laravel-react-i18n are installed and resolvable"
    - "The Feature test suite runs against a refreshed database per test"
    - "Every Phase 1 requirement has a failing (skipped/red) test file that later waves turn green"
    - "lang/en/app.php and lang/el/app.php exist with a parallel key set the Vite i18n plugin can compile"
  artifacts:
    - path: "config/permission.php"
      provides: "spatie permission config published"
      contains: "models"
    - path: "tests/Pest.php"
      provides: "RefreshDatabase enabled for Feature suite"
      contains: "->use(RefreshDatabase::class)"
    - path: "database/seeders/RolesAndPermissionsSeeder.php"
      provides: "Role + permission seeder used by all RBAC tests"
      contains: "RolesAndPermissionsSeeder"
    - path: "lang/en/app.php"
      provides: "English source strings"
      contains: "return ["
    - path: "lang/el/app.php"
      provides: "Greek translation strings"
      contains: "return ["
  key_links:
    - from: "vite.config.ts"
      to: "lang/*.php"
      via: "laravel-react-i18n vite plugin compiles PHP lang files to JSON"
      pattern: "i18n\\(\\)"
---

<objective>
Wave 0: stand up the test and package foundation every other Phase 1 plan builds on. Install spatie/laravel-permission v7 and laravel-react-i18n, publish the permission config, enable RefreshDatabase for the Feature suite, write the RolesAndPermissionsSeeder, create the bilingual lang files, and scaffold a failing test for every Phase 1 requirement so each later task has a red test to turn green.

Purpose: VALIDATION.md mandates Wave 0 land before any feature wave so every later task can be sampled against a test. This plan also installs the two new approved dependencies that Plans 02 and 06 require.

Output: Installed packages, published config, enabled test suite, seeder, lang files, and 9 scaffolded test files (initially red/skipped) covering all 9 phase requirements.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install spatie/laravel-permission v7 and laravel-react-i18n, publish config, enable RefreshDatabase</name>
  <read_first>
    - composer.json (current dependency versions — PHP 8.3, Laravel 13)
    - package.json (current npm deps and scripts)
    - tests/Pest.php (RefreshDatabase line is currently commented out)
    - vite.config.ts (current plugin order — laravel, inertia, react, tailwindcss, wayfinder)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 8 — i18n vite plugin; Installation section)
  </read_first>
  <action>
    Run these commands from the project root:

    ```
    composer require spatie/laravel-permission:^7.4
    php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"
    php artisan optimize:clear
    npm install laravel-react-i18n@^2.0.5
    ```

    `vendor:publish` creates `config/permission.php` AND a spatie migration in `database/migrations/`
    (e.g. `xxxx_create_permission_tables.php`). Leave the migration as published — do NOT edit it.

    Edit `tests/Pest.php`: uncomment the RefreshDatabase line so the Feature suite block reads exactly:

    ```php
    pest()->extend(TestCase::class)
        ->use(RefreshDatabase::class)
        ->in('Feature');
    ```

    Edit `vite.config.ts`: add the i18n plugin import and append it to the `plugins` array
    AFTER the wayfinder plugin:

    ```ts
    import i18n from 'laravel-react-i18n/vite';
    // ...
    plugins: [
        laravel({ /* existing config */ }),
        inertia(),
        react({ /* existing config */ }),
        tailwindcss(),
        wayfinder({ formVariants: true }),
        i18n(),
    ],
    ```

    Do NOT change any other plugin options — only add the import and the `i18n()` entry.

    Edit `.gitignore`: append a line to ignore the i18n plugin's generated JSON files:

    ```
    /lang/php_*.json
    ```

    Run `php artisan migrate` to apply the spatie permission tables to the dev database.
  </action>
  <verify>
    <automated>composer show spatie/laravel-permission && npm ls laravel-react-i18n && php artisan migrate:status</automated>
  </verify>
  <acceptance_criteria>
    - `composer show spatie/laravel-permission` reports a version starting with `7.`
    - `npm ls laravel-react-i18n` reports a version starting with `2.`
    - File `config/permission.php` exists
    - A spatie migration file matching `*_create_permission_tables.php` exists in `database/migrations/`
    - `tests/Pest.php` contains `->use(RefreshDatabase::class)` with no leading `//`
    - `vite.config.ts` contains `import i18n from 'laravel-react-i18n/vite';` and `i18n()` inside the plugins array
    - `.gitignore` contains the line `/lang/php_*.json`
    - `php artisan migrate:status` lists the permission tables migration as `Ran`
  </acceptance_criteria>
  <done>Both packages installed, permission config + migration published and migrated, RefreshDatabase enabled for Feature suite, i18n vite plugin wired.</done>
</task>

<task type="auto">
  <name>Task 2: Write RolesAndPermissionsSeeder and the bilingual lang files</name>
  <read_first>
    - database/seeders/DatabaseSeeder.php (current seeder — only calls UserFactory if anything)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 — RolesAndPermissionsSeeder hierarchical superset)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Copywriting Contract — all English source strings that go into lang/en/app.php)
  </read_first>
  <action>
    Create `database/seeders/RolesAndPermissionsSeeder.php` via `php artisan make:seeder RolesAndPermissionsSeeder --no-interaction`,
    then implement its `run()` method exactly as follows:

    - Call `app()[\Spatie\Permission\PermissionRegistrar::class]->forgetCachedPermissions();` FIRST (clears stale cache).
    - Define permission arrays:
      - `$userPermissions = ['create-recipes', 'manage-own-ingredients'];`
      - `$moderatorPermissions = array_merge($userPermissions, ['review-ingredients']);`
      - `$adminPermissions = array_merge($moderatorPermissions, ['manage-users']);`
    - Loop `$adminPermissions` and call `\Spatie\Permission\Models\Permission::firstOrCreate(['name' => $perm]);` for each.
    - Create roles: `$userRole = \Spatie\Permission\Models\Role::firstOrCreate(['name' => 'User']);` and the same for `'Moderator'` and `'Admin'`.
    - Assign supersets: `$userRole->syncPermissions($userPermissions);`, `$moderatorRole->syncPermissions($moderatorPermissions);`, `$adminRole->syncPermissions($adminPermissions);`.
    - Create the default dev Admin: `User::firstOrCreate(['email' => 'admin@twosides.test'], ['name' => 'Admin', 'password' => \Illuminate\Support\Facades\Hash::make('password')]);` then `$admin->assignRole('Admin');`.
    - Use explicit `use` imports for `App\Models\User`, the spatie `Role`/`Permission` models, and `Hash`.

    Edit `database/seeders/DatabaseSeeder.php`: in `run()`, add `$this->call(RolesAndPermissionsSeeder::class);` as the first call.
    (UnitSeeder and AllergenSeeder are added by Plan 03 — do not add them here.)

    Create the lang directory files. Create `lang/en/app.php` returning a PHP array of the English UI strings.
    Use these keys and exact English copy (sourced verbatim from 01-UI-SPEC.md Copywriting Contract). Use `:name` and `:role`
    as Laravel placeholder syntax (laravel-react-i18n supports `:placeholder` replacement):

    ```php
    <?php

    return [
        'nav' => [
            'users' => 'Users',
            'dashboard' => 'Dashboard',
        ],
        'admin' => [
            'page_heading' => 'User Management',
            'search_placeholder' => 'Search by name or email…',
            'result_count' => 'Showing :shown of :total users',
            'empty_heading' => 'No users found',
            'empty_body' => 'No users match your search. Try a different name or email.',
            'toast_role_changed' => 'Role updated — :name is now a :role.',
            'toast_deactivated' => ":name's account has been deactivated.",
            'toast_deleted' => ":name's account has been deleted.",
            'toast_error' => 'Something went wrong. Please try again.',
            'deactivate_title' => 'Deactivate account?',
            'deactivate_body' => "Deactivating :name's account will prevent them from logging in. You can reactivate it at any time.",
            'deactivate_dismiss' => 'Keep account active',
            'deactivate_confirm' => 'Deactivate account',
            'delete_title' => 'Delete account?',
            'delete_body' => "This will permanently delete :name's account and all their data. This action cannot be undone.",
            'delete_dismiss' => 'Keep account',
            'delete_confirm' => 'Delete account',
            'guard_self_role' => 'You cannot change your own role',
            'guard_self_deactivate' => 'You cannot deactivate your own account',
            'guard_self_delete' => 'You cannot delete your own account',
            'guard_last_admin_role' => 'Cannot remove the last Admin role',
            'guard_last_admin_deactivate' => 'Cannot deactivate the last Admin',
            'guard_last_admin_delete' => 'Cannot delete the last Admin',
        ],
        'auth' => [
            'deactivated' => 'Your account has been deactivated. Contact an administrator.',
        ],
        'language' => [
            'en' => 'EN',
            'el' => 'EL',
        ],
    ];
    ```

    Create `lang/el/app.php` with the SAME key structure and Greek translations:

    ```php
    <?php

    return [
        'nav' => [
            'users' => 'Χρήστες',
            'dashboard' => 'Πίνακας ελέγχου',
        ],
        'admin' => [
            'page_heading' => 'Διαχείριση χρηστών',
            'search_placeholder' => 'Αναζήτηση με όνομα ή email…',
            'result_count' => 'Εμφάνιση :shown από :total χρήστες',
            'empty_heading' => 'Δεν βρέθηκαν χρήστες',
            'empty_body' => 'Κανένας χρήστης δεν ταιριάζει με την αναζήτησή σας. Δοκιμάστε διαφορετικό όνομα ή email.',
            'toast_role_changed' => 'Ο ρόλος ενημερώθηκε — ο/η :name είναι πλέον :role.',
            'toast_deactivated' => 'Ο λογαριασμός του/της :name απενεργοποιήθηκε.',
            'toast_deleted' => 'Ο λογαριασμός του/της :name διαγράφηκε.',
            'toast_error' => 'Κάτι πήγε στραβά. Δοκιμάστε ξανά.',
            'deactivate_title' => 'Απενεργοποίηση λογαριασμού;',
            'deactivate_body' => 'Η απενεργοποίηση του λογαριασμού του/της :name θα τον/την εμποδίσει να συνδεθεί. Μπορείτε να τον επανενεργοποιήσετε ανά πάσα στιγμή.',
            'deactivate_dismiss' => 'Διατήρηση ενεργού λογαριασμού',
            'deactivate_confirm' => 'Απενεργοποίηση λογαριασμού',
            'delete_title' => 'Διαγραφή λογαριασμού;',
            'delete_body' => 'Αυτό θα διαγράψει οριστικά τον λογαριασμό του/της :name και όλα τα δεδομένα του. Αυτή η ενέργεια δεν μπορεί να αναιρεθεί.',
            'delete_dismiss' => 'Διατήρηση λογαριασμού',
            'delete_confirm' => 'Διαγραφή λογαριασμού',
            'guard_self_role' => 'Δεν μπορείτε να αλλάξετε τον δικό σας ρόλο',
            'guard_self_deactivate' => 'Δεν μπορείτε να απενεργοποιήσετε τον δικό σας λογαριασμό',
            'guard_self_delete' => 'Δεν μπορείτε να διαγράψετε τον δικό σας λογαριασμό',
            'guard_last_admin_role' => 'Δεν είναι δυνατή η αφαίρεση του τελευταίου ρόλου Admin',
            'guard_last_admin_deactivate' => 'Δεν είναι δυνατή η απενεργοποίηση του τελευταίου Admin',
            'guard_last_admin_delete' => 'Δεν είναι δυνατή η διαγραφή του τελευταίου Admin',
        ],
        'auth' => [
            'deactivated' => 'Ο λογαριασμός σας απενεργοποιήθηκε. Επικοινωνήστε με έναν διαχειριστή.',
        ],
        'language' => [
            'en' => 'EN',
            'el' => 'EL',
        ],
    ];
    ```

    Run `vendor/bin/pint --dirty --format agent` after creating the PHP files.
  </action>
  <verify>
    <automated>php artisan db:seed --class=RolesAndPermissionsSeeder --no-interaction && php artisan tinker --execute 'echo \Spatie\Permission\Models\Role::count();'</automated>
  </verify>
  <acceptance_criteria>
    - `database/seeders/RolesAndPermissionsSeeder.php` exists and contains `forgetCachedPermissions`
    - `RolesAndPermissionsSeeder.php` contains `'review-ingredients'` and `'manage-users'` and `admin@twosides.test`
    - `database/seeders/DatabaseSeeder.php` contains `$this->call(RolesAndPermissionsSeeder::class)`
    - After running the seeder, `\Spatie\Permission\Models\Role::count()` returns `3`
    - `lang/en/app.php` exists, contains `'page_heading' => 'User Management'` and `'result_count' => 'Showing :shown of :total users'`
    - `lang/el/app.php` exists, contains `'page_heading' => 'Διαχείριση χρηστών'`
    - `lang/en/app.php` and `lang/el/app.php` have identical top-level key sets (`nav`, `admin`, `auth`, `language`)
  </acceptance_criteria>
  <done>Seeder creates 3 roles + 4 permissions + a default Admin; lang files exist in both locales with a parallel key set.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold a failing test file for every Phase 1 requirement</name>
  <read_first>
    - tests/Feature/DashboardTest.php (existing Pest test style — closure-based, factory usage)
    - tests/Feature/Auth/RegistrationTest.php (existing registration test pattern)
    - .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — exact test files and filters)
  </read_first>
  <action>
    Create 9 Pest feature test files. Each file at this stage contains test stubs that are intentionally
    RED (failing) — they assert the target behavior so later waves turn them green. Do NOT mark tests as
    `->skip()` permanently; write real assertions that will fail until the feature exists. Use
    `RolesAndPermissionsSeeder` via `$this->seed(\Database\Seeders\RolesAndPermissionsSeeder::class)` in a
    `beforeEach()` where RBAC roles are needed.

    Create each file with `php artisan make:test --pest <Path>` (omit the `tests/Feature/` prefix from the name,
    but DO include the subdirectory, e.g. `Access/RoleAssignmentTest`):

    1. `tests/Feature/Access/RoleAssignmentTest.php` (ACCESS-01, ACCESS-03):
       - `test('new registration is assigned the User role')` — POST `/register` with valid data, then
         `expect(User::latest()->first()->hasRole('User'))->toBeTrue();`
       - `test('user model uses the HasRoles trait')` — assert
         `in_array(\Spatie\Permission\Traits\HasRoles::class, class_uses_recursive(User::class))`.

    2. `tests/Feature/Access/AccessControlTest.php` (ACCESS-02, ACCESS-04):
       - `test('moderator can reach the ingredient review queue placeholder')` — actingAs a Moderator,
         GET `/admin/ingredients`, `assertOk()`.
       - `test('plain user gets 403 on the ingredient review queue')` — actingAs a User, GET `/admin/ingredients`,
         `assertForbidden()`.
       - `test('plain user gets 403 on every admin route')` — actingAs a User, GET `/admin/users`,
         `assertForbidden()`.

    3. `tests/Feature/Access/AdminUserManagementTest.php` (ACCESS-03):
       - `test('admin can list users')` — actingAs an Admin, GET `/admin/users`, `assertOk()`.
       - `test('admin can assign a role')` — actingAs an Admin, PUT `/admin/users/{user}/role` with
         `['role' => 'Moderator']`, then `expect($target->fresh()->hasRole('Moderator'))->toBeTrue();`
       - `test('admin can deactivate a user')` — actingAs an Admin, PUT `/admin/users/{user}/status`,
         then `expect($target->fresh()->account_status->value)->toBe('deactivated');`
       - `test('admin can soft-delete a user')` — actingAs an Admin, DELETE `/admin/users/{user}`,
         then `$this->assertSoftDeleted($target);`

    4. `tests/Feature/Access/DeactivatedUserTest.php` (ACCESS-04):
       - `test('deactivated user cannot log in')` — create a User with `account_status` deactivated,
         POST `/login` with their credentials, `$this->assertGuest();` and assert a redirect back to login
         with an `email` error.

    5. `tests/Feature/Access/AdminSelfProtectionTest.php` (ACCESS-04):
       - `test('admin cannot change their own role')` — actingAs an Admin, PUT
         `/admin/users/{self}/role` with `['role' => 'User']`, assert a validation error and the role unchanged.
       - `test('admin cannot deactivate themselves')` — actingAs an Admin, PUT `/admin/users/{self}/status`,
         assert a validation error.
       - `test('admin cannot delete themselves')` — actingAs an Admin, DELETE `/admin/users/{self}`,
         assert a validation error and the user not soft-deleted.
       - `test('system refuses to remove the last admin role')` — with exactly one Admin in the DB, actingAs
         a second Admin... (note: requires 2 admins to act; structure as: 2 admins exist, delete one, then the
         remaining is last — attempting to demote the last admin via a third actor fails). Implement as:
         create one Admin only, attempt to demote them via a separate Admin actor created then immediately
         soft-deleted is not viable — instead assert `User::role('Admin')->count()` guard: create exactly one
         Admin, acting as that Admin attempt to demote themselves (covered above); ADD a dedicated case where
         two Admins exist, the acting Admin demotes the OTHER admin successfully, then a second attempt to
         demote the now-last Admin fails with a validation error.

    6. `tests/Feature/Localization/SetLocaleTest.php` (I18N-01, I18N-02):
       - `test('set locale middleware applies the user locale')` — create a User with `locale = 'el'`,
         actingAs them, GET `/dashboard`, then `expect(app()->getLocale())->toBe('el');`
       - `test('locale shared prop is present in the inertia payload')` — actingAs a User, GET `/dashboard`,
         use `assertInertia` to assert the `locale` prop exists.

    7. `tests/Feature/Localization/LocaleUpdateTest.php` (I18N-01):
       - `test('locale switcher persists the choice to the user record')` — actingAs a User, PUT `/locale`
         with `['locale' => 'el']`, then `expect($user->fresh()->locale)->toBe('el');`
       - `test('locale update rejects an unsupported locale')` — PUT `/locale` with `['locale' => 'fr']`,
         assert a validation error.

    8. `tests/Feature/Lookup/LookupTableSeedTest.php` (covers the lookup-table seed requirement):
       - `test('allergen lookup table seeds the 14 EU allergens')` — run UnitSeeder + AllergenSeeder,
         `expect(\App\Models\Allergen::count())->toBe(14);`
       - `test('unit lookup table is seeded with weight, volume and count units')` — assert
         `\App\Models\Unit::count()` is greater than `0` and that units of type `weight`, `volume`, `count` all exist.

    9. `tests/Feature/Ui/StyleguideTest.php` (UI-01) and `tests/Feature/Ui/PageRenderTest.php` (UI-03) —
       create BOTH files:
       - `StyleguideTest.php`: `test('styleguide page renders in the local environment')` — actingAs a User,
         GET `/dev/styleguide`, `assertOk()`.
       - `PageRenderTest.php`: `test('dashboard renders for an authenticated user')`,
         `test('admin users page renders for an admin')`, `test('settings profile renders for an authenticated user')` —
         each actingAs the appropriate user, GET the route, `assertOk()`.

    These tests WILL fail now — that is correct. They are the sampling targets for later waves.
    Run `vendor/bin/pint --dirty --format agent` after creating the files.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="RoleAssignment|AccessControl|AdminUserManagement|DeactivatedUser|AdminSelfProtection|SetLocale|LocaleUpdate|LookupTableSeed|Styleguide|PageRender" 2>&1 | Select-String -Pattern "Tests:"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 test files exist at the exact paths listed in the action
    - `tests/Feature/Access/RoleAssignmentTest.php` contains `hasRole('User')`
    - `tests/Feature/Access/AccessControlTest.php` contains `/admin/ingredients` and `assertForbidden`
    - `tests/Feature/Localization/SetLocaleTest.php` contains `app()->getLocale()`
    - `tests/Feature/Lookup/LookupTableSeedTest.php` contains `->toBe(14)`
    - `php artisan test --compact --filter=PageRender` runs and reports test results (failures expected — files exist and execute)
    - The combined filter command executes all 9 files' tests (the `Tests:` summary line appears with a non-zero count)
  </acceptance_criteria>
  <done>9 requirement-covering test files exist and execute; they fail (red) as expected, giving later waves concrete sampling targets.</done>
</task>

</tasks>

<verification>
- `composer show spatie/laravel-permission` reports v7.x; `npm ls laravel-react-i18n` reports v2.x
- `config/permission.php` exists; spatie permission migration ran
- `tests/Pest.php` has RefreshDatabase enabled
- `php artisan db:seed --class=RolesAndPermissionsSeeder` creates 3 roles + 4 permissions + 1 Admin
- `lang/en/app.php` and `lang/el/app.php` exist with identical key structures
- All 9 Wave 0 test files exist and execute (they are red — that is the expected Wave 0 state)
</verification>

<success_criteria>
- Both new dependencies installed and resolvable
- Feature suite runs with RefreshDatabase per test
- RolesAndPermissionsSeeder seeds the full role/permission hierarchy + dev Admin
- Bilingual lang files exist with parallel keys
- Every Phase 1 requirement has a failing test file as a sampling target
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
