---
phase: 04-recipe-tests
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - app/Http/Controllers/Recipes/RecipeController.php
  - app/Http/Resources/RecipeBuilderResource.php
  - resources/js/types/recipe.ts
  - resources/js/components/recipes/test-summary-block.tsx
  - resources/js/pages/recipes/show.tsx
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "recipes/show.tsx surfaces a compact test summary block (count + latest score)"
    - "The summary block links to the dedicated tests page"
    - "When a recipe has no tests, the block shows a 'Record first test' link"
    - "RecipeController::show provides test_count and latest_test_score to the builder"
  artifacts:
    - path: "resources/js/components/recipes/test-summary-block.tsx"
      provides: "compact test summary strip for the builder"
      contains: "TestSummaryBlock"
  key_links:
    - from: "resources/js/pages/recipes/show.tsx"
      to: "resources/js/components/recipes/test-summary-block.tsx"
      via: "renders TestSummaryBlock below the metrics panel"
      pattern: "TestSummaryBlock"
    - from: "resources/js/components/recipes/test-summary-block.tsx"
      to: "recipes.tests.index"
      via: "Wayfinder route link to the tests page"
      pattern: "recipes/.*tests|RecipeTestController"
    - from: "app/Http/Controllers/Recipes/RecipeController.php"
      to: "app/Models/RecipeTest.php"
      via: "withCount('tests') + latestTest aggregate"
      pattern: "withCount\\('tests'\\)|latestTest"
---

<objective>
Wire test visibility into the recipe builder: `RecipeController::show` provides a test count + latest overall score, the `TestSummaryBlock` component renders a compact strip on `recipes/show.tsx`, and a human-verify checkpoint exercises the full Phase 4 flow end to end.

Purpose: Satisfies success criterion 3 — "test records are visible on the recipe detail page" — since `recipes/show.tsx` IS the recipe detail surface. Confirms the whole phase works.
Output: 2 backend edits, 1 types edit, 1 new component, 1 show.tsx edit, and a verified end-to-end flow.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recipe-tests/04-CONTEXT.md
@.planning/phases/04-recipe-tests/04-UI-SPEC.md
@.planning/phases/04-recipe-tests/04-02-SUMMARY.md
@.planning/phases/04-recipe-tests/04-03-SUMMARY.md

<interfaces>
<!-- Contracts from prior plans + existing codebase. -->

From app/Models/Recipe.php (plan 04-01 added these):
```php
public function tests(): HasMany;       // hasMany(RecipeTest::class)
public function latestTest(): HasOne;   // hasOne(RecipeTest::class)->latestOfMany('tested_at')
```

app/Http/Controllers/Recipes/RecipeController.php::show() — current behavior (lines 164-247):
- `Gate::authorize('view', $recipe);`
- `$recipe->load(['sections.ingredientLines', 'steps', 'draft', 'currentVersion', 'cuisine', 'tags']);`
- Renders `Inertia::render('recipes/show', ['recipe' => (new RecipeBuilderResource($recipe))->resolve(), 'draft' => ..., 'metrics' => ..., 'versions' => ..., 'cuisines' => ..., 'units' => ..., 'tags' => ..., 'can' => [...]]);`

app/Http/Resources/RecipeBuilderResource.php — the resource serializing the `recipe` prop. The builder's `RecipeBuilderData` TS type maps to its output.

resources/js/pages/recipes/show.tsx — the builder page. The right column renders `<MetricsPanel ... />` inside `<div className="hidden lg:block lg:overflow-y-auto lg:border-l lg:border-border">` around line 693-706. The TestSummaryBlock is placed BELOW the MetricsPanel inside that same right column div.

resources/js/types/recipe.ts — `RecipeShowProps` interface (line 204) has: recipe, draft, metrics, versions, cuisines, units, tags, can. `RecipeBuilderData` is the `recipe` prop type.

Wayfinder route helper for the tests page: `import { index as recipeTestsIndex } from '@/actions/App/Http/Controllers/Recipes/RecipeTestController';` — regenerated by `npm run build`.

UI-SPEC Surface 5 — Test Summary Block: a horizontal strip (not a full panel), placed below the metrics panel slot. Left = "{N} test{s} · Latest: {N}/10" (Body/16px) or "No tests yet" (Label/muted). Right = "View tests →" ghost link, or "Record first test" when count is 0. Clicking navigates to the tests page (no modal).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecipeController::show provides test aggregates + TestSummaryBlock component</name>
  <files>app/Http/Controllers/Recipes/RecipeController.php, app/Http/Resources/RecipeBuilderResource.php, resources/js/types/recipe.ts, resources/js/components/recipes/test-summary-block.tsx, lang/en/app.php, lang/el/app.php</files>
  <read_first>
    - app/Http/Controllers/Recipes/RecipeController.php (the file being modified — the show() method, lines 164-247)
    - app/Http/Resources/RecipeBuilderResource.php (the file being modified — current toArray output)
    - app/Models/Recipe.php (tests() / latestTest() relations from plan 04-01)
    - resources/js/types/recipe.ts (the file being modified — RecipeShowProps interface, line 204)
    - resources/js/components/recipes/metrics-panel/metrics-panel.tsx (sibling component composition + warm-minimal styling reference)
    - lang/en/app.php (the `tests` key group added in plan 04-03 — summary-block strings)
    - .planning/phases/04-recipe-tests/04-UI-SPEC.md (Surface 5 Test Summary Block; Copywriting Contract summary-block rows)
  </read_first>
  <action>
    **app/Http/Controllers/Recipes/RecipeController.php** — in `show()`, after the existing `$recipe->load([...])` call, load the test aggregates:
    - Add `->loadCount('tests')` and `->load('latestTest')` on `$recipe` (or include `withCount('tests')` if reworking the load — keep it a single aggregate, no N+1).
    - Compute `$testSummary = ['count' => $recipe->tests_count, 'latest_score' => $recipe->latestTest?->overall_rating];`
    - Add `'test_summary' => $testSummary,` to the `Inertia::render('recipes/show', [...])` props array.

    **app/Http/Resources/RecipeBuilderResource.php** — no change needed if `test_summary` is passed as a sibling prop (preferred). Only edit this resource if the executor finds it cleaner to nest the summary inside the `recipe` payload — if so, add `'test_summary' => [...]` to its `toArray()`. Default decision: pass `test_summary` as a top-level Inertia prop, leave RecipeBuilderResource untouched. (Listed in `files_modified` only as a guard in case the nested approach is chosen — prefer the sibling-prop approach.)

    **resources/js/types/recipe.ts** — add a `TestSummary` interface and extend `RecipeShowProps`:
    ```typescript
    export interface TestSummary {
      count: number;
      latest_score: number | null;
    }
    ```
    Add `test_summary: TestSummary;` to the `RecipeShowProps` interface.

    Create **resources/js/components/recipes/test-summary-block.tsx** — exports `TestSummaryBlock`. Props: `{ recipeId: number; summary: TestSummary }`. Renders a horizontal strip (per UI-SPEC Surface 5), NOT a full card panel:
    - Container: `flex items-center justify-between` with `md` padding, a top border (`border-t border-border`) to separate it from the metrics panel.
    - Left, when `summary.count > 0`: `{count} test{s} · Latest: {score}/10` (Body 16px) — use `t('app.tests.summary_has_tests', { count, score })`. When `summary.count === 0`: `t('app.tests.summary_no_tests')` ("No tests yet", Label/muted).
    - Right: a ghost `Button` link. When count > 0 → label `t('app.tests.summary_link')` ("View tests →"); when 0 → `t('app.tests.summary_link_empty')` ("Record first test"). Both navigate to `recipeTestsIndex({ recipe: recipeId }).url` via Inertia `<Link>` or `router.visit` — no modal opens.
    - Use `useTranslations()` and `cn()`. Import the Wayfinder helper `index as recipeTestsIndex` from `@/actions/App/Http/Controllers/Recipes/RecipeTestController`.

    **i18n** — add to `lang/en/app.php` and `lang/el/app.php` under the `tests` key group (added in plan 04-03): `summary_has_tests` (EN: ":count test|:count tests · Latest: :score/10" — use Laravel pluralization, or a simpler ":count tests · Latest: :score/10" if pluralization is awkward), `summary_no_tests` (EN: "No tests yet"), `summary_link` (EN: "View tests →"), `summary_link_empty` (EN: "Record first test"). Add Greek equivalents.

    Run `vendor/bin/pint --dirty --format agent`, then `npx prettier --write` on the TS files, then `npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/Http/Controllers/Recipes/RecipeController.php` `show()` contains `'test_summary'` in the `Inertia::render` props and references `tests_count` / `latestTest`
    - `resources/js/types/recipe.ts` contains `export interface TestSummary` and `test_summary: TestSummary` inside `RecipeShowProps`
    - `resources/js/components/recipes/test-summary-block.tsx` contains `TestSummaryBlock`, branches on `summary.count`, and imports a route helper from `@/actions/App/Http/Controllers/Recipes/RecipeTestController`
    - `lang/en/app.php` contains `summary_no_tests`, `summary_link`, and `summary_link_empty` under the `tests` group
    - `lang/el/app.php` contains the matching Greek summary keys
    - `npm run build` completes with exit code 0
  </acceptance_criteria>
  <done>RecipeController::show passes a test_summary prop, the TestSummary type exists, and TestSummaryBlock renders the count/score strip with a link to the tests page.</done>
</task>

<task type="auto">
  <name>Task 2: Render TestSummaryBlock on recipes/show.tsx</name>
  <files>resources/js/pages/recipes/show.tsx</files>
  <read_first>
    - resources/js/pages/recipes/show.tsx (the file being modified — imports block, the right-column metrics panel div around lines 693-706, ShowPageProps type around line 65)
    - resources/js/components/recipes/test-summary-block.tsx (Task 1 — the component being placed)
    - resources/js/types/recipe.ts (RecipeShowProps with the new test_summary field)
  </read_first>
  <action>
    Edit **resources/js/pages/recipes/show.tsx**:
    - Add the import `import { TestSummaryBlock } from '@/components/recipes/test-summary-block';` alongside the other `@/components/recipes/...` imports.
    - The page already extends `RecipeShowProps` via `ShowPageProps` — `test_summary` is now part of `RecipeShowProps` (from Task 1), so destructure `test_summary` from props alongside `metrics`, `versions`, etc.
    - In the right-hand column div `<div className="hidden lg:block lg:overflow-y-auto lg:border-l lg:border-border">` (around line 693), render `<TestSummaryBlock recipeId={recipe.id} summary={test_summary} />` immediately AFTER the closing tag of `<MetricsPanel ... />` (still inside that right-column div, so it sits below the metrics panel).
    - Confirm `recipe.id` is available in scope — the `recipe` prop is `RecipeBuilderData`; if `id` is not directly on it, use the recipe id already used elsewhere on the page (e.g. in the destroy/duplicate route calls). Use whatever id source the existing delete/duplicate actions already use.

    Run `npx prettier --write` on show.tsx, then `npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/pages/recipes/show.tsx` contains `import { TestSummaryBlock }` and `<TestSummaryBlock`
    - `<TestSummaryBlock` appears after `<MetricsPanel` in the file (placed below the metrics panel)
    - `show.tsx` destructures `test_summary` from props
    - `npm run build` completes with exit code 0 and no TypeScript errors
  </acceptance_criteria>
  <done>The recipe builder renders the test summary block below the metrics panel, linking through to the dedicated tests page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end Phase 4 human verification</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/04-recipe-tests/04-UI-SPEC.md (the visual/interaction contract being verified)
  </read_first>
  <what-built>
    The complete Phase 4 recipe-tests feature: the dedicated `/recipes/{recipe}/tests` page with the Record test modal (trial + experiment modes), structured ratings, photo upload (drag-drop + previews + lightbox), the test card list, the delete-test confirmation, and the test summary block on the recipe builder. The full RecipeTestTest Pest suite is GREEN and `npm run build` passes.
  </what-built>
  <action>
    Verification-only checkpoint — no code changes. Before pausing for the human, confirm the automated gates pass: run `php artisan test --compact` (full suite GREEN) and `npm run build` (exit 0). Then ensure the app is serving (`composer run dev` or `npm run dev` + Herd) and present the verification steps below to the user. Wait for the resume signal before completing the plan.
  </action>
  <how-to-verify>
    Run `composer run dev` (or confirm `npm run dev` + Herd are serving the app). Resolve the app URL with the `get-absolute-url` tool. Then:

    1. Open an existing recipe's builder page (`/recipes/{slug-or-id}`). Confirm a test summary strip appears below the metrics panel showing "No tests yet" + a "Record first test" link.
    2. Click "Record first test" → confirm you land on `/recipes/{recipe}/tests` with the heading "Tests for {Recipe Name}", the empty state ("No tests yet" + "Record your first test"), and a "Record test" button.
    3. Click "Record test" → the modal opens. With type = "Trial run": pick a version, set a tested date, set an overall rating, type tasting notes, set a couple of per-dimension scores, click "Add dimension" and add a custom one. Drag 2–3 images onto the photo drop zone (or click to browse) → confirm thumbnail previews appear before submit and the "{N}/8 photos" indicator updates. Click "Save test".
    4. Confirm the toast "Test recorded." fires and the new test appears as a card — type badge, version, overall score (colored per score), tasting notes, and a photo strip.
    5. Click a photo thumbnail → confirm the lightbox opens, the arrows navigate between photos, and the "{current}/{total}" indicator is correct. Close it.
    6. Click "Record test" again, switch the type toggle to "Experiment" → confirm the Experiment section reveals (Hypothesis, Outcome, Verdict, change rows). Fill a hypothesis, add a change row, pick a verdict, set an overall rating, save → confirm the experiment card shows the verdict badge.
    7. From a test card's action menu, choose "Edit" → confirm the modal pre-fills; from the menu choose "Delete" → confirm the "Delete test?" dialog, delete it, confirm the "Test deleted." toast and the card disappears.
    8. Return to the recipe builder → confirm the test summary strip now shows the test count + "Latest: {N}/10" and a "View tests →" link.
    9. Switch the UI language to Greek → confirm the tests page, modal, and summary block render Greek strings.
    10. Resize to a mobile viewport → confirm the tests list is single-column and the modal goes full-screen.
  </how-to-verify>
  <verify>
    <automated>php artisan test --compact && npm run build</automated>
  </verify>
  <done>Automated gates pass (full Pest suite GREEN, npm run build exit 0) and the human has approved the end-to-end Phase 4 flow.</done>
  <resume-signal>Type "approved" to complete Phase 4, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `php artisan test --compact` — full suite GREEN, no regressions.
- `npm run build` succeeds with no TypeScript errors.
- Human checkpoint approved.
</verification>

<success_criteria>
- The recipe builder surfaces a compact test summary block linking to the tests page.
- Empty-state recipes show a "Record first test" link.
- The end-to-end flow (record trial, record experiment, photos + lightbox, edit, delete, i18n, responsive) is human-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-recipe-tests/04-04-SUMMARY.md`
</output>
