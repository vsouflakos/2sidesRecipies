---
phase: 04-recipe-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_17_000011_create_recipe_tests_table.php
  - database/migrations/2026_05_17_000012_create_recipe_test_photos_table.php
  - app/Enums/TestType.php
  - app/Enums/TestVerdict.php
  - app/Models/RecipeTest.php
  - app/Models/RecipeTestPhoto.php
  - app/Models/Recipe.php
  - database/factories/RecipeTestFactory.php
  - database/factories/RecipeTestPhotoFactory.php
  - tests/Feature/Recipes/RecipeTestTest.php
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "recipe_tests and recipe_test_photos tables exist with the documented columns"
    - "RecipeTest model casts type/verdict to enums and ratings/change_rows to arrays"
    - "RecipeTest links to a RecipeVersion via recipe_version_id"
    - "RecipeTestTest feature suite executes without parse errors and is RED (routes/controller not yet built)"
  artifacts:
    - path: "database/migrations/2026_05_17_000011_create_recipe_tests_table.php"
      provides: "recipe_tests table schema"
      contains: "Schema::create('recipe_tests'"
    - path: "database/migrations/2026_05_17_000012_create_recipe_test_photos_table.php"
      provides: "recipe_test_photos table schema"
      contains: "Schema::create('recipe_test_photos'"
    - path: "app/Models/RecipeTest.php"
      provides: "RecipeTest Eloquent model with casts and relations"
      contains: "class RecipeTest extends Model"
    - path: "app/Models/RecipeTestPhoto.php"
      provides: "RecipeTestPhoto Eloquent model with url accessor"
      contains: "class RecipeTestPhoto extends Model"
    - path: "app/Enums/TestType.php"
      provides: "trial/experiment backed enum"
      contains: "enum TestType: string"
    - path: "app/Enums/TestVerdict.php"
      provides: "worked/didnt_work/inconclusive backed enum"
      contains: "enum TestVerdict: string"
    - path: "tests/Feature/Recipes/RecipeTestTest.php"
      provides: "feature test suite for TEST-01..04"
      contains: "RecipeTestTest"
    - path: "database/factories/RecipeTestFactory.php"
      provides: "RecipeTest factory with trial + experiment states"
      contains: "class RecipeTestFactory extends Factory"
  key_links:
    - from: "app/Models/RecipeTest.php"
      to: "app/Models/RecipeVersion.php"
      via: "belongsTo recipe_version_id"
      pattern: "belongsTo\\(RecipeVersion::class"
    - from: "app/Models/Recipe.php"
      to: "app/Models/RecipeTest.php"
      via: "tests() HasMany + latestTest() HasOne"
      pattern: "hasMany\\(RecipeTest::class"
---

<objective>
Establish the Phase 4 data layer: the `recipe_tests` and `recipe_test_photos` tables, the `RecipeTest` / `RecipeTestPhoto` models, the `TestType` / `TestVerdict` enums, factories, and the Wave 0 Pest feature test suite that covers TEST-01..04.

Purpose: Every later plan (backend controller, frontend page) builds on this schema and against this test suite. Writing the test suite first gives waves 2 and 3 a concrete RED→GREEN target.
Output: 2 migrations, 2 enums, 2 models, 1 model edit, 2 factories, 1 feature test file, and the `php artisan storage:link` symlink.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recipe-tests/04-CONTEXT.md
@.planning/phases/04-recipe-tests/04-RESEARCH.md
@.planning/phases/04-recipe-tests/04-VALIDATION.md

<interfaces>
<!-- Key existing contracts the executor builds against. Use these directly. -->

From app/Models/RecipeVersion.php — the entity a test links to:
```php
class RecipeVersion extends Model {
    // fillable: recipe_id, version_number, committed_by, committed_at, change_note, snapshot, yield_g, ...
    public function recipe(): BelongsTo;          // belongsTo(Recipe::class)
}
```

From app/Models/Recipe.php — gains tests() and latestTest() in this plan:
```php
class Recipe extends Model {
    use HasFactory, SoftDeletes;
    public function user(): BelongsTo;
    public function versions(): HasMany;          // hasMany(RecipeVersion::class)
    public function currentVersion(): BelongsTo;  // belongsTo(RecipeVersion::class, 'current_version_id')
    public function draft(): HasOne;
}
```

From app/Enums/Difficulty.php — the enum pattern to mirror (backed string enum with label()).

From database/factories/RecipeVersionFactory.php — the factory pattern:
```php
class RecipeVersionFactory extends Factory {
    public function definition(): array {
        return ['recipe_id' => Recipe::factory(), 'version_number' => 1, 'committed_by' => User::factory(), ...];
    }
}
```

Existing recipe-domain migrations are numbered 2026_05_17_000003 through 2026_05_17_000010. New migrations continue at 000011 and 000012.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrations and enums</name>
  <read_first>
    - database/migrations/2026_05_17_000010_create_recipe_tag_table.php (latest migration — confirm numbering and Schema::create style)
    - database/migrations/2026_05_17_000007_create_recipe_versions_table.php (foreignId / constrained style to mirror)
    - app/Enums/Difficulty.php (backed-enum + label() pattern to mirror exactly)
    - app/Enums/AccountStatus.php (second enum reference)
  </read_first>
  <action>
    Create two migrations via `php artisan make:migration --no-interaction`, then rename the generated files to the exact names below so they sort after the Phase 3 recipe migrations.

    **database/migrations/2026_05_17_000011_create_recipe_tests_table.php** — `Schema::create('recipe_tests', ...)`:
    - `$table->id();`
    - `$table->foreignId('recipe_id')->constrained('recipes')->cascadeOnDelete();`
    - `$table->foreignId('recipe_version_id')->constrained('recipe_versions')->restrictOnDelete();`
    - `$table->foreignId('user_id')->constrained('users')->cascadeOnDelete();`
    - `$table->string('type');` — 'trial' | 'experiment'
    - `$table->timestamp('tested_at');`
    - `$table->text('tasting_notes')->nullable();`
    - `$table->unsignedTinyInteger('overall_rating');` — 1-10, required
    - `$table->json('ratings')->nullable();` — array of {dimension, score, is_custom}
    - `$table->text('hypothesis')->nullable();`
    - `$table->text('outcome_narrative')->nullable();`
    - `$table->string('verdict')->nullable();` — 'worked' | 'didnt_work' | 'inconclusive'
    - `$table->json('change_rows')->nullable();` — array of {what_changed, expected_effect, actual_effect}
    - `$table->timestamps();`
    - The `down()` method drops the table.

    **database/migrations/2026_05_17_000012_create_recipe_test_photos_table.php** — `Schema::create('recipe_test_photos', ...)`:
    - `$table->id();`
    - `$table->foreignId('recipe_test_id')->constrained('recipe_tests')->cascadeOnDelete();`
    - `$table->string('path');` — relative path within disk, e.g. 'recipe-tests/5/abc.jpg'
    - `$table->unsignedSmallInteger('order')->default(0);`
    - `$table->timestamps();`
    - The `down()` method drops the table.

    Create **app/Enums/TestType.php** via `php artisan make:enum TestType --string --no-interaction` (or hand-write if the command is unavailable):
    ```php
    enum TestType: string {
        case Trial = 'trial';
        case Experiment = 'experiment';
        public function label(): string {
            return match ($this) {
                self::Trial => 'Trial Run',
                self::Experiment => 'Experiment',
            };
        }
    }
    ```

    Create **app/Enums/TestVerdict.php**:
    ```php
    enum TestVerdict: string {
        case Worked = 'worked';
        case DidntWork = 'didnt_work';
        case Inconclusive = 'inconclusive';
        public function label(): string {
            return match ($this) {
                self::Worked => 'Worked',
                self::DidntWork => "Didn't work",
                self::Inconclusive => 'Inconclusive',
            };
        }
    }
    ```

    Run `php artisan migrate --no-interaction`. Then run `php artisan storage:link` once — this creates the `public/storage` symlink required for the photo-upload feature in plan 04-03. If the symlink already exists the command is a safe no-op.

    Run `vendor/bin/pint --dirty --format agent` after creating the PHP files.
  </action>
  <verify>
    <automated>php artisan migrate:status | grep -E "recipe_tests|recipe_test_photos"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate:status` lists both `2026_05_17_000011_create_recipe_tests_table` and `2026_05_17_000012_create_recipe_test_photos_table` as Ran
    - `database/migrations/2026_05_17_000011_create_recipe_tests_table.php` contains `Schema::create('recipe_tests'` and `unsignedTinyInteger('overall_rating')` and `json('change_rows')`
    - `database/migrations/2026_05_17_000012_create_recipe_test_photos_table.php` contains `Schema::create('recipe_test_photos'` and `constrained('recipe_tests')->cascadeOnDelete()`
    - `app/Enums/TestType.php` contains `enum TestType: string` and cases `Trial = 'trial'` and `Experiment = 'experiment'`
    - `app/Enums/TestVerdict.php` contains `enum TestVerdict: string` and cases `Worked = 'worked'`, `DidntWork = 'didnt_work'`, `Inconclusive = 'inconclusive'`
    - `public/storage` exists as a symlink (or junction on Windows) pointing into `storage/app/public`
  </acceptance_criteria>
  <done>Both tables exist in the DB, both enums exist with label() methods, and the storage symlink is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create RecipeTest + RecipeTestPhoto models, factories, and the Recipe relations</name>
  <read_first>
    - app/Models/RecipeVersion.php (model casts() + relation style to mirror)
    - app/Models/Recipe.php (the file being modified — current relations block)
    - database/factories/RecipeVersionFactory.php (factory style to mirror)
    - database/factories/RecipeFactory.php (factory style to mirror)
    - app/Enums/TestType.php (created in Task 1 — referenced by model casts)
    - app/Enums/TestVerdict.php (created in Task 1 — referenced by model casts)
  </read_first>
  <action>
    Create **app/Models/RecipeTest.php** via `php artisan make:model RecipeTest --no-interaction` then edit it:
    - `use HasFactory;` with the `/** @use HasFactory<RecipeTestFactory> */` docblock.
    - `protected $fillable = ['recipe_id', 'recipe_version_id', 'user_id', 'type', 'tested_at', 'tasting_notes', 'overall_rating', 'ratings', 'hypothesis', 'outcome_narrative', 'verdict', 'change_rows'];`
    - `casts()` method returning:
      ```php
      'type'           => TestType::class,
      'verdict'        => TestVerdict::class,
      'ratings'        => 'array',
      'change_rows'    => 'array',
      'tested_at'      => 'datetime',
      'overall_rating' => 'integer',
      ```
    - Relations:
      - `recipe(): BelongsTo` → `belongsTo(Recipe::class)`
      - `recipeVersion(): BelongsTo` → `belongsTo(RecipeVersion::class)`
      - `user(): BelongsTo` → `belongsTo(User::class)`
      - `photos(): HasMany` → `hasMany(RecipeTestPhoto::class)->orderBy('order')`

    Create **app/Models/RecipeTestPhoto.php** via `php artisan make:model RecipeTestPhoto --no-interaction` then edit it:
    - `use HasFactory;` with `/** @use HasFactory<RecipeTestPhotoFactory> */`.
    - `protected $fillable = ['recipe_test_id', 'path', 'order'];`
    - `casts()` returning `'order' => 'integer'`.
    - Relation `recipeTest(): BelongsTo` → `belongsTo(RecipeTest::class)`.
    - An accessor `url(): Attribute` that returns `Storage::disk(config('filesystems.default', 'public'))->url($this->path)` — import `Illuminate\Database\Eloquent\Casts\Attribute` and `Illuminate\Support\Facades\Storage`. Add `'url'` to `protected $appends = ['url'];` so the resolved URL is always serialized to the frontend.

    Edit **app/Models/Recipe.php** — add two relations after the existing `draft()` relation:
    - `tests(): HasMany` → `hasMany(RecipeTest::class)`
    - `latestTest(): HasOne` → `hasOne(RecipeTest::class)->latestOfMany('tested_at')`
    Add the `RecipeTest` import. `HasOne` is already imported.

    Create **database/factories/RecipeTestFactory.php** (`@extends Factory<RecipeTest>`):
    - `definition()` returns a trial-shaped test:
      ```php
      'recipe_id'         => Recipe::factory(),
      'recipe_version_id' => RecipeVersion::factory(),
      'user_id'           => User::factory(),
      'type'              => TestType::Trial,
      'tested_at'         => now(),
      'tasting_notes'     => $this->faker->sentence(),
      'overall_rating'    => $this->faker->numberBetween(1, 10),
      'ratings'           => [['dimension' => 'Taste', 'score' => 7, 'is_custom' => false]],
      'hypothesis'        => null,
      'outcome_narrative' => null,
      'verdict'           => null,
      'change_rows'       => null,
      ```
    - Add a state `experiment()` that sets `type => TestType::Experiment`, `hypothesis => $this->faker->sentence()`, `outcome_narrative => $this->faker->sentence()`, `verdict => TestVerdict::Worked`, and `change_rows => [['what_changed' => 'More salt', 'expected_effect' => 'Saltier', 'actual_effect' => 'Saltier']]`.

    Create **database/factories/RecipeTestPhotoFactory.php** (`@extends Factory<RecipeTestPhoto>`):
    - `definition()` returns `['recipe_test_id' => RecipeTest::factory(), 'path' => 'recipe-tests/1/'.$this->faker->uuid().'.jpg', 'order' => 0]`.

    Run `vendor/bin/pint --dirty --format agent` after creating the files.
  </action>
  <verify>
    <automated>php artisan tinker --execute 'echo App\Models\RecipeTest::factory()->make()->type->value; echo App\Models\Recipe::factory()->create()->tests()->count();'</automated>
  </verify>
  <acceptance_criteria>
    - `app/Models/RecipeTest.php` contains `class RecipeTest extends Model`, `TestType::class`, `TestVerdict::class`, and `hasMany(RecipeTestPhoto::class)`
    - `app/Models/RecipeTest.php` contains `belongsTo(RecipeVersion::class)` and `belongsTo(Recipe::class)`
    - `app/Models/RecipeTestPhoto.php` contains `class RecipeTestPhoto extends Model`, `protected $appends = ['url']`, and `Storage::disk(config('filesystems.default'`
    - `app/Models/Recipe.php` contains `hasMany(RecipeTest::class)` and `hasOne(RecipeTest::class)->latestOfMany('tested_at')`
    - `database/factories/RecipeTestFactory.php` contains `class RecipeTestFactory extends Factory` and a `public function experiment()` state method
    - `database/factories/RecipeTestPhotoFactory.php` contains `class RecipeTestPhotoFactory extends Factory`
    - The tinker verify command prints `trial` followed by `0` with no errors
  </acceptance_criteria>
  <done>Both models exist with correct casts/relations/url accessor, Recipe has tests()/latestTest(), and both factories build valid instances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the Wave 0 RecipeTestTest feature suite (RED)</name>
  <read_first>
    - tests/Feature/Recipes/RecipeCrudTest.php (Pest feature-test style, RolesAndPermissionsSeeder beforeEach, actingAs, assertRedirect)
    - tests/Feature/Recipes/RecipeVersionTest.php (second feature-test reference)
    - .planning/phases/04-recipe-tests/04-VALIDATION.md (the Phase Requirements → Test Map this suite must cover)
    - app/Models/RecipeTest.php (model fillable/casts the assertions reference)
  </read_first>
  <behavior>
    The suite covers every row of the VALIDATION.md test map. It is expected RED until plan 04-02 ships the routes/controller. Each test seeds RolesAndPermissionsSeeder, creates an owner User assigned the 'User' role, creates a Recipe owned by that user with at least one RecipeVersion.
    - Test (TEST-01): `POST /recipes/{recipe}/tests` with `type=trial`, `recipe_version_id`, `tested_at`, `overall_rating` → asserts redirect and a `recipe_tests` row exists linked to that recipe_version_id with type 'trial'.
    - Test (TEST-01): `GET /recipes/{recipe}/tests` as the owner → asserts a 200 Inertia response rendering component `recipes/tests/index`.
    - Test (TEST-02): `POST` with `type=experiment` plus `hypothesis`, `outcome_narrative`, `verdict=worked` → asserts the row stores hypothesis, outcome_narrative, and verdict.
    - Test (TEST-02): `POST` with `type=experiment` and NO `hypothesis` → asserts a 422 / session validation error on `hypothesis`.
    - Test (TEST-03): `POST` stores `tasting_notes` and `overall_rating` → asserts the persisted values.
    - Test (TEST-03): `POST` with a `ratings` array of per-dimension `{dimension, score, is_custom}` → asserts `ratings` casts back to an array with those keys.
    - Test (TEST-03): `POST` with `photos` built via `UploadedFile::fake()->image()` and `Storage::fake()` → asserts a file is stored on the faked disk and a `recipe_test_photos` row is created.
    - Test (TEST-04): `POST` with `type=experiment` and a `change_rows` array → asserts `change_rows` casts to an array.
    - Test (TEST-04): asserts each stored change row has the keys `what_changed`, `expected_effect`, `actual_effect`.
    - Test (auth): a non-owner User cannot `GET /recipes/{recipe}/tests` for another user's recipe → asserts 403.
    - Test (auth): a non-owner cannot `DELETE` another user's test → asserts 403.
    - Test (CRUD): `DELETE /recipes/{recipe}/tests/{test}` by the owner removes the test and cascade-deletes its `recipe_test_photos` rows.
    - Test (CRUD): `PUT /recipes/{recipe}/tests/{test}` by the owner updates `tasting_notes` and `overall_rating`.
  </behavior>
  <action>
    Create the feature test file via `php artisan make:test --pest RecipeTestTest --no-interaction` — this generates `tests/Feature/RecipeTestTest.php`. Move it to **tests/Feature/Recipes/RecipeTestTest.php** and update its namespace/location to match the existing `tests/Feature/Recipes/` files (Pest files have no namespace; just place the file in the folder).

    Write real Pest test cases — one `test('...', function () { ... })` per behavior bullet above. Follow the conventions established by `tests/Feature/Recipes/RecipeCrudTest.php`:
    - `beforeEach(fn () => $this->seed(RolesAndPermissionsSeeder::class));`
    - Build the owner with `User::factory()->create()` then `->assignRole('User')`.
    - Build the recipe + version with the factories from Task 2: `Recipe::factory()->for($owner, 'user')->create()` and `RecipeVersion::factory()->for($recipe)->create(['version_number' => 1])`.
    - For photo tests use `Storage::fake(config('filesystems.default', 'public'))` and `UploadedFile::fake()->image('shot.jpg', 800, 600)`. Assert with `Storage::disk(...)->assertExists(...)` is acceptable, but prefer asserting a `RecipeTestPhoto` row exists and its `path` is non-empty.
    - For Inertia component assertions use `$response->assertInertia(fn (Assert $page) => $page->component('recipes/tests/index'))` — import `Inertia\Testing\AssertableInertia as Assert`.
    - Use a top-level docblock comment: `/** Covers TEST-01, TEST-02, TEST-03, TEST-04. RED until plan 04-02 ships the routes + controller. */`

    Write REAL assertions — do NOT use `skip()` or `markTestIncomplete()`. The suite is expected to be RED (failing) because the routes do not exist yet; that is the correct Wave 0 state and gives plan 04-02 a GREEN target.

    Run `vendor/bin/pint --dirty --format agent`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=RecipeTestTest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Recipes/RecipeTestTest.php` exists and contains `RolesAndPermissionsSeeder`
    - The file contains at least 12 `test(` calls (one per behavior bullet)
    - The file contains `UploadedFile::fake()` and `Storage::fake(`
    - The file contains `recipes/tests/index` (Inertia component assertion)
    - The file contains NO `skip(` and NO `markTestIncomplete`
    - `php artisan test --compact --filter=RecipeTestTest` runs without PHP parse/fatal errors (tests are RED — failing assertions or missing-route 404s are expected and acceptable at this stage)
  </acceptance_criteria>
  <done>The RecipeTestTest suite exists with real assertions covering TEST-01..04, executes without parse errors, and is RED — giving plan 04-02 a concrete GREEN target.</done>
</task>

</tasks>

<verification>
- `php artisan migrate:status` shows both new migrations as Ran.
- `php artisan test --compact` executes the full suite without parse/fatal errors; RecipeTestTest is RED.
- `public/storage` symlink exists.
- `vendor/bin/pint --test --format agent` reports no style violations in the new files (informational — do not run with --test per CLAUDE.md; pint --dirty was already run).
</verification>

<success_criteria>
- recipe_tests and recipe_test_photos tables exist with all documented columns.
- RecipeTest model casts type/verdict to enums, ratings/change_rows to arrays, and exposes recipe/recipeVersion/user/photos relations.
- RecipeTestPhoto exposes a serialized `url` accessor.
- Recipe has tests() and latestTest() relations.
- RecipeTestFactory (with experiment() state) and RecipeTestPhotoFactory build valid instances.
- RecipeTestTest covers TEST-01..04 and is RED, executing without parse errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-recipe-tests/04-01-SUMMARY.md`
</output>
