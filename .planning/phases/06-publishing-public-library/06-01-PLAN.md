---
phase: 06-publishing-public-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_18_000001_add_publish_columns_to_recipes_table.php
  - app/Models/Recipe.php
  - app/Policies/RecipePolicy.php
  - tests/Feature/Library/PublishRecipeTest.php
  - tests/Feature/Library/LibraryBrowseTest.php
  - resources/js/pages/library/index.tsx
  - resources/js/pages/library/show.tsx
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-04]

must_haves:
  truths:
    - "A new recipe has is_published = false until the owner publishes it"
    - "The recipes table has is_published, published_version_id, published_at columns"
    - "RecipePolicy::view returns true for any viewer (including guests) on a published recipe"
    - "RecipePolicy::delete returns false while a recipe is published"
    - "PublishRecipeTest and LibraryBrowseTest exist with real assertions and run RED (routes/controllers not yet built)"
    - "library/index.tsx and library/show.tsx stub pages exist so Vite manifest resolves the library routes"
  artifacts:
    - path: "database/migrations/2026_05_18_000001_add_publish_columns_to_recipes_table.php"
      provides: "Publish-state columns on recipes table"
      contains: "is_published"
    - path: "app/Policies/RecipePolicy.php"
      provides: "Guest-viewable published recipes, delete blocked while published"
      contains: "?User $user"
    - path: "tests/Feature/Library/PublishRecipeTest.php"
      provides: "Red-test scaffold for PUB-01, PUB-02, PUB-03"
      min_lines: 60
    - path: "tests/Feature/Library/LibraryBrowseTest.php"
      provides: "Red-test scaffold for PUB-04"
      min_lines: 60
    - path: "resources/js/pages/library/index.tsx"
      provides: "Stub library index page"
    - path: "resources/js/pages/library/show.tsx"
      provides: "Stub public recipe page"
  key_links:
    - from: "app/Models/Recipe.php"
      to: "recipe_versions table"
      via: "publishedVersion belongsTo relation"
      pattern: "publishedVersion"
    - from: "app/Policies/RecipePolicy.php"
      to: "recipes.is_published"
      via: "view/delete gate on publish state"
      pattern: "is_published"
---

<objective>
Lay the publish-state foundation for Phase 6: add the three publish columns to the
`recipes` table, extend `RecipePolicy` so guests can view published recipes and
deletion is blocked while a recipe is published, and write the Wave 0 RED test
scaffold for all four PUB requirements.

Purpose: PUB-02/03/04 backend (Plan 02) cannot be built until the `is_published`
column exists, the policy allows guest viewing, and concrete RED tests give the
backend a GREEN target. Stub pages must exist before the build so Inertia route
tests do not fail on Vite manifest lookup (established Phase 4 lesson).
Output: One migration, an updated `Recipe` model + `RecipePolicy`, two RED Pest
feature test files, and two stub Inertia pages.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-publishing-public-library/06-CONTEXT.md
@.planning/phases/06-publishing-public-library/06-RESEARCH.md

# Files this plan modifies / patterns to replicate
@app/Models/Recipe.php
@app/Policies/RecipePolicy.php
@database/migrations/2026_05_17_000003_create_recipes_table.php
@database/migrations/2026_05_17_000007_create_recipe_versions_table.php

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

Recipe model (app/Models/Recipe.php) — current fillable includes:
  user_id, name, slug, hero_image_path, yield_amount, yield_unit_id, portions,
  portion_size_g, prep_time_minutes, cook_time_minutes, difficulty, cuisine_id,
  notes, current_version_id, selling_price
Existing relations: user(), cuisine(), tags(), sections(), ingredientLines(),
  steps(), versions(), currentVersion() [belongsTo RecipeVersion via current_version_id],
  draft(), tests(), latestTest(), conversation()
Recipe uses SoftDeletes.

RecipeVersion model (app/Models/RecipeVersion.php) — columns:
  recipe_id, version_number, committed_by, committed_at, change_note, snapshot (array),
  yield_g, cached_nutrition_json (array), cached_cost_per_gram, cached_cost_per_portion,
  cached_allergen_slugs (array), cached_selling_price.
  RecipeVersion has NO is_published — publish state lives on the Recipe.

RecipePolicy (app/Policies/RecipePolicy.php) — current methods, all owner-only,
  non-nullable User:
    view(User $user, Recipe $recipe): bool   -> $recipe->user_id === $user->id
    update(User $user, Recipe $recipe): bool -> $recipe->user_id === $user->id
    delete(User $user, Recipe $recipe): bool -> $recipe->user_id === $user->id

RecipeFactory exists. RecipeVersionFactory exists. UserFactory exists.
RecipeVersionService::commit(Recipe $recipe, ?string $changeNote, int $userId)
  commits the current draft as a new immutable RecipeVersion and updates
  recipes.current_version_id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add publish columns migration and extend the Recipe model</name>
  <files>database/migrations/2026_05_18_000001_add_publish_columns_to_recipes_table.php, app/Models/Recipe.php</files>
  <read_first>
    - database/migrations/2026_05_17_000003_create_recipes_table.php (column ordering, the `selling_price` column comes near the end)
    - database/migrations/2026_05_17_000007_create_recipe_versions_table.php (deferred-FK convention used in Phase 3)
    - app/Models/Recipe.php (fillable list, casts() method, relation method style)
  </read_first>
  <action>
    Create the migration with: `php artisan make:migration add_publish_columns_to_recipes_table --no-interaction`
    Rename the generated file (or accept the generated timestamp) so it sorts AFTER
    the existing recipe-domain migrations (timestamp prefix 2026_05_18_ or later).

    In the migration `up()`, on the `recipes` table add three columns:
      $table->boolean('is_published')->default(false)->after('selling_price');
      $table->unsignedBigInteger('published_version_id')->nullable()->after('is_published');
      $table->timestamp('published_at')->nullable()->after('published_version_id');
      $table->index('published_at');
    Do NOT add a foreign-key constraint on `published_version_id` — declare it as a
    plain `unsignedBigInteger` (this follows the Phase 3 deferred/nullable-FK
    convention; a hard FK is unnecessary because the column is nullable and the
    recipe_versions row is never hard-deleted).
    In `down()`, drop the index first (`$table->dropIndex(['published_at']);`) then
    `$table->dropColumn(['is_published', 'published_version_id', 'published_at']);`.

    Run the migration: `php artisan migrate --no-interaction`

    In app/Models/Recipe.php:
    - Add `'is_published'`, `'published_version_id'`, `'published_at'` to the
      `$fillable` array.
    - In the `casts()` method, add: `'is_published' => 'boolean'` and
      `'published_at' => 'datetime'`.
    - Add a new relation method after `currentVersion()`:
        public function publishedVersion(): BelongsTo
        {
            return $this->belongsTo(RecipeVersion::class, 'published_version_id');
        }
    Use the existing `BelongsTo` import (already present in the file).
  </action>
  <verify>
    <automated>php artisan migrate:status | grep -i publish_columns; php artisan tinker --execute 'echo (new \App\Models\Recipe)->isFillable("is_published") ? "OK" : "FAIL"; echo (new \App\Models\Recipe)->publishedVersion() instanceof \Illuminate\Database\Eloquent\Relations\BelongsTo ? " OK" : " FAIL";'</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate:status` shows the `add_publish_columns_to_recipes_table` migration as `Ran`
    - database-schema for the `recipes` table shows columns `is_published`, `published_version_id`, `published_at`
    - grep `is_published` in app/Models/Recipe.php returns a match inside `$fillable`
    - grep `'is_published' => 'boolean'` in app/Models/Recipe.php returns a match
    - grep `public function publishedVersion` in app/Models/Recipe.php returns a match
    - The tinker command above prints `OK OK`
  </acceptance_criteria>
  <done>
    The `recipes` table has `is_published` (default false), `published_version_id`
    (nullable), `published_at` (nullable, indexed). The `Recipe` model is fillable on
    all three, casts them correctly, and exposes a `publishedVersion()` belongsTo
    relation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend RecipePolicy for guest viewing and publish-blocked deletion</name>
  <files>app/Policies/RecipePolicy.php</files>
  <read_first>
    - app/Policies/RecipePolicy.php (current three owner-only methods)
    - app/Models/User.php (confirm namespace App\Models\User)
  </read_first>
  <action>
    Edit app/Policies/RecipePolicy.php. Make exactly two changes — do NOT touch the
    `update` method (publishing/unpublishing is an owner-only `update` action and the
    existing owner-only check is correct).

    Change 1 — `view` becomes guest-aware. Replace the `view` method with:
      /**
       * Determine whether the viewer can see the recipe.
       *
       * A published recipe is visible to anyone, including guests. A private
       * recipe is visible only to its owner.
       */
      public function view(?User $user, Recipe $recipe): bool
      {
          if ($recipe->is_published) {
              return true;
          }

          return $user !== null && $recipe->user_id === $user->id;
      }
    The `?User $user` nullable type hint is required — with a non-nullable `User`,
    Laravel skips the policy for guest requests and denies access (RESEARCH Pitfall 1).

    Change 2 — `delete` is blocked while published. Replace the `delete` method with:
      /**
       * Determine whether the user can delete the recipe.
       *
       * Deletion is refused while a recipe is published — the owner must
       * unpublish it first.
       */
      public function delete(User $user, Recipe $recipe): bool
      {
          if ($recipe->is_published) {
              return false;
          }

          return $recipe->user_id === $user->id;
      }
    Keep `delete`'s `User $user` non-nullable — only an authenticated owner ever
    reaches this method.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent && php artisan test --compact --filter=RecipePolicy 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep `public function view(?User \$user, Recipe \$recipe): bool` in app/Policies/RecipePolicy.php returns a match
    - grep `if (\$recipe->is_published)` in app/Policies/RecipePolicy.php returns at least 2 matches (one in view, one in delete)
    - grep `public function delete(User \$user, Recipe \$recipe): bool` in app/Policies/RecipePolicy.php returns a match (delete user stays non-nullable)
    - The `update` method is unchanged: grep `public function update(User \$user, Recipe \$recipe): bool` still returns a match
    - `vendor/bin/pint --dirty` reports no style errors
  </acceptance_criteria>
  <done>
    `RecipePolicy::view` allows any viewer (guest or authenticated) to see a
    published recipe and restricts private recipes to their owner. `RecipePolicy::delete`
    returns false while the recipe is published. The `update` method is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 RED test scaffold and library stub pages</name>
  <files>tests/Feature/Library/PublishRecipeTest.php, tests/Feature/Library/LibraryBrowseTest.php, resources/js/pages/library/index.tsx, resources/js/pages/library/show.tsx</files>
  <read_first>
    - .planning/phases/06-publishing-public-library/06-RESEARCH.md (the "Phase Requirements -> Test Map" table — the 12 behaviors to cover)
    - tests/Feature/Recipes/RecipeControllerTest.php (Pest feature-test style: `uses(RefreshDatabase::class)`, factory usage, `actingAs`, route() helpers) — if this exact path differs, find an existing recipe feature test under tests/Feature/ to mirror
    - database/factories/RecipeFactory.php and database/factories/RecipeVersionFactory.php (available states)
    - resources/js/pages/recipes/index.tsx (stub-page export shape and `.layout` static property pattern)
  </read_first>
  <behavior>
    PublishRecipeTest.php — write real assertions (no skip/markTestIncomplete):
    - PUB-01: a freshly created recipe has `is_published === false` in the database.
    - PUB-01: a guest GET to `/recipes/{id}` (the owner route) for a PRIVATE recipe
      is redirected/forbidden (not 200).
    - PUB-02: the owner POSTs `route('recipes.publish', $recipe)` with a valid
      `version_id` → `is_published` becomes true, `published_version_id` equals the
      posted version, `published_at` is set.
    - PUB-02: publish is rejected (422 / validation error) when the chosen version's
      snapshot references a sub-recipe whose parent recipe is not published.
    - PUB-02: committing a NEW version after publishing does NOT change
      `published_version_id` (it stays pinned to the originally published version).
    - PUB-03: the owner DELETEs `route('recipes.unpublish', $recipe)` → `is_published`
      becomes false, `published_version_id` and `published_at` become null.
    - PUB-03: DELETE `route('recipes.destroy', $recipe)` while the recipe is
      published is refused (403) and the recipe is NOT soft-deleted.

    LibraryBrowseTest.php — write real assertions:
    - PUB-04: a guest GET `route('library.index')` returns 200 (no auth required).
    - PUB-04: the library index lists only `is_published = true` recipes — a private
      recipe of another user does not appear.
    - PUB-04: the library index respects each filter param — name search, tag,
      cuisine, allergen, difficulty, max_total_time (one assertion per filter).
    - PUB-04: a guest GET `route('library.show', $publishedRecipe->slug)` returns 200.
    - PUB-04: the public recipe page payload omits cost data — assert the Inertia
      props for `library/show` do NOT contain `cost_per_portion`, `selling_price`,
      `notes`, or any tests/conversation key.
    - PUB-04: GET `route('library.show', $unpublishedRecipe->slug)` returns 404.
  </behavior>
  <action>
    Create directory tests/Feature/Library/ and two Pest feature test files:
      tests/Feature/Library/PublishRecipeTest.php
      tests/Feature/Library/LibraryBrowseTest.php
    Use `php artisan make:test --pest Library/PublishRecipeTest` and
    `php artisan make:test --pest Library/LibraryBrowseTest` to generate the
    skeletons, then write the assertions described in <behavior> above. Each test
    file must `uses(RefreshDatabase::class)`.

    Write REAL assertions — do NOT use `skip()`, `markTestIncomplete()`, or empty
    bodies. The tests reference `route('recipes.publish', ...)`,
    `route('recipes.unpublish', ...)`, `route('library.index')`, and
    `route('library.show', ...)` which do not exist yet — that is correct; these
    tests are expected to FAIL RED until Plan 02 builds the routes/controllers.
    For the cost-redaction assertion use Inertia's `assertInertia` with
    `->missing('recipe.cost_per_portion')` style assertions, or assert the JSON
    props array does not contain those keys.

    For sub-recipe setup in the sub-recipe-prerequisite test: build a parent recipe
    whose chosen version `snapshot` has a section line with a
    `sub_recipe_version_id` pointing to a RecipeVersion of an UNPUBLISHED recipe.

    Create two stub Inertia pages so the library routes (added in Plan 02) resolve
    in the Vite manifest (Phase 4 lesson — Inertia route tests trigger a manifest
    lookup):
      resources/js/pages/library/index.tsx
      resources/js/pages/library/show.tsx
    Each stub is a minimal default-export React component returning a single
    placeholder div with the page name, e.g.:
      export default function LibraryIndex() {
          return <div>Library</div>;
      }
    No props typing, no layout needed yet — Plan 03 replaces these stubs entirely.

    Run `npm run build` after creating the stubs so the manifest includes them.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && php artisan test --compact --filter=Library 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - File tests/Feature/Library/PublishRecipeTest.php exists and is >= 60 lines
    - File tests/Feature/Library/LibraryBrowseTest.php exists and is >= 60 lines
    - grep `skip(` and `markTestIncomplete` across both test files returns NO matches
    - grep `route('recipes.publish'` in PublishRecipeTest.php returns a match
    - grep `route('library.index'` in LibraryBrowseTest.php returns a match
    - File resources/js/pages/library/index.tsx exists with a default export
    - File resources/js/pages/library/show.tsx exists with a default export
    - `npm run build` completes without a Vite error
    - `php artisan test --compact --filter=Library` runs the tests (they are RED — failing because routes are missing — which is the correct Wave 0 state; the command itself must execute without a fatal parse error)
  </acceptance_criteria>
  <done>
    Two Pest feature test files with real assertions covering all 12 PUB behaviors
    exist and execute (RED, as expected). Two library stub pages exist and the Vite
    build succeeds.
  </done>
</task>

</tasks>

<verification>
- `php artisan migrate:status` shows the publish-columns migration ran.
- `app/Models/Recipe.php` is fillable+cast on the three publish columns and has a
  `publishedVersion()` relation.
- `app/Policies/RecipePolicy.php` `view` uses `?User`, `delete` blocks while published,
  `update` unchanged.
- `tests/Feature/Library/PublishRecipeTest.php` and `LibraryBrowseTest.php` exist with
  real assertions and run RED.
- `resources/js/pages/library/index.tsx` and `show.tsx` stubs exist; `npm run build` passes.
</verification>

<success_criteria>
- The schema, model, and policy foundation for the publish lifecycle is in place.
- The Wave 0 RED test scaffold gives Plan 02 a concrete GREEN target for all four
  PUB requirements.
- The library stub pages prevent Vite manifest errors when Plan 02's routes are tested.
</success_criteria>

<output>
After completion, create `.planning/phases/06-publishing-public-library/06-01-SUMMARY.md`
</output>
