---
phase: 06-publishing-public-library
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - resources/js/components/recipes/publish-recipe-dialog.tsx
  - resources/js/components/recipes/unpublish-recipe-dialog.tsx
  - resources/js/pages/recipes/show.tsx
  - resources/js/components/recipes/recipe-card.tsx
  - resources/js/components/recipes/library-recipe-card.tsx
  - resources/js/pages/library/index.tsx
  - resources/js/pages/library/show.tsx
  - resources/js/layouts/guest-public-layout.tsx
  - resources/js/components/app-sidebar.tsx
  - resources/js/types/recipe.ts
  - lang/en/app.php
  - lang/el/app.php
  - app/Http/Resources/RecipeBuilderResource.php
  - app/Http/Resources/RecipeListResource.php
autonomous: false
requirements: [PUB-01, PUB-02, PUB-03, PUB-04]

must_haves:
  truths:
    - "An owner sees a Publish recipe control in the builder header and a Publish/Unpublish item in the My Recipes card menu"
    - "Publishing opens a version-picker dialog; the chosen version is sent to recipes.publish"
    - "A published recipe shows a Published badge and an Update to current cue when a newer version exists"
    - "The Delete card menu item is disabled (with tooltip) while a recipe is published"
    - "A user (or guest) can open /library, search and filter published recipes, and click through to a public recipe page"
    - "The public recipe page shows nutrition and allergens but no cost, notes, tests, or AI"
  artifacts:
    - path: "resources/js/components/recipes/publish-recipe-dialog.tsx"
      provides: "Version-picker publish dialog"
      min_lines: 50
    - path: "resources/js/pages/library/index.tsx"
      provides: "Public library browse grid (replaces the Plan 01 stub)"
      min_lines: 80
    - path: "resources/js/pages/library/show.tsx"
      provides: "Dedicated public recipe page (replaces the Plan 01 stub)"
      min_lines: 80
    - path: "resources/js/components/recipes/library-recipe-card.tsx"
      provides: "Library card with author name, links to library.show"
      min_lines: 30
    - path: "resources/js/layouts/guest-public-layout.tsx"
      provides: "Minimal guest-safe layout for public pages"
      min_lines: 20
  key_links:
    - from: "resources/js/components/recipes/library-recipe-card.tsx"
      to: "library.show route"
      via: "Wayfinder libraryShow({slug}).url Link href"
      pattern: "libraryShow|library/"
    - from: "resources/js/components/recipes/publish-recipe-dialog.tsx"
      to: "recipes.publish route"
      via: "router.post with version_id"
      pattern: "publish"
    - from: "resources/js/pages/library/index.tsx"
      to: "library.index controller props"
      via: "router.reload only:['recipes'] debounced search"
      pattern: "router.reload"
---

<objective>
Build the four Phase 6 UI surfaces per the approved 06-UI-SPEC.md design contract:
the publish/unpublish controls in the recipe builder and My Recipes card menu, the
public library browse page, and the dedicated public recipe page — with a
guest-safe layout, a new "Library" nav entry, and full EN/EL copy. A human-verify
checkpoint closes the phase.

Purpose: delivers the user-facing half of PUB-01..PUB-04 — the visible publish
lifecycle and the public browse/read experience.
Output: two dialog components, a library card component, two rebuilt library
pages, a guest layout, an updated builder + card + sidebar, TS types, EN/EL
translations, two resource updates, and a verified end-to-end flow.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-publishing-public-library/06-CONTEXT.md
@.planning/phases/06-publishing-public-library/06-UI-SPEC.md
@.planning/phases/06-publishing-public-library/06-02-SUMMARY.md

# Patterns to replicate / files this plan touches
@resources/js/pages/recipes/index.tsx
@resources/js/components/recipes/recipe-card.tsx
@resources/js/components/app-sidebar.tsx
@resources/js/components/nav-user.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

Wayfinder helpers (regenerated in Plan 02 — import these, do NOT hand-write URLs):
  import { index as libraryIndex, show as libraryShow } from '@/routes/library';
    libraryIndex().url -> '/library'
    libraryShow({ slug }).url -> '/library/{slug}'
  Publish action helpers (under @/actions/App/Http/Controllers/Recipes/PublishRecipeController):
    store -> POST recipes/{recipe}/publish   (body: { version_id })
    destroy -> DELETE recipes/{recipe}/publish

recipes/index.tsx pattern (mirror for library/index.tsx):
  - useState per filter, 300ms debounced search via debounceTimerRef + setTimeout.
  - reloadWithParams calls router.reload({ data:{...}, only:['recipes'],
    preserveState:true, replace:true, onFinish:... }).
  - RecipeFilters component: <RecipeFilters filters={currentFilters}
    cuisines={} allergens={} tags={} onFiltersChange={} />. RecipeFiltersState
    interface has: search, tag, cuisine, allergen, ingredient, difficulty,
    max_total_time. For the library, pass the same state shape but DO NOT
    surface/forward the `ingredient` value (UI-SPEC: ingredient filter excluded).
  - Pagination block: identical PaginationContent/Item/Link usage.
  - Skeleton grid: 6 skeletons, grid-cols-1 md:grid-cols-2 lg:grid-cols-3, gap-6,
    aria-busy on the wrapper.

RecipeCard (resources/js/components/recipes/recipe-card.tsx):
  - Props { recipe: RecipeCardData, className? }.
  - Wrapped in <Link href={recipesShow(recipe).url}> -> the OWNER route (auth).
    The library card MUST link to libraryShow instead.
  - Renders hero image (UtensilsCrossedIcon placeholder when no hero), name (h3
    20px semibold), cuisine Badge variant="secondary", time+difficulty metadata
    row, cost+calories metrics row, AllergenIcons.
  - The card today is a single <Link> wrapper with NO DropdownMenu — this plan
    adds a card actions menu (UI-SPEC Surface 1).

RecipeCardData TS type (resources/js/types/recipe.ts): id, name, slug,
  hero_image_path, cuisine, total_time, prep_time_minutes, cook_time_minutes,
  difficulty, cost_per_portion, calories_per_portion, allergen_slugs.

AppSidebar (resources/js/components/app-sidebar.tsx): mainNavItems array — add
  the library entry { title: t('app.library.nav'), href: libraryIndex().url,
  icon: Globe } after the recipes entry. Globe from lucide-react.

NavUser already returns null when auth.user is null — AppSidebarLayout does NOT
  crash for guests, but the sidebar still renders auth-only nav. For the public
  pages use a dedicated minimal GuestPublicLayout instead (UI-SPEC Surface 3).

show.tsx builder header (resources/js/pages/recipes/show.tsx, ~line 549):
  The header is a `<div className="flex items-center gap-4 border-b ...">`
  containing the name Input, auto-save indicator, AiChatSheet, VersionHistorySheet,
  Save Version Button, Recall Button, and the actions DropdownMenu (Duplicate +
  Delete items). The recipe prop comes from RecipeBuilderResource; the `can` prop
  has { update, delete }. Builder holds the draft in local React state; `versions`
  prop is an array of { id, version_number, change_note, created_at, is_current }.

RecipeBuilderResource (app/Http/Resources/RecipeBuilderResource.php) — toArray
  currently emits id, name, slug, hero_image_path, yield_amount, portions,
  prep/cook times, difficulty, cuisine_id, cuisine, notes, selling_price,
  current_version_number, edit_sequence, tags, sections, steps. It does NOT emit
  is_published / published_version_id / current_version_id today — this plan adds
  them.

UI-SPEC copy contract — EN/EL strings to add (see 06-UI-SPEC.md "Copywriting
  Contract" table — Task 2 copies ALL rows verbatim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish/unpublish controls — dialogs, builder header, My Recipes card menu</name>
  <files>resources/js/components/recipes/publish-recipe-dialog.tsx, resources/js/components/recipes/unpublish-recipe-dialog.tsx, resources/js/pages/recipes/show.tsx, resources/js/components/recipes/recipe-card.tsx, resources/js/types/recipe.ts, app/Http/Resources/RecipeBuilderResource.php, app/Http/Resources/RecipeListResource.php</files>
  <read_first>
    - resources/js/pages/recipes/show.tsx (the full builder header region ~line 544-643 — the action group and the Duplicate/Delete DropdownMenu)
    - resources/js/components/recipes/recipe-card.tsx (the card body — where the Published badge and a card menu go; the card has NO DropdownMenu today, add one)
    - resources/js/components/recipes/recipe-builder/save-version-dialog.tsx (Dialog component composition pattern — DialogContent/Header/Title/Description/Footer, controlled open state)
    - resources/js/types/recipe.ts (RecipeCardData and the builder recipe/version types — extend them for publish state)
    - app/Http/Resources/RecipeBuilderResource.php and app/Http/Resources/RecipeListResource.php (the toArray output to add publish keys to)
    - .planning/phases/06-publishing-public-library/06-UI-SPEC.md (Surface 1, Surface 2, and the "Publish Flow — Interaction Contracts" section — exact dialog structure, button variants, badge placement)
  </read_first>
  <action>
    Backend resource keys: confirm the frontend receives the publish state. In
    app/Http/Resources/RecipeBuilderResource.php add to the `toArray` output:
    `'is_published' => (bool) $this->is_published`,
    `'published_version_id' => $this->published_version_id`, and
    `'current_version_id' => $this->current_version_id`. In
    app/Http/Resources/RecipeListResource.php add
    `'is_published' => (bool) $this->is_published`. Do not remove or rename any
    existing keys in either resource.

    Extend TS types in resources/js/types/recipe.ts:
    - Add to RecipeCardData: `is_published: boolean;`
    - Add to the builder recipe type (the type backing RecipeBuilderResource —
      `RecipeBuilderData` or equivalent): `is_published: boolean;`,
      `published_version_id: number | null;`, `current_version_id: number | null;`.

    Create resources/js/components/recipes/publish-recipe-dialog.tsx — the version
    picker dialog (UI-SPEC "Initial Publish"):
    - Props: { recipeId: number; versions: Array<{id:number; version_number:number;
      created_at:string}>; subRecipeError?: string[]; open: boolean;
      onOpenChange:(v:boolean)=>void }.
    - shadcn `Dialog` with DialogTitle = t('app.recipes.publish_dialog_title'),
      DialogDescription = t('app.recipes.publish_dialog_body').
    - shadcn `Select` listing committed versions, label "v{N} — saved {date}",
      default selection = the latest (highest version_number).
    - When `subRecipeError` is non-empty, render a destructive `Alert` listing the
      sub-recipe names and disable the confirm button.
    - Footer: "Don't publish" Button (variant="ghost", text
      t('app.recipes.publish_dialog_dismiss'), calls onOpenChange(false)) and
      "Publish recipe" Button (variant="default", text
      t('app.recipes.publish_dialog_confirm')) which does
      `router.post(publishStore({ recipe: recipeId }).url, { version_id: selected },
      { preserveScroll: true, onSuccess: () => { onOpenChange(false);
      toast.success(t('app.recipes.publish_toast')); } })`.
      Import the Wayfinder publish `store` action helper as `publishStore`.

    Create resources/js/components/recipes/unpublish-recipe-dialog.tsx — the
    confirmation dialog (UI-SPEC "Unpublish"):
    - Props: { recipeId:number; open:boolean; onOpenChange:(v:boolean)=>void }.
    - DialogTitle = t('app.recipes.unpublish_dialog_title'), DialogDescription =
      t('app.recipes.unpublish_dialog_body').
    - Footer: "Keep published" Button (variant="ghost", text
      t('app.recipes.unpublish_dialog_dismiss')) and "Unpublish recipe" Button
      (variant="destructive", text t('app.recipes.unpublish_dialog_confirm'))
      which does `router.delete(publishDestroy({ recipe: recipeId }).url,
      { preserveScroll: true, onSuccess: () => onOpenChange(false) })`.

    Edit resources/js/pages/recipes/show.tsx — add publish controls to the builder
    header action group (the `flex items-center gap-4` header div). Per UI-SPEC
    Surface 2:
    - When `!recipe.is_published`: render a "Publish recipe" Button
      (variant="outline", size="sm", text t('app.recipes.publish_btn')) that opens
      PublishRecipeDialog (manage open state with useState).
    - When `recipe.is_published`: render a "Published" Badge (variant="secondary"
      with a `Globe` icon prefix, text t('app.recipes.published_badge')) + an
      "Unpublish recipe" Button (variant="ghost", size="sm",
      text t('app.recipes.unpublish_btn')) that opens UnpublishRecipeDialog.
    - When `recipe.is_published && recipe.published_version_id !==
      recipe.current_version_id`: render an "Update to current" cue banner
      directly BELOW the header action row — `<div className="bg-muted rounded-md
      px-4 py-3 ...">` with text t('app.recipes.update_public_version_cue') and an
      "Update to current" Button (variant="outline", size="sm", text
      t('app.recipes.update_public_version_btn')) that does a one-click
      `router.post(publishStore({ recipe: recipe.id }).url, { version_id:
      recipe.current_version_id }, { preserveScroll: true, onSuccess: () =>
      toast.success(t('app.recipes.update_version_toast')) })` — NO confirmation
      dialog (intentional asymmetry per CONTEXT.md).
    - Wire the PublishRecipeDialog `versions` prop from the existing `versions`
      page prop. Use `flex items-center gap-2` so the existing header layout does
      not reflow.

    Edit resources/js/components/recipes/recipe-card.tsx — per UI-SPEC Surface 1:
    - When `recipe.is_published`: render a "Published" Badge (variant="secondary"
      with a `Globe` icon prefix, text t('app.recipes.published_badge')) in the
      card body, on its own line between the recipe name and the cuisine badge.
      `whitespace-nowrap` so it does not wrap.
    - Add a card actions `DropdownMenu` (the card has none today) with: a
      "Publish recipe" item (when not published) OR an "Unpublish recipe" item
      (when published), and a "Delete" item rendered DISABLED (aria-disabled="true",
      `cursor-not-allowed`, wrapped in a Tooltip reading
      t('app.recipes.delete_blocked_tooltip')) when `recipe.is_published` is true.
      Do NOT use the native `disabled` attribute on a non-button element. The
      publish item opens PublishRecipeDialog; the unpublish item opens
      UnpublishRecipeDialog — manage their open state with useState in the card.
      The whole card is a `<Link>`: the DropdownMenu trigger Button must call
      `e.preventDefault(); e.stopPropagation();` on click so opening the menu does
      not navigate.

    The translation keys referenced above are added in Task 2 — use these exact
    key paths so Task 2's lang entries match.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent && npm run build 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File resources/js/components/recipes/publish-recipe-dialog.tsx exists; grep `version_id` returns a match; grep `Select` returns a match
    - File resources/js/components/recipes/unpublish-recipe-dialog.tsx exists; grep `variant="destructive"` returns a match
    - grep `PublishRecipeDialog` in resources/js/pages/recipes/show.tsx returns a match
    - grep `published_version_id !== ` in resources/js/pages/recipes/show.tsx (the update-cue condition) returns a match
    - grep `is_published` in resources/js/components/recipes/recipe-card.tsx returns a match
    - grep `aria-disabled` in resources/js/components/recipes/recipe-card.tsx returns a match
    - grep `is_published` in resources/js/types/recipe.ts returns a match
    - grep `'is_published'` in app/Http/Resources/RecipeBuilderResource.php returns a match; grep `'current_version_id'` returns a match
    - grep `'is_published'` in app/Http/Resources/RecipeListResource.php returns a match
    - `npm run build` completes without error and `npx tsc --noEmit` reports no type errors
  </acceptance_criteria>
  <done>
    The builder header shows a Publish control, a Published badge, an Unpublish
    control, and a conditional update-version cue. The My Recipes card shows a
    Published badge and a card menu with publish/unpublish items and a
    publish-blocked-disabled Delete item. Both dialogs post to the Plan 02 routes.
    Both resources expose the publish state to the frontend.
  </done>
</task>

<task type="auto">
  <name>Task 2: Public library index, public recipe page, library card, guest layout, nav, translations</name>
  <files>resources/js/components/recipes/library-recipe-card.tsx, resources/js/layouts/guest-public-layout.tsx, resources/js/pages/library/index.tsx, resources/js/pages/library/show.tsx, resources/js/components/app-sidebar.tsx, resources/js/types/recipe.ts, lang/en/app.php, lang/el/app.php</files>
  <read_first>
    - resources/js/pages/recipes/index.tsx (the full structure to mirror — header, search input, RecipeFilters, skeleton grid, card grid, pagination, the .layout static property)
    - resources/js/components/recipes/recipe-card.tsx (the card to base LibraryRecipeCard on — note it links to recipesShow; the library card links to libraryShow)
    - resources/js/layouts/app/app-sidebar-layout.tsx (layout composition — to model GuestPublicLayout)
    - resources/js/components/app-sidebar.tsx (the mainNavItems array to extend)
    - resources/js/components/app-logo.tsx (the AppLogo component used in the guest header)
    - resources/js/pages/library/index.tsx and resources/js/pages/library/show.tsx (the Plan 01 stubs being replaced)
    - lang/en/app.php and lang/el/app.php (existing structure — where the new `library` section and the `recipes` publish keys go; confirm EN/EL parity)
    - .planning/phases/06-publishing-public-library/06-UI-SPEC.md (Surface 3, Surface 4, the Copywriting Contract table, Responsive Behavior, Accessibility)
  </read_first>
  <action>
    Add Phase 6 translation keys to BOTH lang/en/app.php and lang/el/app.php using
    the exact copy from the 06-UI-SPEC.md "Copywriting Contract" table.

    New top-level `library` section:
      EN: 'library' => [ 'nav' => 'Library', 'page_title' => 'Library',
        'by' => 'by :name',
        'empty_heading' => 'No published recipes yet',
        'empty_body' => 'Be the first to share a recipe with the community.',
        'empty_search_heading' => 'No recipes match your search',
        'empty_search_body' => 'Try different keywords or adjust your filters.',
        'published_label' => 'Published :date',
        'sign_in' => 'Sign in' ]
      EL: 'library' => [ 'nav' => 'Βιβλιοθήκη', 'page_title' => 'Βιβλιοθήκη',
        'by' => 'από :name',
        'empty_heading' => 'Δεν υπάρχουν ακόμα δημοσιευμένες συνταγές',
        'empty_body' => 'Γίνετε ο πρώτος που θα μοιραστεί μια συνταγή με την κοινότητα.',
        'empty_search_heading' => 'Δεν βρέθηκαν συνταγές',
        'empty_search_body' => 'Δοκιμάστε διαφορετικές λέξεις ή αλλάξτε τα φίλτρα σας.',
        'published_label' => 'Δημοσιεύθηκε :date',
        'sign_in' => 'Σύνδεση' ]

    Extend the existing `recipes` section in both files:
      EN: 'publish_btn' => 'Publish recipe',
        'unpublish_btn' => 'Unpublish recipe', 'published_badge' => 'Published',
        'publish_dialog_title' => 'Publish Recipe',
        'publish_dialog_body' => 'Choose the version to make public. This version will be visible to all users.',
        'publish_dialog_confirm' => 'Publish recipe',
        'publish_dialog_dismiss' => "Don't publish",
        'unpublish_dialog_title' => 'Unpublish Recipe',
        'unpublish_dialog_body' => 'This recipe will be removed from the public library. Your recipe data is unchanged.',
        'unpublish_dialog_confirm' => 'Unpublish recipe',
        'unpublish_dialog_dismiss' => 'Keep published',
        'delete_blocked_tooltip' => 'Unpublish this recipe before deleting it.',
        'delete_blocked_dialog_title' => 'Cannot Delete Published Recipe',
        'sub_recipe_not_published' => ':name must be published before this recipe can be published.',
        'update_public_version_cue' => 'New version available — update the public version?',
        'update_public_version_btn' => 'Update to current',
        'update_version_toast' => 'Public version updated.',
        'publish_toast' => 'Recipe published.'
      EL: 'publish_btn' => 'Δημοσίευση συνταγής',
        'unpublish_btn' => 'Κατάργηση δημοσίευσης συνταγής',
        'published_badge' => 'Δημοσιευμένη',
        'publish_dialog_title' => 'Δημοσίευση Συνταγής',
        'publish_dialog_body' => 'Επιλέξτε την έκδοση που θα γίνει δημόσια. Η έκδοση αυτή θα είναι ορατή σε όλους τους χρήστες.',
        'publish_dialog_confirm' => 'Δημοσίευση συνταγής',
        'publish_dialog_dismiss' => 'Χωρίς δημοσίευση',
        'unpublish_dialog_title' => 'Κατάργηση Δημοσίευσης Συνταγής',
        'unpublish_dialog_body' => 'Αυτή η συνταγή θα αφαιρεθεί από τη δημόσια βιβλιοθήκη. Τα δεδομένα σας παραμένουν αναλλοίωτα.',
        'unpublish_dialog_confirm' => 'Κατάργηση δημοσίευσης συνταγής',
        'unpublish_dialog_dismiss' => 'Διατήρηση δημοσίευσης',
        'delete_blocked_tooltip' => 'Καταργήστε τη δημοσίευση πριν τη διαγράψετε.',
        'delete_blocked_dialog_title' => 'Δεν είναι δυνατή η Διαγραφή Δημοσιευμένης Συνταγής',
        'sub_recipe_not_published' => 'Το :name πρέπει να δημοσιευτεί πριν από αυτή τη συνταγή.',
        'update_public_version_cue' => 'Νέα έκδοση διαθέσιμη — ενημερώνετε τη δημόσια έκδοση;',
        'update_public_version_btn' => 'Ενημέρωση στην τρέχουσα',
        'update_version_toast' => 'Η δημόσια έκδοση ενημερώθηκε.',
        'publish_toast' => 'Η συνταγή δημοσιεύθηκε.'
    Every component key referenced in Task 1 and this task must match these exact
    paths (e.g. `t('app.recipes.publish_btn')`, `t('app.library.nav')`). No
    generic dismiss labels ("Cancel"/"OK"/"Close") anywhere.

    Add TS types to resources/js/types/recipe.ts:
    - `PublicRecipeCardData` = RecipeCardData minus `cost_per_portion` plus
      `author_name: string`.
    - `PublicRecipeData` = { id, name, slug, hero_image_path: string|null,
      cuisine: string|null, difficulty: string|null, total_time: number|null,
      author_name: string, published_at: string|null,
      sections: Array<{ name:string; lines:Array<{quantity:string;
      unit:string|null; name:string}>; steps:Array<{order:number;
      instruction:string}> }>, nutrition: Record<string,unknown>|null,
      allergen_slugs: string[] }.

    Create resources/js/components/recipes/library-recipe-card.tsx — a card
    variant based on RecipeCard but:
    - Props: { recipe: PublicRecipeCardData; className? }.
    - Wrap in `<Link href={libraryShow({ slug: recipe.slug }).url}>` (Wayfinder),
      NOT recipesShow.
    - DO NOT render the cost metric row (public — cost never appears).
    - Render the author line below the cuisine badge:
      `<span className="text-[14px] text-muted-foreground">{t('app.library.by',
      { name: recipe.author_name })}</span>`.
    - Keep hero image (UtensilsCrossedIcon placeholder), name, cuisine badge,
      time+difficulty, calories, AllergenIcons — same layout as RecipeCard.

    Create resources/js/layouts/guest-public-layout.tsx — a minimal layout for the
    public pages (UI-SPEC "Guest layout"):
    - A simple top bar: AppLogo on the left; a "Library" nav link (libraryIndex)
      and a "Sign in" link (text t('app.library.sign_in')) on the right. Show the
      "Sign in" link only when `usePage().props.auth.user === null`.
    - `{children}` rendered in a centered max-width container below the bar.
    - Used for BOTH library pages regardless of auth state, so the public surfaces
      never depend on the auth-only sidebar.

    Replace resources/js/pages/library/index.tsx (the Plan 01 stub) with the full
    public library page — mirror recipes/index.tsx structure (UI-SPEC Surface 3):
    - Props interface: { recipes: PaginatedRecipes<PublicRecipeCardData>; filters;
      cuisines; allergens; tags } — same shape as RecipeIndexProps but the
      paginated data is PublicRecipeCardData.
    - Page heading "Library" (t('app.library.page_title')) at text-[28px]
      font-semibold leading-tight. NO "New Recipe" CTA.
    - Search Input with SearchIcon inset-left, `w-full pl-9`, 300ms debounced
      `router.reload({ only:['recipes'], ... })`.
    - RecipeFilters component — pass cuisines/allergens/tags and the filter state;
      do NOT include the `ingredient` value in the reload `data` payload.
    - Card grid `grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3` rendering
      LibraryRecipeCard. 6-skeleton loading grid with aria-busy on the wrapper.
      Pagination identical to recipes/index.tsx.
    - Two empty states: no-recipes-at-all (t('app.library.empty_heading') /
      empty_body) and no-search-results (t('app.library.empty_search_heading') /
      empty_search_body). No CTA button (guests cannot publish).
    - Set `LibraryIndex.layout = (page) => <GuestPublicLayout>{page}</GuestPublicLayout>`.

    Replace resources/js/pages/library/show.tsx (the Plan 01 stub) with the
    dedicated public recipe page — NOT a reuse of the builder (UI-SPEC Surface 4).
    Props: { recipe: PublicRecipeData }. Sections top-to-bottom:
    1. Hero image — full-width aspect-video object-cover; UtensilsCrossedIcon
       placeholder on bg-muted when hero_image_path is null.
    2. Header block `py-8 px-4 md:px-8`: recipe name as `<h1>` 28px semibold
       leading-tight; "by {author_name}" 14px text-muted-foreground; "Published
       {date}" 14px text-muted-foreground (t('app.library.published_label'));
       cuisine Badge (variant="secondary") + difficulty Badge (variant="outline")
       + formatted time on one `flex gap-2` row.
    3. Metrics strip `border-y py-4 px-4 md:px-8 flex flex-wrap gap-6`: calories
       per portion ("X kcal / portion"); AllergenIcons row. NO cost, NO selling
       price, NO food-cost %.
    4. Sections + ingredient lines from `recipe.sections` — section heading `<h2>`
       20px semibold; each line 16px `flex gap-2` quantity+unit+name. No cost.
    5. Steps — `<ol>` ordered list `px-4 md:px-8 py-6`; step number 20px semibold,
       step text 16px leading-relaxed.
    6. Nutrition panel — shadcn `Tabs` "Per portion" / "Per 100 g" rendering from
       `recipe.nutrition`; 14px values.
    Explicitly OMIT: cost per portion, food cost %, selling price, chef notes,
    recipe tests, AI conversation, version history, Save/Recall, edit button.
    Use `<Head title={recipe.name} />`. Set `LibraryShow.layout = (page) =>
    <GuestPublicLayout>{page}</GuestPublicLayout>`.

    Edit resources/js/components/app-sidebar.tsx — add the library nav entry to
    `mainNavItems` AFTER the recipes entry:
      { title: t('app.library.nav'), href: libraryIndex().url, icon: Globe }
    Import `Globe` from lucide-react and `index as libraryIndex` from
    `@/routes/library`. The entry is always shown (no permission gate).
  </action>
  <verify>
    <automated>php -r '$en=require "lang/en/app.php"; $el=require "lang/el/app.php"; $f=function($a,$p="") use (&$f){$o=[];foreach($a as $k=>$v){if(is_array($v)){$o=array_merge($o,$f($v,$p.$k."."));}else{$o[]=$p.$k;}}return $o;}; $m=array_diff($f($en),$f($el)); echo empty($m)?"PARITY OK":"MISSING ".implode(",",$m);' && npm run build 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | tail -5 && php artisan test --compact --filter=LibraryBrowseTest 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File resources/js/components/recipes/library-recipe-card.tsx exists; grep `libraryShow` returns a match; grep `cost_per_portion` returns NO match
    - File resources/js/layouts/guest-public-layout.tsx exists; grep `AppLogo` returns a match; grep `auth.user` returns a match
    - resources/js/pages/library/index.tsx is > 80 lines; grep `LibraryRecipeCard` returns a match; grep `RecipeFilters` returns a match; grep `GuestPublicLayout` returns a match
    - grep `ingredient` in resources/js/pages/library/index.tsx returns NO match (ingredient filter excluded)
    - resources/js/pages/library/show.tsx is > 80 lines; grep `Tabs` returns a match; grep `cost` and `selling_price` and `notes` each return NO match
    - grep `<h1` in resources/js/pages/library/show.tsx returns a match; grep `<ol` returns a match
    - grep `libraryIndex` in resources/js/components/app-sidebar.tsx returns a match; grep `Globe` returns a match
    - The php parity check prints `PARITY OK`
    - `npm run build` completes without error; `npx tsc --noEmit` reports no type errors
    - `php artisan test --compact --filter=LibraryBrowseTest` is GREEN (Inertia pages now resolve)
  </acceptance_criteria>
  <done>
    The public library index renders a filterable, searchable, paginated grid of
    LibraryRecipeCards inside a guest-safe layout. The public recipe page is a
    dedicated clean page with hero, header, metrics strip, sections, steps, and a
    nutrition tabs panel — no cost/notes/tests/AI. The "Library" nav entry exists
    and EN/EL translations are complete with parity.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end publish + public library verification</name>
  <files>none — verification only</files>
  <what-built>
    The complete Phase 6 publish lifecycle and public library:
    - Publish/unpublish controls in the recipe builder header and the My Recipes
      card menu, with a version-picker dialog, an unpublish confirmation dialog,
      and an "Update to current" cue.
    - A guest-accessible public library at /library with search, filters, and
      pagination.
    - A dedicated public recipe page at /library/{slug} showing nutrition and
      allergens with cost/notes/tests/AI removed.
    - A "Library" nav entry and a guest-safe layout for the public pages.
  </what-built>
  <action>
    Verification-only checkpoint — no code is written. The executor pauses and the
    user runs the manual verification steps below, then types the resume signal.
  </action>
  <how-to-verify>
    Prerequisite: `php artisan test --compact` is fully green and `npm run build`
    (or `npm run dev`) is running.

    1. Log in. Open a recipe with at least 2 saved versions in the builder.
       Confirm a "Publish recipe" button shows in the header.
    2. Click "Publish recipe" → the version-picker dialog opens. Pick a version
       that is NOT the latest and confirm. Expect a "Recipe published." toast and
       a "Published" badge in the header.
    3. Make an edit and Save a new version. Confirm the "Update to current" cue
       banner appears below the header. Click "Update to current" → expect a
       "Public version updated." toast and the banner disappears.
    4. Go to My Recipes. Confirm the published recipe's card shows a "Published"
       badge and that the card menu's "Delete" item is disabled with a tooltip.
    5. Open the new "Library" nav entry. Confirm the published recipe appears,
       search by its name works, and a filter (e.g. cuisine) narrows results.
    6. Click the recipe card → the public recipe page opens at /library/{slug}.
       Confirm: hero, author "by {name}", published date, ingredient lines, steps,
       and a nutrition tabs panel are shown — and NO cost, selling price, food
       cost %, chef notes, tests, or AI chat appear.
    7. Open a private browser window (logged OUT). Visit /library directly →
       expect the library to load without a login redirect. Visit the public
       recipe slug URL → expect the public page to load for the guest.
    8. Visit a slug of an UNPUBLISHED recipe while logged out → expect a 404.
    9. Back in the builder, click "Unpublish recipe" → confirm dialog → confirm
       the recipe leaves the public library and the badge clears.
  </how-to-verify>
  <verify>
    Manual human verification only — see <how-to-verify>. No automated command;
    the full automated suite is exercised in Task 2's verify
    (`php artisan test --compact --filter=LibraryBrowseTest`) and the phase gate
    runs `php artisan test --compact`.
  </verify>
  <done>
    The user has completed all nine verification steps and confirmed: publish with
    version picker, update-to-current cue, unpublish, the publish-blocked Delete
    item, guest browse of /library, the clean public recipe page with no
    cost/notes/tests/AI, and the 404 for an unpublished slug. User typed the
    resume signal.
  </done>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `php artisan test --compact` — full suite green including PublishRecipeTest and
  LibraryBrowseTest.
- `npm run build` succeeds; `npx tsc --noEmit` reports no type errors.
- EN/EL translation parity check passes.
- Human-verify checkpoint approved — publish, update-version, unpublish, guest
  browse, and the privacy redactions all confirmed.
</verification>

<success_criteria>
- An owner can publish a recipe (version picker), see a Published badge, push a
  version update with one click, and unpublish — from both the builder and the
  My Recipes card (PUB-02, PUB-03).
- The Delete control is blocked while published (PUB-03).
- Any visitor, logged in or not, can browse and search the public library and
  open a clean public recipe page with no cost/notes/tests/AI exposed (PUB-04).
- A new recipe stays private until published (PUB-01).
</success_criteria>

<output>
After completion, create `.planning/phases/06-publishing-public-library/06-03-SUMMARY.md`
</output>
