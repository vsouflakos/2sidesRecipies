---
phase: 05-ai-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - config/prism.php
  - config/ai.php
  - .env.example
  - app/Models/RecipeConversation.php
  - app/Models/RecipeConversationMessage.php
  - app/Models/Recipe.php
  - database/migrations/XXXX_create_recipe_conversations_table.php
  - database/migrations/XXXX_create_recipe_conversation_messages_table.php
  - database/factories/RecipeConversationFactory.php
  - database/factories/RecipeConversationMessageFactory.php
  - tests/Pest.php
  - tests/Feature/Recipes/RecipeConversationTest.php
  - tests/Unit/Support/Recipes/AgentContextBuilderTest.php
  - app/Support/Recipes/PrismAdapter.php
autonomous: true
requirements: [AI-06]
user_setup:
  - service: ai-provider
    why: "The AI agent requires an LLM provider; deployer chooses (Anthropic, OpenAI, Gemini, Ollama, etc.) — no default is baked in"
    env_vars:
      - name: AI_PROVIDER
        source: "Choose a provider name matching a key in config/prism.php (e.g. 'anthropic', 'openai'). Leave empty to disable the AI feature."
      - name: AI_MODEL
        source: "Model identifier for the chosen provider (e.g. 'claude-3-5-sonnet-20241022', 'gpt-4o')"
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (only if AI_PROVIDER=anthropic)"
      - name: OPENAI_API_KEY
        source: "OpenAI Platform -> API keys (only if AI_PROVIDER=openai)"

must_haves:
  truths:
    - "The prism-php/prism package is installed and its config file is published"
    - "config('ai.provider') and config('ai.model') resolve from env with empty defaults (no baked-in vendor)"
    - "A recipe has exactly one RecipeConversation (hasOne); a conversation has many ordered messages"
    - "A conversation message stores role, content, and a proposal_state JSON column"
    - "The Wave 0 test suite for AI-01..AI-07 exists with real assertions and is RED (routes/services not yet built)"
  artifacts:
    - path: "config/ai.php"
      provides: "App-level AI config — provider, model, max_tokens, context budget"
      contains: "'provider' => env('AI_PROVIDER'"
    - path: "config/prism.php"
      provides: "Prism multi-provider config (published)"
      contains: "providers"
    - path: "app/Models/RecipeConversation.php"
      provides: "One-thread-per-recipe conversation model"
      contains: "function messages"
    - path: "app/Models/RecipeConversationMessage.php"
      provides: "Conversation message model with proposal_state cast"
      contains: "proposal_state"
    - path: "app/Support/Recipes/PrismAdapter.php"
      provides: "Thin provider-agnostic wrapper reading config/ai.php"
      contains: "function provider"
    - path: "tests/Feature/Recipes/RecipeConversationTest.php"
      provides: "Wave 0 feature test suite for AI-01..AI-07"
      contains: "Prism::fake"
    - path: "tests/Unit/Support/Recipes/AgentContextBuilderTest.php"
      provides: "Wave 0 unit test for context serialization (AI-02)"
      contains: "AgentContextBuilder"
  key_links:
    - from: "config/ai.php"
      to: "config/prism.php"
      via: "provider name string matches a providers[] key"
      pattern: "env\\('AI_PROVIDER'"
    - from: "app/Models/Recipe.php"
      to: "RecipeConversation"
      via: "hasOne relation"
      pattern: "hasOne\\(RecipeConversation"
---

<objective>
Install the AI dependency, establish the provider-agnostic configuration layer, create the
conversation data model, and scaffold the Wave 0 test suite.

This plan delivers AI-06 (provider configurable via a provider-agnostic adapter) and lays the
foundation every later Phase 5 plan builds on. It introduces the project's first AI dependency.

Purpose: A swappable LLM provider exists as a config change with no default vendor; the
conversation thread + message tables exist; later waves have a concrete RED test target.
Output: prism-php/prism installed, config/prism.php + config/ai.php, two migrations + models +
factories, a thin PrismAdapter, and the RED Wave 0 test suite.
</objective>

<execution_context>
@C:/Users/VasilisSouflakos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/VasilisSouflakos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-agent/05-CONTEXT.md
@.planning/phases/05-ai-agent/05-RESEARCH.md
@.planning/phases/05-ai-agent/05-VALIDATION.md

<interfaces>
<!-- Existing code the executor must respect. No exploration needed — use these directly. -->

From app/Models/Recipe.php — relations already present (add a new one alongside them):
```php
public function draft(): HasOne;        // App\Models\RecipeDraft
public function tests(): HasMany;       // App\Models\RecipeTest
public function currentVersion(): BelongsTo;  // App\Models\RecipeVersion
```

From app/Support/Recipes/RecipeDraftManager.php — the path agent edits will later use:
```php
public function applyEdit(RecipeDraft $draft, string $action, array $newData): void;
public function recall(RecipeDraft $draft, int $expectedSequence): array;
```

Existing route names already registered in routes/web.php (recipe block — for Wave 0 tests):
```
recipes.show          GET    recipes/{recipe}
recipes.draft.update  PUT    recipes/{recipe}/draft
recipes.duplicate     POST   recipes/{recipe}/duplicate
```

Prism PHP test pattern (from 05-RESEARCH.md, package not yet installed):
```php
use Prism\Prism\Facades\Prism;
use Prism\Prism\Testing\TextResponseFake;
use Prism\Prism\Enums\FinishReason;

Prism::fake([
    TextResponseFake::make()
        ->withText('...')
        ->withFinishReason(FinishReason::Stop),
]);
Prism::assertCallCount(1);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Prism, publish config, add app-level AI config</name>
  <read_first>
    - composer.json (current dependency list — confirm no LLM SDK present)
    - config/services.php (existing config style)
    - .env.example (current env var documentation style)
    - .planning/phases/05-ai-agent/05-RESEARCH.md (Prism install + config sections, lines 125-145 and 513-547)
  </read_first>
  <action>
    Install Prism PHP and create the two-layer config (Prism config for provider API keys,
    app config for provider/model selection — keeps the abstraction provider-agnostic).

    1. Run: `composer require prism-php/prism` (this is the approved new dependency).
    2. Run: `php artisan vendor:publish --tag=prism-config` to publish `config/prism.php`.
       If the tag differs, run `php artisan vendor:publish --provider="Prism\Prism\PrismServiceProvider"`.
    3. Create `config/ai.php` verbatim with this content:
       ```php
       <?php

       return [
           // Provider name MUST match a key in config/prism.php 'providers'.
           // Empty string means "not configured" — the "Ask AI" UI is then hidden.
           'provider' => env('AI_PROVIDER', ''),
           'model' => env('AI_MODEL', ''),
           'max_tokens' => (int) env('AI_MAX_TOKENS', 4096),
           // Character budget for the serialized context payload (AI-02 truncation guard).
           'context_budget_chars' => (int) env('AI_CONTEXT_BUDGET_CHARS', 80000),
       ];
       ```
    4. Confirm `config/prism.php` has a `providers` array with `anthropic` and `openai` entries
       each reading `env('ANTHROPIC_API_KEY', '')` / `env('OPENAI_API_KEY', '')`. If the published
       file lacks one of these provider keys, add it.
    5. Append to `.env.example` (after the existing variables, with a blank line + comment header):
       ```
       # AI Agent (Phase 5) — leave AI_PROVIDER empty to disable the AI chat feature
       AI_PROVIDER=
       AI_MODEL=
       AI_MAX_TOKENS=4096
       AI_CONTEXT_BUDGET_CHARS=80000
       ANTHROPIC_API_KEY=
       OPENAI_API_KEY=
       ```
    Do NOT set any default provider/model — the deployer must choose (CONTEXT.md locked decision).
  </action>
  <verify>
    <automated>php -r "exit(class_exists('Prism\\Prism\\Prism') ? 0 : 1);" && php artisan config:show ai.provider && php artisan config:show ai.model</automated>
  </verify>
  <acceptance_criteria>
    - composer.json contains `"prism-php/prism"`
    - File config/prism.php exists and contains the string `providers`
    - File config/ai.php exists and contains `'provider' => env('AI_PROVIDER', '')`
    - File config/ai.php contains `'context_budget_chars'`
    - .env.example contains `AI_PROVIDER=` and `AI_MODEL=` and `ANTHROPIC_API_KEY=`
    - `php artisan config:show ai.provider` runs without error
  </acceptance_criteria>
  <done>Prism installed, config/prism.php published, config/ai.php created with empty-default provider/model, env vars documented.</done>
</task>

<task type="auto">
  <name>Task 2: Conversation data model — migrations, models, factories</name>
  <read_first>
    - app/Models/Recipe.php (relation declaration style — add new relation alongside draft/tests)
    - app/Models/RecipeTest.php (model structure: fillable, casts() method style, factory wiring)
    - database/migrations (look at a recent recipe migration, e.g. the recipe_tests migration, for column/FK conventions)
    - database/factories/RecipeTestFactory.php (factory style)
    - .planning/codebase/CONVENTIONS.md (PHP model conventions)
  </read_first>
  <action>
    Create two tables and their models for the one-thread-per-recipe conversation.

    1. Create migration `create_recipe_conversations_table`:
       - `id` (bigIncrements)
       - `recipe_id` — `foreignId('recipe_id')->constrained()->cascadeOnDelete()`
       - `timestamps`
       - Add `$table->unique('recipe_id');` — ONE conversation per recipe (CONTEXT.md locked decision).
    2. Create migration `create_recipe_conversation_messages_table`:
       - `id` (bigIncrements)
       - `recipe_conversation_id` — `foreignId(...)->constrained()->cascadeOnDelete()`
       - `role` — `string('role')` (values: 'user', 'assistant', 'tool_proposal')
       - `content` — `text('content')`
       - `proposal_state` — `json('proposal_state')->nullable()` — holds `{action, data, status, summary, kind}`
         where status is one of pending|applied|dismissed|failed and kind is one of edit|variant
       - `timestamps`
       - Index: `$table->index(['recipe_conversation_id', 'created_at']);`
    3. Create model `app/Models/RecipeConversation.php`:
       - `protected $fillable = ['recipe_id'];`
       - `public function recipe(): BelongsTo` → `belongsTo(Recipe::class)`
       - `public function messages(): HasMany` → `hasMany(RecipeConversationMessage::class)->orderBy('created_at')`
       - `use HasFactory;` with the RecipeConversationFactory docblock.
    4. Create model `app/Models/RecipeConversationMessage.php`:
       - `protected $fillable = ['recipe_conversation_id', 'role', 'content', 'proposal_state'];`
       - `protected function casts(): array { return ['proposal_state' => 'array']; }`
       - `public function conversation(): BelongsTo` → `belongsTo(RecipeConversation::class, 'recipe_conversation_id')`
       - `use HasFactory;`
    5. Add to `app/Models/Recipe.php` a relation alongside `draft()`:
       ```php
       public function conversation(): HasOne
       {
           return $this->hasOne(RecipeConversation::class);
       }
       ```
    6. Create `database/factories/RecipeConversationFactory.php` and
       `database/factories/RecipeConversationMessageFactory.php` following RecipeTestFactory style.
       The message factory: `role` defaults to 'user', `content` a faker sentence, `proposal_state` null.
       Add a `->proposal()` state that sets role to 'tool_proposal' and proposal_state to
       `['action' => 'update_metadata', 'data' => [], 'status' => 'pending', 'summary' => 'Reduce sugar', 'kind' => 'edit']`.
    Use `php artisan make:model` / `make:migration` / `make:factory` with `--no-interaction`.
    Run `php artisan migrate` after creating the migrations.
  </action>
  <verify>
    <automated>php artisan migrate --no-interaction && php -r "require 'vendor/autoload.php'; \$a=new ReflectionClass('App\\Models\\RecipeConversationMessage'); exit(0);" && php artisan test --compact --filter=RecipeConversation 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - A migration file matching `*create_recipe_conversations_table*` exists and contains `unique('recipe_id')`
    - A migration file matching `*create_recipe_conversation_messages_table*` exists and contains `proposal_state`
    - app/Models/RecipeConversation.php exists and contains `function messages`
    - app/Models/RecipeConversationMessage.php exists and contains `'proposal_state' => 'array'`
    - app/Models/Recipe.php contains `hasOne(RecipeConversation`
    - database/factories/RecipeConversationMessageFactory.php exists and contains `proposal`
    - `php artisan migrate` completes without error
  </acceptance_criteria>
  <done>Two tables migrated, two models with relations, Recipe->conversation hasOne, factories with a proposal state.</done>
</task>

<task type="auto">
  <name>Task 3: PrismAdapter + Wave 0 RED test suite</name>
  <read_first>
    - config/ai.php (the file created in Task 1)
    - tests/Feature/Recipes/RecipeTestController related test (any existing recipe feature test for actingAs + route patterns)
    - tests/Pest.php (current shared test setup)
    - .planning/phases/05-ai-agent/05-RESEARCH.md (Validation Architecture section, lines 634-668; Prism fake example lines 568-595)
    - .planning/phases/05-ai-agent/05-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <action>
    Create the thin provider-agnostic adapter and the RED Wave 0 test suite. The tests must
    write REAL assertions (no skip/markTestIncomplete) — they will be RED because the
    controllers/services are built in plans 02-04. This gives those plans a concrete GREEN target.

    1. Create `app/Support/Recipes/PrismAdapter.php`:
       ```php
       <?php

       namespace App\Support\Recipes;

       class PrismAdapter
       {
           public function provider(): string
           {
               return (string) config('ai.provider', '');
           }

           public function model(): string
           {
               return (string) config('ai.model', '');
           }

           public function maxTokens(): int
           {
               return (int) config('ai.max_tokens', 4096);
           }

           /** The AI feature is available only when both provider and model are configured. */
           public function isConfigured(): bool
           {
               return filled($this->provider()) && filled($this->model());
           }
       }
       ```
    2. Create `tests/Feature/Recipes/RecipeConversationTest.php` (Pest) covering AI-01..AI-07.
       In a `beforeEach`, set `config(['ai.provider' => 'anthropic', 'ai.model' => 'claude-3-5-sonnet-20241022'])`
       and call `Prism::fake([...])` with a `TextResponseFake`. Write these tests with REAL assertions
       (they reference routes built in later plans — expect RED until then):
       - `it('streams an assistant response to a chat message', ...)` — posts to
         `route('recipes.conversation.stream', $recipe)` with `['message' => 'How do I improve this?']`,
         asserts `assertOk()` and that an assistant message row was persisted (AI-01, AI-03).
       - `it('hides the AI feature when no provider is configured', ...)` — sets `ai.provider` to '',
         GETs `route('recipes.show', $recipe)`, asserts the Inertia prop `ai_enabled` is `false` (AI-06).
       - `it('exposes the AI feature when a provider is configured', ...)` — asserts `ai_enabled` is `true`.
       - `it('applies an accepted suggestion to the working draft', ...)` — creates a recipe with a
         draft and a `tool_proposal` message (factory `->proposal()` state), posts to
         `route('recipes.conversation.apply', [$recipe, $message])`, asserts the draft's
         `edit_sequence` incremented by 1 (AI-04).
       - `it('rejects an invalid suggestion and records a failed proposal state', ...)` — applies a
         proposal whose data fails `UpdateRecipeDraftRequest` validation, asserts the draft is
         unchanged (`edit_sequence` did not change) and the message `proposal_state['status']`
         is `'failed'` (AI-07).
       - `it('creates a recipe variant as a new independent recipe', ...)` — posts to
         `route('recipes.conversation.variant', [$recipe, $message])`, asserts a new Recipe row
         exists owned by the same user (AI-05).
       - `it('forbids accessing another user conversation', ...)` — a non-owner posting a message
         gets `403`.
       Use `App\Models\Recipe`, `User`, `RecipeDraft` factories. For tests referencing routes that
       do not yet exist, the test will error/fail RED — that is the intended Wave 0 state.
    3. Create `tests/Unit/Support/Recipes/AgentContextBuilderTest.php` (Pest) covering AI-02:
       - `it('includes the recipe draft, chef notes, and all test feedback in the system prompt', ...)`
         — builds a recipe with a draft (notes set), 2 RecipeTest records, calls
         `app(App\Support\Recipes\AgentContextBuilder::class)->buildSystemPrompt($recipe)`, asserts
         the returned string contains the chef notes text and both tests' verdict values.
       - `it('rebuilds messages from persisted conversation history', ...)` — asserts
         `buildMessages()` returns one entry per user/assistant message in created order.
       These reference `AgentContextBuilder` (built in plan 02) — RED until then.
    4. If `Prism::fake()` needs shared setup, add a helper but do NOT make live API calls.
    Run the suite to confirm it is RED (compiles, real assertions, fails because routes/services
    are not yet built).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=RecipeConversation 2>&1 | tail -8; php artisan test --compact --filter=AgentContextBuilder 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - app/Support/Recipes/PrismAdapter.php exists and contains `function isConfigured`
    - tests/Feature/Recipes/RecipeConversationTest.php exists and contains `Prism::fake`
    - tests/Feature/Recipes/RecipeConversationTest.php contains `recipes.conversation.stream`
    - tests/Feature/Recipes/RecipeConversationTest.php contains `recipes.conversation.apply`
    - tests/Feature/Recipes/RecipeConversationTest.php contains `recipes.conversation.variant`
    - tests/Feature/Recipes/RecipeConversationTest.php contains `ai_enabled`
    - tests/Unit/Support/Recipes/AgentContextBuilderTest.php exists and contains `AgentContextBuilder`
    - Neither test file contains `markTestIncomplete` or `->skip(`
    - `php artisan test --filter=RecipeConversation` runs (tests are RED, which is expected for Wave 0)
  </acceptance_criteria>
  <done>PrismAdapter created; Wave 0 feature + unit test suites exist with real assertions, RED, giving plans 02-04 concrete GREEN targets.</done>
</task>

</tasks>

<verification>
- `composer show prism-php/prism` confirms the package is installed
- `php artisan migrate:status` shows both new conversation migrations applied
- `php artisan test --filter=RecipeConversation` runs and is RED (no fatal parse errors)
- `php artisan config:show ai` shows provider/model with empty defaults
</verification>

<success_criteria>
- prism-php/prism installed; config/prism.php published; config/ai.php created with no default vendor (AI-06)
- recipe_conversations + recipe_conversation_messages tables exist with models, factories, relations
- Recipe->conversation() hasOne relation present
- PrismAdapter resolves provider/model/maxTokens and reports isConfigured()
- Wave 0 test suite (RecipeConversationTest + AgentContextBuilderTest) exists, RED, real assertions
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-agent/05-01-SUMMARY.md`
</output>
